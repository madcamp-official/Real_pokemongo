# 리빙 도감 (가칭) 백엔드 기능명세서

### 현실 생물 채집·동정·육성 서비스 — "현실판 포켓몬고"

---

## 0. 문서 정보

| 항목 | 내용 |
| --- | --- |
| 문서 목적 | 「리빙도감 FE/BE 기능명세서 v0.3」에서 **백엔드(서버) / AI 추론 서버** 담당 범위만 분리·재구성 |
| 기반 문서 | 리빙도감_FE_BE_기능명세서.md (v0.3) |
| 버전 | v1.0 (BE 단독판) |
| 우선순위 표기 | P0 = MVP 필수 · P1 = 정식 출시 목표 · P2 = 이후 확장 |
| 인프라 전제 | AI 추론(사진 보정, 종 동정 등)은 전량 자체 GPU 서버(백엔드)에서 처리 |
| 범위 제외 | F15(오프라인 모드)는 스코프 제외, 온디바이스 폴백 모델 개발 항목 전체 삭제. 번호 결번 유지 |
| 변경 이력 | v1.1: F19(터치 기반 사전 위험 경고) 신규 추가 / v1.2: 보호자·자녀 2단계 계정 모델을 단일 사용자 계정으로 통합, F13(보호자 대시보드) 제거 |

---

## 1. 백엔드 역할 원칙

- 인증/동의 관리
- 전체 AI 추론 파이프라인(사진 보정 정밀 처리, 종 동정, 3D 에셋 매칭 로직)
- 데이터베이스(도감/개체/친밀도/퀘스트/지도 등)
- 동기화·충돌 해결
- 알림 스케줄링
- 콘텐츠(종 카드) 관리

### 1.1 인프라 스펙 (AI 추론 서버)

| 항목 | 사양 |
| --- | --- |
| GPU | RTX 3090 20GB × 1 |
| vCPU | 40 core |
| Memory | 50GB |
| Disk | 100GB |
| OS | Ubuntu 20.04 (20-2208-csv) |
| 역할 | F3(사진 보정 정밀 모델), F4(AI 동정 모델) 상시 추론 서비스. 트래픽 증가 시 큐잉/배치 처리 또는 스케일아웃 검토 대상 |

---

## 2. 기능별 백엔드 상세 명세

### F1. 온보딩 & 회원가입 · P0

> v1.2: 보호자/자녀 2단계 계정 구조를 제거하고 단일 사용자 계정 모델로 단순화.

- 사용자 계정/인증 처리(이메일 검증, 닉네임·아바타 포함 단일 가입 트랜잭션)
- 동의 이력 저장(법적 근거 확보용 타임스탬프·버전 관리)
- 게스트 세션 → 정식 계정 전환 시 데이터 마이그레이션 API

**제공 API**: `POST /auth/signup`, `POST /auth/consent`, `POST /session/guest/convert`

---

### F2. 생물 촬영 · P0

- 이미지 업로드 수신 API
- 원본/보정본 저장소(오브젝트 스토리지) 관리
- 업로드된 사진을 F3(보정)·F4(동정) 파이프라인으로 라우팅하는 오케스트레이션
- 메타데이터(촬영 시각/위치) 검증 및 저장

**제공 API**: `POST /sightings/upload` (multipart, 위치 메타데이터 optional) → 즉시 F3/F4 파이프라인 트리거 후 `sighting_id` 반환 (프론트는 폴링 또는 소켓으로 결과 수신)

---

### F3. 저품질 사진 보정 · P1 (AI 추론 서버 처리)

- GPU 서버에서 정밀 보정 모델 상시 구동: 모션 디블러링, 노출 보정, ROI 크롭·업스케일, 버스트 프레임 합성
- 보정 신뢰도가 낮으면 "재촬영 필요" 플래그를 프론트로 반환
- 보정 전/후 이미지 저장 정책(원본 폐기 여부 등) 관리
- 향후 모델 재학습용 데이터 수집 파이프라인(동의 범위 내 필터링 포함)

**제공 API(내부)**: `POST /vision/enhance` (F2 업로드에 연쇄 호출) → 응답에 `enhanced_image_ref`, `confidence`, `retake_suggested` 포함

---

### F4. AI 동정 · P0 (AI 추론 서버 처리)

- GPU 서버의 분류 모델 추론(상위 N개 후보 + 신뢰도 반환)
- 지역별 생물상 파인튜닝 모델 버전 관리
- 위험 생물 여부 플래그 판정 로직
- 동정 확정 시 도감(F5) 자동 등록 트랜잭션 처리
- 사용자 선택형 오답 데이터 수집(모델 개선용)

**제공 API**: `POST /identify` → `{candidates:[{species_id, confidence}], is_dangerous, needs_user_confirmation}`, `POST /identify/confirm`

---

### F5. 도감 · P0

- 사용자별 도감 진행 상태 저장·조회 API
- 전체 Species 마스터 데이터 대비 보유율 계산
- Creature(개체) 단위 레코드 관리(F4 동정 확정 시 자동 생성)
- 페이지네이션/필터링 쿼리 최적화

**제공 API**: `GET /dex?filter=...&sort=...`, `GET /dex/completion`, `GET /creatures/{id}`

---

### F6. 종 카드 콘텐츠 · P0

- Species 콘텐츠 마스터 DB(다국어 텍스트 버전 관리)
- 콘텐츠 CMS(운영자가 종 카드 내용을 등록/수정)
- 위험 생물 플래그 및 안전 수칙 데이터 별도 관리
- F16·F10에서 참조할 수 있도록 공용 콘텐츠 API 제공

**제공 API**: `GET /species/{id}/card`

---

### F7. 3D 모델 생성·렌더링 · P1

- 종 단위 3D 베이스 에셋(리깅 포함) 저장·버전 관리
- 개체별 색상 추출 로직(촬영 사진 → 사전 정의 컬러 슬롯 2~3개 매핑) 처리 후 결과를 메타데이터로 저장
- 무늬 프리셋(3~5종) 매칭 알고리즘
- 에셋 CDN 배포 파이프라인
- 포유류형/조류형/곤충형 등 템플릿 카탈로그 관리

**제공 API**: `GET /assets/species/{id}/base_model`, `GET /creatures/{id}/variation` (컬러 슬롯 값, 무늬 프리셋 ID 반환)

---

### F8. 배지 · 레벨 보상 · P1

- 활동 지표 집계(촬영 수, 신규 종 발견 수, 퀘스트 완료 수, 연속 접속일 등) 배치/실시간 처리
- 경험치·레벨 산정 로직
- 배지 획득 조건 판정 및 지급

**제공 API**: `GET /profile/xp`, `GET /badges`, `POST /badges/claim`

---

### F9. 친밀도(Bond) 시스템 · P1

- Creature별 Bond 수치 저장 및 증가 로직(상호작용 이벤트 수신·검증)
- Bond 구간별 해금 콘텐츠(모션 풀, 심화 종 카드 콘텐츠) 매핑 테이블
- 방치 시에도 감소 없음(단순 유지) 정책 구현
- 마지막 상호작용 시각 기준 "재회" 판정 로직

**제공 API**: `POST /creatures/{id}/interact`, `GET /creatures/{id}/bond`

---

### F10. 퀘스트 시스템 · P1

- 퀘스트 정의 데이터(조건/보상/기간) 관리
- 조건 자동 검증 로직(F6 서식지·활동시간대 데이터, F11 위치 데이터, F4 동정 이력과 대조)
- 난이도 자동 조정 알고리즘(도감 보유 종수 기준)
- F17 이벤트 연동 스케줄링
- 완료 시 F8 보상 지급 트랜잭션

**제공 API**: `GET /quests?active=true`, `POST /quests/{id}/progress` (또는 서버가 Sighting/위치 이벤트로부터 자동 판정), `POST /quests/{id}/claim`

---

### F11. 지도 & 탐험 기록 · P1

- 좌표 저장 시 반경 흐림 처리(geo-fuzzing) 로직(사용자 정밀도 설정 반영)
- 자주 방문 지역 클러스터링 연산
- 지도 데이터를 F10 퀘스트 조건 판정에 제공하는 내부 API
- 위치 데이터 보존 기간·삭제 정책 관리

**제공 API**: `GET /map/pins`, `GET /map/explored-regions` (내부: 퀘스트 엔진이 위치 데이터 조회)

---

### F12. 알림 & 재참여 유도 · P2

- 시간대/계절/개체 상태 기반 알림 트리거 규칙 엔진
- 발송 스케줄러(취침시간 제외 등 사용자 설정 반영)
- 알림 총량 제한 로직
- 푸시 발송 인프라(FCM/APNs 연동)

**제공 API**: `POST /notifications/preferences`(사용자 설정 수신), 서버 → 클라이언트 푸시 발송(딥링크 payload 포함)

---

### ~~F13. 보호자 대시보드 & 안전장치~~ · 제거됨 (v1.2)

> 단일 사용자 계정 모델로 전환하면서 보호자 전용 권한 분리, 사용 시간 제한, 보호자 알림을 스코프에서 제외했다. 데이터 수집 범위(위치·사진) 설정 저장 및 F2/F11 반영 로직은 F18(설정)로 흡수. 번호 결번 유지.

---

### F14. 소셜 기능 · P2

- 친구 관계 그래프 관리, 상대방 본인의 수락/거절 워크플로우
- 닉네임 기반 검색(개인식별정보 노출 방지 로직 포함)
- 읽기 전용 홈 가든 데이터 제공 API(타인에게 노출 가능한 필드만 필터링)
- 반응 스티커 화이트리스트 관리

**제공 API**: `POST /friends/request`, `POST /friends/approve`, `GET /friends/{id}/garden`(read-only), `POST /reactions/send`

---

### F16. 홈 가든 (3D 개체 전시 공간) · P1

- GardenLayout(타일 맵, 배치 좌표) 서버 동기화 및 계정 복원용 저장
- 데코레이션 언락 상태 관리(F8 연동)
- 타일-개체 호환 규칙 마스터 데이터 제공(서식지 정보 기반, F6과 공유)
- 작명 자격 조건(재만남 횟수 등) 판정 로직
- 오랜만 접속 시 "재회" 이벤트 트리거 신호 제공
- 닉네임(작명) 금칙어 서버 재검증(F13 연동)

**제공 API**: `GET/PUT /garden/layout`, `GET /garden/tile-compatibility`, `POST /creatures/{id}/name`(작명), `GET /creatures/{id}/status`(시간/계절 연산은 클라이언트가 수행하되 기준 데이터는 서버 제공)

---

### F17. 계절 · 한정 이벤트 · P2

- 이벤트 스케줄 관리(시작/종료 시각, 지역·계절 조건)
- 한정 종/데코레이션의 노출 범위 제어(동정 가능 여부를 F4 파이프라인에 임시 활성화)
- F10 퀘스트와의 연동 스케줄링
- 이벤트 종료 후에도 획득 데이터는 영구 보존되도록 정책 보장

**제공 API**: `GET /events/active` (내부: 이벤트 상태가 F4·F10 API 응답에 조건부로 반영)

---

### F18. 설정 & 계정 관리 · P0

- 클라우드 복원 API(도감/홈 가든 배치/Bond 수치 등 전체 데이터 번들링)
- 계정 삭제 시 법규 기준 데이터 파기 절차 실행(파기 로그 보관)
- 알림·위치/사진 수집 범위 등 사용자 설정 저장소(v1.2: 옛 F13 설정 항목 흡수)

**제공 API**: `GET /account/restore-bundle`, `DELETE /account`(파기 워크플로우 트리거)

---

### F19. 터치 기반 사전 위험 경고 (신규) · P1 (AI 추론 서버 처리)

> 카메라 프리뷰에서 사용자가 피사체를 터치하면, 셔터를 누르기 전에 위험 여부를 잠정적으로 알려주는 기능. F4(AI 동정) 정밀 파이프라인과는 별도의 경량/저해상도 추론 경로로 분리 운영한다.

- 프리뷰 탭 이벤트 전용 API 신규 추가 — 크롭된 저해상도 이미지를 받아 F4보다 가벼운 모델/설정으로 빠르게 추론(응답 속도 우선, 정확도는 후순위)
- 응답은 **잠정 추정치**로 취급 — 도감(F5) 자동 등록 트랜잭션을 트리거하지 않고, Creature 레코드도 생성하지 않음(F4 확정 동정과 분리)
- 위험 생물 마스터 데이터(F6과 공유)를 참조하여 `is_dangerous` 플래그 판정
- F3·F4와 동일한 단일 GPU 자원을 공유하므로, 정밀 동정(F4) 대비 낮은 우선순위 큐 또는 별도 경량 모델 서빙 슬롯 적용 검토 필요
- 향후 모델 개선을 위해 잠정 추정 결과와 실제 F4 확정 결과 간 오차 데이터 수집(선택)

**제공 API**: `POST /vision/preview-scan` (신규) → 터치 좌표 + 크롭 이미지 수신, `{species_guess, is_dangerous, confidence}` 반환

---

## 3. API 엔드포인트 전체 목록

| 엔드포인트 | 메서드 | 담당 | 관련 기능 |
| --- | --- | --- | --- |
| `/auth/signup` | POST | 백엔드 | F1 |
| `/auth/consent` | POST | 백엔드 | F1 |
| `/session/guest/convert` | POST | 백엔드 | F1 |
| `/sightings/upload` | POST | 백엔드 | F2 |
| `/vision/enhance` | POST (내부) | AI 추론 서버 | F3 |
| `/identify` | POST | AI 추론 서버 | F4 |
| `/identify/confirm` | POST | 백엔드 | F4 |
| `/dex`, `/dex/completion`, `/creatures/{id}` | GET | 백엔드 | F5 |
| `/species/{id}/card` | GET | 백엔드 | F6 |
| `/assets/species/{id}/base_model` | GET | 백엔드 | F7 |
| `/creatures/{id}/variation` | GET | 백엔드 | F7 |
| `/profile/xp`, `/badges`, `/badges/claim` | GET/POST | 백엔드 | F8 |
| `/creatures/{id}/interact`, `/bond` | POST/GET | 백엔드 | F9 |
| `/quests`, `/quests/{id}/progress`, `/quests/{id}/claim` | GET/POST | 백엔드 | F10 |
| `/map/pins`, `/map/explored-regions` | GET | 백엔드 | F11 |
| `/notifications/preferences` | POST | 백엔드 | F12 |
| `/friends/*`, `/reactions/send` | POST/GET | 백엔드 | F14 |
| `/garden/layout` | GET/PUT | 백엔드 | F16 |
| `/garden/tile-compatibility` | GET | 백엔드 | F16 |
| `/creatures/{id}/name` | POST | 백엔드 | F16 |
| `/creatures/{id}/status` | GET | 백엔드 | F16 |
| `/events/active` | GET | 백엔드 | F17 |
| `/account/restore-bundle` | GET | 백엔드 | F18 |
| `/account` | DELETE | 백엔드 | F18 |
| `/vision/preview-scan` | POST | AI 추론 서버 | F19 |

---

## 4. 백엔드 소유 데이터

| 데이터 | 저장 위치 | 비고 |
| --- | --- | --- |
| 보정/동정 결과 | 백엔드 | AI 추론 서버 산출물, DB 저장 |
| 도감/Creature/Bond | 백엔드 | 계정 복원 대상 |
| 촬영 원본 이미지 | 백엔드(영구, 오브젝트 스토리지) | 프론트 업로드 수신 후 저장 |
| 홈 가든 배치(GardenLayout) | 백엔드(동기화 저장) | 다중 기기 사용 시 충돌은 서버 기준 병합 |
| 사용자 설정/동의 이력 | 백엔드 | 법적 근거 보관 필수 |

---

## 5. 릴리즈 로드맵 (백엔드)

- **MVP(v1.0)**: 인증/동의(F1), 업로드(F2), AI 추론 서버 기본 구축(F4, F3는 후순위 가능), 도감/종 카드 DB(F5·F6), 설정·계정관리 최소 기능(F18)
- **정식 출시(v1.x)**: F3 정밀 보정 모델 가동, F8 보상 로직, F9 Bond 로직, F10 퀘스트 엔진, F11 geo-fuzzing
- **확장(v2.0)**: F12 규칙 엔진, F14 친구 그래프·승인 워크플로우, F17 이벤트 스케줄러

---

## 6. 백엔드 관점 리스크

1. **AI 추론 서버 단일 GPU 병목**: RTX 3090 1장으로 F3(보정)·F4(동정) 트래픽을 동시 처리해야 하므로, 동시 사용자 증가 시 큐잉 지연 발생 가능 — 배치 처리 최적화 또는 스케일아웃(추가 GPU) 계획 필요. F19(프리뷰 터치 스캔)가 추가되면서 촬영당 1회였던 호출 빈도가 촬영 전 다회 터치까지 늘어날 수 있어 병목이 더 심화될 수 있음 — 우선순위 큐 또는 별도 경량 서빙 슬롯 분리가 사실상 필수.
2. **네트워크 불안정 구간 대응**: 업로드 실패 시 타임아웃 정책 및 재시도 허용 범위 설계 필요.
3. **동기화 충돌**: 홈 가든 배치처럼 로컬 우선 저장 데이터는 다중 기기 사용 시 서버 기준 병합 정책을 프론트와 동일하게 구현해야 함.

---

*본 문서는 「리빙도감_FE_BE_기능명세서.md (v0.3)」에서 백엔드 담당 범위만 재구성한 문서이며, 상세 API 요청/응답 스키마와 DB 테이블 설계는 별도 문서로 후속 작성 예정.*
