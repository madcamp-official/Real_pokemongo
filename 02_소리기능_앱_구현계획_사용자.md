# 02. 소리 기능 앱 구현계획 — 사용자 담당

> 담당자: 사용자
>
> 작업 브랜치: `feature/audio-app`
>
> 시작 기준: `COMMON_DESIGN_COMMIT`
>
> 수정 범위: 원칙적으로 `app/`
>
> 계약 기준: `docs/audio/API_CONTRACT.md`, `docs/audio/STATE_MACHINE.md`, `docs/audio/fixtures/`

---

## 1. 목표

서버·GPU 완료를 기다리지 않고 고정 fixture를 이용해 다음 앱 흐름을 완성한다.

```text
소리 찾기 진입
→ 마이크 권한
→ 3~15초 녹음
→ 로컬 검사
→ 업로드
→ 종 후보 표시
→ 사용자 확정
→ 도감 반영
```

추가로 동정 후보 또는 도감에서 유사도 채점을 요청하고 결과를 표시한다.

기존 카메라, 사진 동정, 지도, 도감, 정원 기능의 동작과 로딩 UI를 변경하지 않는다.

---

## 2. 파일 소유 범위

주요 수정 경로:

```text
app/package.json
app/app.json
app/src/navigation/
app/src/screens/audio/
app/src/components/audio/
app/src/hooks/useAudioRecorder.ts
app/src/api/audio.ts
app/src/store/audioUploadQueueStore.ts
app/src/services/audioStorage.ts
app/src/types/api.ts
app/src/screens/onboarding/ConsentScreen.tsx
app/src/screens/SettingsScreen.tsx
```

수정 금지:

```text
seed-service/
docs/audio/API_CONTRACT.md
docs/audio/fixtures/
```

공통 계약 변경이 필요하면 `docs/audio/CHANGE_REQUESTS.md`에 요청만 남기고 구현을 멈춘다.

---

## 3. 0단계 — 시작 준비

- [ ] 현재 브랜치가 `feature/audio-app`인지 확인
- [ ] 시작점이 `COMMON_DESIGN_COMMIT`인지 확인
- [ ] `app/AGENTS.md`를 읽음
- [ ] Expo SDK 57의 정확한 `expo-audio` 공식 문서를 읽음
- [ ] 현재 앱 타입 검사가 통과하는지 확인
- [ ] 사진 촬영 → 동정 → 확정 흐름을 기준 영상 또는 체크리스트로 남김

완료 기준:

- 소리 기능을 추가하기 전 기존 기능 상태가 기록되어 있다.

---

## 4. 1단계 — 오디오 의존성과 권한

작업:

- Expo SDK 57과 호환되는 `expo-audio` 설치
- iOS 마이크 사용 설명 추가
- Android `RECORD_AUDIO` 권한 추가
- `expo-audio` config plugin 설정
- 백그라운드 녹음은 활성화하지 않음
- 마이크 권한 요청과 설정 이동 처리

권한 문구 예시:

```text
주변 생물의 소리를 녹음해 도감에서 찾아보기 위해 마이크를 사용합니다.
```

테스트:

- 최초 허용
- 최초 거부
- 다시 묻지 않음
- 설정에서 권한 철회
- 권한 거부 후 사진 기능 정상 사용

완료 기준:

- 사용자 버튼 입력 전에는 마이크가 활성화되지 않는다.
- 앱이 백그라운드로 가면 녹음이 종료된다.

---

## 5. 2단계 — 앱 API 타입과 fixture 어댑터

작업:

- `app/src/types/api.ts`에 오디오 요청·응답 타입 추가
- `app/src/api/audio.ts` 추가
- `docs/audio/fixtures`를 읽는 개발용 mock 어댑터 추가
- 실제 API와 mock이 같은 TypeScript 타입을 사용하게 구성
- 환경 설정으로 mock/실제 서버를 전환

필요 함수:

```text
uploadAudioSighting()
identifyAudioSighting()
confirmAudioIdentification()
scoreAudioSimilarity()
getSpeciesSounds()
deleteAudioSighting()
```

규칙:

- JSON 필드명은 공통 계약의 snake_case를 유지한다.
- 앱에서 임의로 후보를 만들어내지 않는다.
- 서버 오류와 `unknown` 결과를 구분한다.

완료 기준:

- 서버가 없어도 fixture 9개로 모든 화면 상태를 재현할 수 있다.

---

## 6. 3단계 — 녹음 상태 머신

구현 상태:

```text
idle
requesting_permission
ready
recording
local_checking
uploading
identifying
result
confirming
confirmed
failed
discarded
```

`useAudioRecorder` 책임:

- 녹음 시작과 정지
- 경과 시간
- 최소 3초 검사
- 15초 자동 종료
- 인터럽트 처리
- 임시 파일 경로 반환
- 녹음 세션 해제

별도 흐름 책임:

- 업로드와 동정은 녹음 훅 안에 직접 넣지 않는다.
- API 상태 머신 또는 화면 컨트롤러가 업로드·동정을 담당한다.

필수 방어:

- 녹음 버튼 연타 방지
- 중복 업로드 방지
- 동일 재시도에 같은 `client_recording_id` 사용
- 앱 백그라운드 전환 시 불완전 녹음 폐기
- 로그아웃 시 임시 파일 삭제
- 블루투스 또는 오디오 장치 변경 처리

완료 기준:

- 허용되지 않은 상태 전이가 발생하지 않는다.
- 녹음 중 앱 종료 후 재실행해도 녹음 상태로 굳지 않는다.

---

## 7. 4단계 — 소리 찾기 화면

권장 화면 구성:

```text
SoundIntroScreen
AudioRecorderScreen
AudioQualityResultScreen
AudioIdentifyResultScreen
AudioSimilarityResultScreen
ReferenceSoundPlayerScreen 또는 modal
```

진입:

- 메인 방사형 메뉴 또는 메인 내비게이션에 `소리 찾기` 추가
- 카메라 화면 내부 제스처와 섞지 않음

녹음 화면:

- 닫기
- 녹음 시작·정지
- `00:00 / 00:15`
- 녹음 상태 문구
- 음량 반응 또는 파형
- 안전 안내

로딩:

- 하나의 고정 로딩 컨테이너를 사용
- `확인 중 → 업로드 중 → 찾는 중` 문구만 변경
- 단계 전환 때 로딩 원을 제거·재생성하지 않음
- 컴포넌트 key를 상태마다 바꾸지 않음

완료 기준:

- 사용자가 현재 녹음, 업로드, 분석 중 어느 단계인지 항상 알 수 있다.
- 로딩 원이 사라졌다 나타나는 깜빡임이 없다.

---

## 8. 5단계 — 품질 실패 처리

지원 코드:

```text
TOO_SHORT
MOSTLY_SILENCE
TOO_NOISY
CLIPPED
SPEECH_DETECTED
MULTIPLE_OVERLAP
UNSUPPORTED_SOUND
NO_TARGET_ACTIVITY
```

처리:

- 사유별 한국어 설명
- 다시 녹음 버튼
- 사용자가 해결할 수 있는 한 문장 안내
- `SPEECH_DETECTED`에서는 저장하지 않았음을 알림
- 품질 실패를 네트워크 실패와 구분

완료 기준:

- 모든 fixture 실패 코드가 별도 문구와 행동으로 표시된다.

---

## 9. 6단계 — 동정 결과와 확정

표시:

- 후보 최대 3개
- 한국어 이름과 학명
- 신뢰도 `높음`, `보통`, `낮음`
- 대표 감지 구간
- 위험 여부
- 다른 후보 보기
- 다시 녹음
- 잘 모르겠어요
- 이 종으로 기록하기

확정 규칙:

- `확정`을 누르기 전 도감, 지도, 보상 상태를 변경하지 않는다.
- 확정 중 버튼을 비활성화한다.
- `confirmation_id`를 생성하고 재시도 시 유지한다.
- 네트워크 재시도 후 같은 결과를 안전하게 처리한다.
- 성공 후 관련 React Query 캐시만 무효화한다.

완료 기준:

- 같은 확정 요청을 반복해도 UI에 중복 보상이 나타나지 않는다.

---

## 10. 7단계 — 유사도 채점

진입:

- 소리 동정 후보의 `소리 비교하기`
- 지원 종의 도감 상세 화면

표시:

- 0~100점
- 등급
- 점수 신뢰도
- 가장 잘 맞은 구간
- 품질 피드백
- 참조 소리 듣기
- 다시 녹음
- “종일 확률이 아니라 참조 소리와의 유사도” 안내

금지:

- 점수를 동정 정확도라고 표시
- 점수 결과로 도감·지도·퀘스트·보상 변경
- 미지원 종에서 채점 버튼 활성화

완료 기준:

- 유사도 요청 전후 관찰 및 보상 상태가 동일하다.

---

## 11. 8단계 — 동의와 로컬 데이터

작업:

- 사진 동의와 분리된 오디오 녹음·처리 동의
- 위치 연결은 별도 선택
- 학습 사용 동의는 MVP에서 요청하지 않거나 기본 꺼짐
- 미확정 로컬 파일 삭제
- 로그아웃·계정 삭제 시 오디오 큐 정리

권장 로컬 저장:

- 앱 세션 중 임시 파일
- 재시도에 필요한 최소 메타데이터
- 원본 오디오를 영구 보관하지 않음

완료 기준:

- 사용자가 녹음을 폐기하면 앱에서 접근 가능한 임시 파일이 남지 않는다.

---

## 12. 9단계 — 앱 테스트

단위 테스트:

- 상태 머신 전이
- 3초 미만 차단
- 15초 자동 종료
- 버튼 연타 방지
- 재시도 ID 유지
- 로그아웃 정리

fixture 화면 테스트:

- 정상 업로드
- 품질 실패
- 높은 신뢰도
- 여러 후보
- unknown
- 유사도 성공
- 미지원 종
- 확정 성공
- 네트워크 실패

실기기 테스트:

- Android 마이크 권한
- 녹음 중 홈 이동
- 전화 또는 오디오 인터럽트
- 블루투스 연결·해제
- 느린 네트워크
- 화면 회전
- TalkBack

회귀 테스트:

- 사진 촬영 로딩
- 사진 업로드 큐
- 사진 동정·확정
- 지도
- 도감
- 정원
- 보상

완료 기준:

- 앱 타입 검사 통과
- fixture 전체 흐름 통과
- 기존 기능 회귀 테스트 통과

---

## 13. 권장 커밋 순서

```text
feat(audio-app): configure microphone permission and recording
feat(audio-app): add audio API contract and fixture adapter
feat(audio-app): add recording state machine
feat(audio-app): add identification result flow
feat(audio-app): add similarity scoring UI
test(audio-app): cover audio states and photo regressions
```

각 커밋은 `docs/audio`를 변경하지 않는다.

---

## 14. 앱 완료 및 팀 전달물

- [ ] 앱 브랜치 최신 커밋 SHA
- [ ] Android 실기기 테스트 결과
- [ ] fixture별 화면 캡처
- [ ] 남은 계약 변경 요청
- [ ] mock/실제 API 전환 방법
- [ ] 알려진 제한사항

완료 후 사용자가 `feature/audio-integration` 브랜치에서 서버·GPU 브랜치를 병합한다.
