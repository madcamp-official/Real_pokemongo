# Nature Go 전체 기능명세서 (AI 인수인계용)

> 기준 브랜치: `living-dex_ver2.6`
> 기준일: 2026-07-28
> 앱: Expo SDK 57 · React Native · TypeScript
> 서버: Fastify 5 · TypeScript · PostgreSQL(운영) / InMemory(개발 폴백)
> AI 서버: CAMP-3 GPU (`camp-3`, RTX 3090 24GB)

## 1. 제품 정의

Nature Go는 사용자가 실제 자연 생물을 관찰하고 기록하는 모바일 생태 탐험 앱이다. 사진 또는 주변의 실제 새소리로 종을 찾고, 사용자가 결과를 확정하면 도감·탐험 지도·퀘스트·배지·홈가든에 연결한다.

핵심 루프는 다음과 같다.

```text
시작/로그인
  → 관찰(사진 촬영 또는 조류 소리 녹음)
  → AI 후보·안전 안내 확인
  → 사용자가 종 확정
  → 관찰 저장
  → 도감·지도·퀘스트·배지·홈가든 갱신
```

앱은 어린 사용자도 사용할 수 있으므로, 정확성보다 안전과 개인정보 보호를 우선한다. 불확실한 AI 결과는 단정하지 않고 후보 또는 재관찰 안내로 처리한다.

## 2. 문서 사용 규칙

이 문서는 다른 AI가 전체 구조와 기능 범위를 이해하기 위한 상위 명세다.

- **완료**: 현재 앱과 서버에 연결된 동작
- **부분 구현**: 일부 화면·API·저장만 구현됐거나 외부 연동이 남은 동작
- **계획**: 제품 범위에는 있으나 아직 실제 실행 경로가 없는 동작
- 코드와 문서가 다르면 코드와 테스트를 먼저 확인하고, 확인된 내용으로 문서를 갱신한다.
- 소리 기능의 필드·상태·API는 `docs/audio/`와 [AI_소리_기반_조류_동정_기능명세서.md](AI_소리_기반_조류_동정_기능명세서.md)를 우선한다.

## 3. 제품 원칙과 불변 조건

1. **사용자 확정 전에는 기록하지 않는다.** 사진·소리의 AI 분석은 읽기 전용이고, 확정 API만 관찰·도감·보상 데이터를 변경한다.
2. **위험 안내가 종 정보보다 먼저다.** 위험 후보가 있으면 가까이 가지 말라는 안내를 우선 노출한다.
3. **버섯은 식용 판정을 하지 않는다.** 항상 눈으로만 관찰하도록 안내한다.
4. **위치는 선택 사항이다.** 위치 권한 거부나 획득 실패가 촬영·녹음 자체를 막으면 안 된다.
5. **사진의 EXIF/GPS는 서버에서 제거한다.** 외부 AI에는 정화된 이미지 바이트만 보낸다.
6. **타인 자원과 없는 자원은 동일한 404로 처리한다.** 관찰·사진·정원·개체·오디오에 소유자 정보를 누설하지 않는다.
7. **모든 멱등성 요청은 중복 보상을 만들지 않는다.** 사진/소리 확정, 배지 수령, 퀘스트 수령을 특히 보호한다.
8. **기존 사진 흐름을 소리 기능 때문에 변경하지 않는다.** 오디오 라우트와 상태는 사진 라우트와 분리한다.
9. **사용자 녹음의 사람 말소리는 분석·전사·학습하지 않는다.** 사람 말소리 우세 녹음은 거절하고 보관하지 않는다.
10. **비밀 값은 문서·로그·커밋에 넣지 않는다.** API 키, 토큰, DB 비밀번호, 서버 비밀번호는 `.env` 또는 비밀 관리 수단에서만 사용한다.

## 4. 전체 구현 상태

| 기능 | 상태 | 요약 |
|---|---|---|
| 온보딩·회원·게스트 | 완료 | 튜토리얼, 로그인, 가입, 동의, 게스트 JWT |
| 사진 촬영·갤러리 | 완료 | 단일/버스트 촬영, 줌, 갤러리, 업로드 큐 |
| 터치 사전 위험 스캔 | 완료 | 촬영 전 터치 위치의 빠른 잠정 후보·위험 표시 |
| 사진 AI 동정·확정 | 완료 | 후보, 안전 우선, 확정 뒤 관찰 저장 |
| 도감·종 카드·사진 갤러리 | 완료 | 분류, 완성도, 콘텐츠, 본인 촬영 기록 |
| 지도 탐험 | 부분 구현 | 카카오 지도, 현재 위치, 본인 관찰 핀 구현; 실제 미방문 영역 렌더링은 없음 |
| XP·배지·퀘스트 | 완료 | 진행, 완료, 명시적 수령, XP/레벨 |
| 홈가든 | 대부분 완료 | 2D 가든, 생물 배치, 장식, 작명, 친밀도; 장식 서버 동기화는 없음 |
| 설정·개인정보·계정 삭제 | 대부분 완료 | 서버 동기화 설정, 복원 요약, 로그아웃, 계정 삭제 |
| 조류 소리 동정 앱 | 완료(앱) | 녹음·품질 UI·후보·확정·유사도 UI·목업 API |
| 조류 소리 GPU 서버 | 계획 | BirdNET, 품질 검사, TTL 삭제, 참조 음원, 실제 `/audio/*` API |
| 푸시 알림 | 계획 | 설정 토글 외 실제 알림 없음 |
| 소셜/공유 | 계획 | 친구·정원 공유·반응 없음 |
| 계절 이벤트 | 계획 | 별도 화면·API 없음 |
| 3D 모델 | 계획 | 현재 홈가든은 2D 스프라이트 방식 |

## 5. 사용자와 내비게이션

### 5.1 계정 종류

| 유형 | 특징 |
|---|---|
| 일반 계정 | 이메일·비밀번호·닉네임·아바타를 가진 사용자 |
| 게스트 | 서버가 발급한 임시 사용자와 JWT로 주요 API를 사용 |
| 로그아웃 사용자 | 튜토리얼 완료 뒤 로그인/가입/게스트 시작 화면으로 이동 |

게스트 토큰이 없거나 만료되면 앱은 서버 게스트 세션을 한 번 복구한다. 일반 계정의 401은 게스트 계정으로 자동 전환하지 않고 세션을 정리한다.

### 5.2 화면 구조

```text
Root Stack
├─ Tutorial
├─ Choice
├─ Login
├─ Consent
├─ Signup
├─ Main Tabs
│  ├─ Map (기본 홈)
│  ├─ Camera
│  ├─ Sound
│  ├─ Dex
│  ├─ Garden
│  ├─ Rewards
│  └─ Settings
├─ SpeciesCard
├─ IdentifyResult
└─ PhotoViewer
```

- 기본 탭 바는 숨겨져 있고 지도 하단 중앙의 방사형 메뉴가 주요 진입점이다.
- 방사형 메뉴: 도감, 촬영, 소리 찾기, 홈가든, 퀘스트/보상. 설정은 메뉴 우상단에서 연다.
- 홈가든만 가로 화면으로 잠그고 나머지는 세로 화면을 사용한다.

## 6. 기능 명세

### F1. 온보딩·로그인·동의

**목적:** 사용자에게 앱의 자연 관찰 목적과 데이터 사용을 알리고, 인증된 세션을 만든다.

- 3단계 튜토리얼을 제공한다.
- 시작 화면에서 로그인, 계정 만들기, 게스트 시작을 선택한다.
- 회원가입 입력: 이메일 형식, 비밀번호 8자 이상, 비밀번호 확인, 닉네임 1~12자, 아바타 선택.
- 가입 전에 개인정보·위치·사진 동의를 순서대로 받는다.
- 액세스 토큰은 `expo-secure-store`에 저장하고, 일반 UI 상태는 Zustand persist로 저장한다.
- 게스트도 서버 JWT를 받아 도감·관찰·지도 API를 쓸 수 있다.

**부분 구현/후속:** 게스트를 정식 계정으로 바꿀 때 기존 관찰·도감·사진을 실제로 이전하는 서버 로직은 완성해야 한다.

### F2. 사진 촬영·갤러리 업로드

**목적:** 실제 생물 사진을 안전하게 서버 동정 흐름으로 보낸다.

- 후면 카메라를 사용한다.
- 짧은 탭은 단일 촬영, 길게 누르기는 약 260ms 간격 버스트 촬영이며 최대 8프레임이다.
- 핀치 줌과 1배/약 2배 전환을 제공한다.
- 갤러리에서는 이미지 1장을 선택할 수 있다.
- 위치 수집을 켠 경우에만 현재 기기 위치를 요청해 업로드에 첨부한다. 갤러리 EXIF의 좌표를 직접 전송하지 않는다.
- 촬영 파일은 로컬 영속 업로드 큐에 넣고, 앱 재실행 뒤에도 실패한 업로드를 재시도할 수 있다.
- 카메라/갤러리/위치 권한은 서로 독립적으로 요청한다.

### F3. 사진 정화·품질 보정

**목적:** 개인정보가 포함된 원본을 보호하고, 동정에 적절한 사진만 분석한다.

서버는 업로드 뒤 다음을 수행한다.

1. JPEG/PNG의 EXIF, GPS, 텍스트 메타데이터를 제거한다.
2. 버스트 사진이 비슷하면 평균 병합하고, 크게 다르면 가장 선명한 한 장을 택한다.
3. 흐림·노출을 계산해 품질과 `retake_suggested`를 만든다.
4. 저장/외부 AI 동정에는 정화·보정된 결과만 사용한다.

**부분 구현/후속:** 앱 결과 화면이 서버의 `retake_suggested`를 더 명확하게 안내하도록 연결할 수 있다.

### F4. 터치 기반 사전 스캔

**목적:** 촬영 전에 피사체를 터치하면 빠르게 잠정 종과 위험 가능성을 알려 준다.

- 카메라 프리뷰를 터치하면 1프레임을 촬영한다.
- 화면 좌표를 실제 사진 좌표로 바꾸고, 터치 주변 약 45% 영역을 잘라 224px JPEG로 축소한다.
- `/vision/preview-scan`에 보내 잠정 종명·확신도·위험 여부를 화면에 표시한다.
- 위험 후보면 진동과 안전 메시지를 먼저 보여 준다.
- 사전 스캔은 **읽기 전용**이다. 관찰, 도감, 지도, 퀘스트, XP, 보상을 절대 변경하지 않는다.

### F5. 사진 AI 동정·사용자 확정

**목적:** 사진으로 종 후보를 제공하되 AI의 불확실성을 사용자에게 투명하게 보여 준다.

```text
POST /sightings/upload
  → POST /identify
  → 결과 화면
  → POST /identify/confirm
```

- 동정 프로바이더는 어댑터 구조이며 현재 GPU BioCLIP 우선, 실패 시 설정된 다음 프로바이더로 폴백할 수 있다.
- 결과는 `high / medium / low / unknown` 확신도 정책으로 분류한다.
- 고확신도라도 위험 종이면 안전 안내를 가장 먼저 보여 준다.
- 중간/낮은 확신도는 후보를 비교하게 하고, 미확정은 재촬영을 안내한다.
- 확정 요청은 서버가 그 관찰에 저장한 후보 중 하나만 허용한다.
- 확정 성공 시에만 Observation, 도감, 개체, 퀘스트, 배지, XP, 지도 핀이 갱신된다.

### F6. 도감

**목적:** 사용자가 발견한 생물을 수집·학습할 수 있게 한다.

- 종 목록은 3열 그리드로 보여 주고, 발견 여부·썸네일·분류 필터를 제공한다.
- 도감 완성도는 발견 종 수와 전체 종 수로 계산한다.
- 정렬과 분류 필터를 지원한다.
- 관찰 확정 뒤 관련 쿼리를 새로고침해 도감에 반영한다.
- 미발견 종의 노출 수준은 서버 DTO와 제품 정책을 따른다.

### F7. 종 카드·나의 사진

**목적:** 종의 생태 정보와 안전 정보를 이해시키고, 사용자의 관찰을 다시 볼 수 있게 한다.

- 국명, 학명, 분류, 서식 환경, 활동 시간, 희귀도, 흥미 사실을 표시한다.
- 위험 종은 `safety_notes`를 최상단에 고정한다.
- 관찰 포인트, 퀴즈, 유사종을 보여 준다.
- 본인이 확정한 사진 관찰만 종별 사진 갤러리에 표시한다.
- 사진 URL은 관찰 ID에 묶인 짧은 수명 미디어 토큰으로 접근한다.

### F8. 지도·탐험

**목적:** 관찰 기록과 현재 위치를 실제 지도에서 볼 수 있게 한다.

- 카카오맵 WebView로 지도를 표시한다.
- 위치 권한이 있으면 현재 위치로 이동할 수 있다.
- 확정된 본인 관찰 중 좌표가 있는 기록만 핀으로 표시한다.
- 핀을 누르면 관찰 종과 상세 화면으로 이어지는 정보를 보여 준다.
- 위치 저장을 끈 경우 일반화된 지역 정보만 쓰거나 위치 정보를 비워야 한다.
- 홈 기준 반경 300m 안내 UI가 있다.

**미구현:** 실제 방문 영역을 누적·렌더링하는 `안 가본 곳` 지도 표현은 현재 제공하지 않는다.

### F9. XP·레벨·배지

**목적:** 관찰 학습을 보상하되 확률형 보상 없이 진행을 명확히 보여 준다.

- XP는 누적 기준으로 레벨과 다음 레벨까지 남은 XP를 표시한다.
- 배지는 해금(`unlocked`)과 수령(`claimed`)을 분리한다.
- 배지 수령은 명시적 API 호출이며 같은 배지를 중복 수령할 수 없다.
- 관찰 확정으로 발생하는 배지/퀘스트 진행과 수령 보상을 구분한다.
- 유사도 점수·터치 사전 스캔·미확정 AI 결과는 XP나 배지에 영향을 주지 않는다.

### F10. 퀘스트

**목적:** 자연 탐험 목표를 제공한다.

- 서버가 활성 퀘스트와 진행률을 제공한다.
- 관찰 확정 후 종·분류 조건을 바탕으로 진행을 계산한다.
- 상태는 `active`, `completed`, `claimed`이다.
- 완료 후에도 사용자가 수령해야 XP가 지급된다.
- 같은 퀘스트를 두 번 수령하면 안 된다.

### F11. 홈가든·개체 친밀도

**목적:** 도감에서 만난 생물을 2D 정원에 배치하고 관계를 표현한다.

- 현재 3D/GLB가 아닌 2D 배경과 PNG/SVG 스프라이트를 사용한다.
- 사용자가 해금한 생물 개체를 잔디·물웅덩이·흙·돌·꽃밭 등 호환 타일에 배치한다.
- 배치 모드에서는 후보 위치를 표시하고, 일반 모드에서는 흰색 원형 슬롯을 보여 주지 않는다.
- 생물은 배치 지점에서 아주 작은 범위만 천천히 움직인다. 별도 바닥 그림자는 렌더링하지 않는다.
- 드래그/줌/팬, 구역 해금, 기본 장식(예: 울타리·꽃밭)을 지원한다.
- 생물 배치는 서버 `GET/PUT /garden/layout`으로 저장한다.
- 장식은 현재 기기 로컬 저장이므로 여러 기기 동기화는 아직 없다.
- 개체 상태, 작명, Bond, 재회 여부를 조회하고 상호작용으로 Bond를 올린다.

### F12. 설정·개인정보·계정

**목적:** 사용자가 위치·사진·소리 권한과 자신의 데이터를 통제하게 한다.

- 위치 저장과 사진 사용 동의를 서버 개인정보 설정 API로 조회·변경한다.
- 소리 녹음은 별도 앱 내 동의와 운영체제 마이크 권한을 요구한다.
- 계정 복원 요약을 조회한다.
- 로그아웃은 토큰과 안전하게 폐기할 로컬 임시 데이터를 정리한다.
- 계정 삭제는 인증된 본인 데이터만 삭제하며, 다른 사용자 데이터에는 영향을 주면 안 된다.
- 데이터 내보내기/삭제 시 살아 있는 임시 오디오 메타데이터도 정책에 따라 포함한다.

### F13. 조류 소리 동정·유사도

**목적:** 사용자가 주변에서 들은 실제 새소리로 조류 후보를 찾고, 선택한 후보의 참조 소리와 음향 유사도를 확인하게 한다.

#### 승인된 MVP 범위

| 항목 | 결정 |
|---|---|
| 지원 대상 | 조류만 |
| 녹음 길이 | 최소 3초, 권장 6~10초, 최대 15초 |
| 분석 모델 | BirdNET 우선 |
| 분석 위치 | CAMP-3 GPU |
| 사용자 음원 보관 | 최대 24시간 뒤 자동 삭제 |
| 도감 등록 시점 | 사용자가 후보를 확정할 때만 |
| 유사도 부수효과 | 도감·지도·퀘스트·보상에 영향 없음 |
| 사용자 녹음 재생 | 제공하지 않음 |
| 참조 음원 | 라이선스가 검증된 종 음원만 재생 |

#### 앱 흐름

```text
소리 찾기 진입·동의
  → 마이크 권한
  → 전경 녹음(3~15초)
  → 로컬 길이 확인
  → 업로드·서버 품질 검사
  → BirdNET 후보 분석
  → 후보 선택
  ├─ 유사도 점수 보기(선택)
  └─ 종 확정 → audio 관찰 생성
```

- 한 번에 하나의 녹음만 가능하다.
- 백그라운드 전환, 통화/오디오 인터럽트, 입력 장치 손실 시 미완성 파일을 폐기한다.
- 앱 원본은 업로드 성공 즉시 삭제하고, 서버의 임시 분석본만 24시간 이내 보관한다.
- 로딩 UI는 상태마다 재마운트하지 않고 같은 오버레이의 문구만 변경해 깜빡임을 막는다.
- 품질 거절 코드: `TOO_SHORT`, `MOSTLY_SILENCE`, `TOO_NOISY`, `CLIPPED`, `SPEECH_DETECTED`, `MULTIPLE_OVERLAP`, `UNSUPPORTED_SOUND`, `NO_TARGET_ACTIVITY`.
- 사람 말소리 우세 녹음은 동정하지 않고 즉시 폐기한다.
- 유사도 0~100점은 실제 녹음과 종 참조 음원의 음향 유사도이며, 종일 확률이 아니다.

#### 제외 범위

- 사람 목소리로 새소리를 흉내 내는 연습/게임
- 사람 음성 전사·저장·학습
- 조류 이외 소리 동정
- 앱 내 모델 탑재

BirdNET은 사람의 흉내 실력을 평가하는 모델이 아니다. 흉내 게임은 별도 제품 모드·모델·데이터 동의·API 계약이 필요하므로 이 MVP에 섞으면 안 된다.

#### 구현 책임

| 영역 | 현재 상태 | 해야 할 일 |
|---|---|---|
| 앱 | 구현됨 | `SoundScreen`, 녹음 훅, API 클라이언트, 목업, 설정, 메뉴 진입점 |
| 기존 API 서버 | 계획 | 인증·소유권·오디오 라우트·임시 저장·TTL·관찰 확정 연동 |
| CAMP-3 GPU | 계획 | PCM 전처리, 품질 검사, BirdNET 추론, embedding 유사도 |
| 참조 음원 | 계획 | 라이선스·출처·attribution·embedding·짧은 수명 재생 URL |

정식 오디오 계약은 `docs/audio/API_CONTRACT.md`를 사용한다. 대표 API는 아래와 같다.

```text
POST   /audio/sightings/upload
POST   /audio/identify
POST   /audio/identify/confirm
POST   /audio/similarity/score
GET    /species/:species_id/sounds
DELETE /audio/sightings/:audio_sighting_id
```

## 7. 서버 API 범위

모든 보호된 API는 기존 JWT 세션을 사용한다. 아래는 기능 그룹 기준의 전체 경로다.

| 그룹 | 주요 API |
|---|---|
| 인증 | `POST /auth/signup`, `POST /auth/login`, `POST /session/guest`, `POST /session/guest/convert` |
| 사진 관찰 | `POST /sightings/upload`, `POST /identify`, `POST /identify/confirm` |
| 사전 스캔 | `POST /vision/preview-scan` |
| 도감·종 카드 | `GET /dex`, `GET /dex/completion`, `GET /species/:id/card`, `GET /species/:id/photos` |
| 미디어 | `GET /media/:observationId?mt=...` |
| 지도 | `GET /map.html`, `GET /map/pins`, `GET /map/explored-regions` |
| 퀘스트·보상 | `GET /quests`, `POST /quests/:questId/claim`, `GET /badges`, `POST /badges/:badgeId/claim`, `GET /profile/xp` |
| 개체·가든 | `GET/PUT /garden/layout`, `GET /garden/tile-compatibility`, `GET /creatures/:id/status`, `POST /creatures/:id/name`, `POST /creatures/:id/interact` |
| 개인정보·계정 | `GET /account/restore-bundle`, `GET/PATCH /account/privacy-settings`, `DELETE /account` |
| 오디오(구현 예정) | `/audio/*` 6개 계약 API와 `/species/:id/sounds` |

### API 공통 오류 규칙

```json
{
  "error": "machine_readable_code",
  "message": "사용자에게 보여 줄 안전한 설명",
  "retryable": false,
  "trace_id": "optional-trace-id"
}
```

- 401: 인증되지 않은 요청
- 404: 존재하지 않음 또는 타인 소유(동일 응답)
- 409: 중복 확정/이미 수령한 보상 등 상태 충돌
- 422: 파일·품질·입력은 유효하지만 도메인 규칙상 처리 불가
- 429: 속도/비용 제한
- 503: 외부 AI 또는 GPU 처리기 일시 불가. `unknown` 결과로 위장하지 않는다.

## 8. 핵심 데이터 모델

| 엔터티 | 핵심 필드/규칙 |
|---|---|
| User | 계정, 인증, 위치 저장 동의, 프로필 |
| ConsentRecord | 개인정보·사진·위치 동의 버전과 시각 |
| Taxon | 종 ID, 국명, 학명, 분류, 위험 태그, 콘텐츠 |
| Observation | 사용자·종·시각·좌표·지역·`modality: photo | audio` |
| Media | 정화된 사진 또는 임시 오디오 메타데이터, SHA-256, 보존 등급 |
| Collection/Creature | 도감 해금 상태, 종별 개체, 별명, Bond, 마지막 상호작용 |
| Quest/QuestProgress | 조건, 진행률, 완료/수령 시각 |
| Badge/EarnedBadge | 해금과 수령을 분리 |
| GardenLayout | 타일과 생물 개체의 `row`, `col` 배치 |
| AudioSighting | 소유자, client recording ID, 품질, 임시 파일 참조, 상태, 만료 시각 |
| AudioIdentificationResult | 후보 스냅샷, 모델/버전, 위치 prior 사용 여부 |
| SpeciesSoundReference | 라이선스 참조 음원, attribution, reference set/model version, embedding |

### 위치·미디어 보존

- 사진: 서버가 EXIF/GPS를 제거한 정화본을 저장하고, 원본은 정책에 따라 폐기한다.
- 정밀 좌표는 관찰 기능을 위해 필요한 경우만 보관하며, 사용자 설정과 조회 DTO에서 노출 범위를 분리한다.
- 사용자 오디오: 원본·변환본 모두 최대 24시간. 확정 관찰에도 오디오 바이트는 영구 미디어가 아니다.
- 참조 오디오: 계정 데이터와 분리된 라이선스 보존 정책을 사용한다.

## 9. 시스템 구조

```text
Expo React Native 앱
  ├─ SecureStore: JWT
  ├─ Zustand/MMKV: 화면·설정·업로드·가든 로컬 상태
  └─ Axios API client
          ↓
Fastify seed-service
  ├─ 인증·인가·소유권 확인
  ├─ 관찰/도감/퀘스트/보상 도메인
  ├─ PostgreSQL repositories
  ├─ 미디어 정화·단기 URL
  ├─ 사진 BioCLIP provider adapter
  └─ 오디오 gateway (구현 예정)
          ↓
CAMP-3 GPU
  ├─ 사진 BioCLIP 또는 외부 AI provider
  └─ BirdNET 조류 소리 품질·추론·embedding 서비스 (구현 예정)
```

- 프로바이더는 인터페이스 뒤에 두어 모델/벤더를 교체할 수 있게 한다.
- DB가 연결되지 않은 개발 환경은 InMemory 폴백으로 테스트를 수행할 수 있다. 운영에서는 DB·미디어·AI 설정 누락 시 Mock으로 조용히 기동하면 안 된다.
- 외부 AI 호출은 인증·소유권·일일 한도 검증 뒤에만 실행한다.

## 10. 필수 인수·회귀 테스트

### 공통

- 일반 계정 401이 게스트 계정으로 바뀌지 않는다.
- 타인의 관찰·사진·가든·개체·오디오는 404이며 내용이 노출되지 않는다.
- 위치가 없어도 사진/소리 관찰 흐름이 완주된다.
- 위험 후보는 종 정보보다 먼저 안전 안내가 보인다.
- 버섯 식용 가능 여부를 판단하거나 추천하지 않는다.

### 사진

- EXIF GPS가 들어 있는 JPEG/PNG도 외부 AI로 보내기 전 제거된다.
- 버스트 병합 뒤 저장되는 미디어는 1장이다.
- 사전 스캔은 어떤 영속 데이터도 바꾸지 않는다.
- 업로드→동정→확정은 정확히 1개 관찰과 해당 보상만 만든다.
- 사진 기능이 실패해도 카메라 로딩 UI가 사라졌다 나타나며 깜빡이지 않는다.

### 도감·지도·가든·보상

- 확정 뒤 도감·완성도·지도 핀·퀘스트·XP 관련 쿼리가 갱신된다.
- 좌표가 없는 관찰은 지도 핀으로 보이지 않는다.
- 같은 생물 개체를 가든 두 자리에 배치할 수 없다.
- 호환되지 않는 타일·타인 개체·존재하지 않는 타일 좌표는 거절한다.
- 퀘스트·배지 재수령은 XP를 중복 지급하지 않는다.

### 오디오

- 2초 녹음은 앱에서 업로드하지 않는다.
- 8초 조류 녹음은 후보를 표시한다.
- 무음·과소음·사람 대화 우세는 관찰·보상 없이 거절한다.
- 확정 전 소리 후보는 도감·지도·퀘스트·보상을 바꾸지 않는다.
- 같은 `confirmation_id` 재시도는 한 번의 audio 관찰·보상만 만든다.
- 유사도는 0~100점만 표시하고 제품 데이터는 바꾸지 않는다.
- 만료/삭제된 임시 오디오는 동정·유사도·확정할 수 없다.
- 녹음 중 앱 백그라운드 전환은 로컬 파일을 폐기한다.

## 11. 구현 우선순위

### P0: 현재 품질 유지

1. 사진 동정, 확정, 도감, 지도, 퀘스트, 보상, 가든 배치의 회귀를 막는다.
2. PostgreSQL 운영 연결과 PendingSighting 재시작 안전 저장을 보강한다.
3. 게스트→정식 계정 관찰 데이터 이전을 실제로 구현한다.
4. 사진의 `retake_suggested` 결과를 앱 UI에 연결한다.

### P1: 소리 MVP 완성

1. CAMP-3에 내부 BirdNET 분석 서비스를 배포한다.
2. Fastify의 `/audio/*` 6개 API, 임시 저장, 품질 검사, TTL 삭제 작업을 구현한다.
3. 라이선스가 확인된 국내 조류 참조 음원·출처·embedding을 준비한다.
4. `Observation.modality = audio` 마이그레이션과 사진 회귀 테스트를 추가한다.
5. 실제 기기에서 마이크 권한, 인터럽트, 네트워크 재시도, 24시간 삭제를 검증한다.

### P2: 제품 확장

- 실제 탐험 영역 시각화
- 가든 장식 서버 동기화
- 푸시 알림
- 소셜·정원 공유
- 계절 이벤트
- 3D 렌더링

## 12. 다른 AI에게 주는 작업 지시

1. 변경 전 현재 브랜치와 관련 테스트를 읽고, 범위를 사진/가든/오디오 중 하나로 좁혀 작업한다.
2. `docs/audio/` 계약을 변경하려면 먼저 호환성·마이그레이션·앱/서버 영향도를 문서화한다.
3. 사진 API를 소리 기능에 재사용하거나, 오디오 품질 실패를 사진의 `unknown` 결과로 바꾸지 않는다.
4. 사용자 확정 이전에는 어떤 영속 데이터도 쓰지 않는다.
5. 권한·개인정보·위험 안내 코드를 단순 UI 변경으로 우회하지 않는다.
6. 기능 변경 뒤 최소한 `app`의 TypeScript 검사와 `seed-service`의 typecheck/관련 테스트를 실행한다.
7. 사용자 작업 트리, `.env`, 원격 서버의 기존 서비스는 승인 없이 덮어쓰거나 삭제하지 않는다.

## 13. 관련 문서

```text
현재_구현_기능명세서_AI_핸드오프.md     # 이전 구현 현황 상세
리빙도감_프론트엔드_기능명세서.md         # 초기 프론트 제품 기획
리빙도감_백엔드_기능명세서.md             # 초기 백엔드 제품 기획
홈가든_핸드오프.md                         # 2D 가든 구현 상세
AI_소리_기반_조류_동정_기능명세서.md       # 소리 MVP 단일 상세 명세
docs/audio/                                # 소리 API·데이터·상태의 정식 계약
```

이 문서는 전체 제품의 공통 기준이다. 세부 기능의 API 스키마, 모델 버전, 저장 정책은 각 하위 계약 문서에서 더 구체적으로 관리한다.
