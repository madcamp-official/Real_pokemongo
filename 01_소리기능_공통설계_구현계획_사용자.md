# 01. 소리 기능 공통설계 구현계획 — 사용자 담당

> 담당자: 사용자
>
> 역할: 공통 설계 책임자 및 최종 통합 책임자
>
> 기준 브랜치: `living-dex_ver2.6`
>
> 선행 문서: `소리_기반_동정_유사도_채점_기능명세서.md`
>
> 후속 계획: `02_소리기능_앱_구현계획_사용자.md`, `03_소리기능_서버_GPU_구현계획_팀원.md`

---

## 1. 목적

앱과 서버·GPU를 동시에 구현하기 전에 두 작업자가 공유할 제품 결정, API, 상태, 데이터, 테스트 예시를 로컬 저장소에서 확정한다.

공통 설계가 완료되면:

- 사용자는 같은 기준으로 앱을 구현한다.
- 팀원은 같은 기준으로 서버와 CAMP-3 모델 서비스를 구현한다.
- 실제 GPU 모델이 준비되지 않아도 앱은 고정 JSON으로 개발할 수 있다.
- 서버가 준비된 뒤 앱 코드를 다시 설계하지 않고 연결할 수 있다.

공통 설계 단계에서는 CAMP-3에 모델을 설치하지 않는다.

---

## 2. 확정된 제품 결정

아래 8개 항목은 2026-07-28 사용자 승인으로 확정한다.

| 번호 | 항목 | 확정값 |
|---:|---|---|
| 1 | MVP 지원 생물 | 조류만 |
| 2 | 녹음 길이 | 최소 3초, 권장 6~10초, 최대 15초 |
| 3 | 분석 위치 | CAMP-3 GPU 서버 |
| 4 | 모델 | BirdNET 우선, Perch는 후속 비교 |
| 5 | 미확정 녹음 보관 | 최대 24시간 후 자동 삭제 |
| 6 | 도감 등록 시점 | 사용자가 종을 확정했을 때만 |
| 7 | 유사도 점수 효과 | 도감·지도·퀘스트·보상에 영향 없음 |
| 8 | 연습·흉내 모드 | MVP에서 제외 |

이 값을 바꾸려면 앱과 서버 구현을 멈추고 공통 계약 버전을 먼저 올린다.

---

## 3. 작업 시작 전 보존

현재 작업트리에 지도, 카메라, 동정, 정원 관련 변경사항이 남아 있으므로 먼저 기준점을 만든다.

- [ ] 사진 촬영, 사진 동정, 지도, 도감, 정원의 현재 동작을 확인한다.
- [ ] 현재 변경사항을 기능 단위로 커밋한다.
- [ ] 문서 파일도 보존 대상에 포함한다.
- [ ] 원격 저장소에 push한다.
- [ ] 기준 커밋 SHA를 `BASE_COMMIT`으로 기록한다.
- [ ] `feature/audio-common-design` 브랜치를 만든다.

금지:

- 기존 변경을 버리는 reset
- 기존 파일을 대량 복원하는 checkout
- 소리 기능과 관계없는 수정

---

## 4. 공통 설계 산출물

다음 구조를 만든다.

```text
docs/audio/
  DECISIONS.md
  API_CONTRACT.md
  STATE_MACHINE.md
  DATA_CONTRACT.md
  ACCEPTANCE.md
  CHANGE_REQUESTS.md
  fixtures/
    upload-success.json
    upload-quality-rejected.json
    identify-high-confidence.json
    identify-multiple-candidates.json
    identify-unknown.json
    similarity-success.json
    similarity-not-supported.json
    confirm-success.json
    error-network.json
```

소유권:

- 사용자가 `docs/audio/`의 유일한 수정 책임자다.
- 팀원은 공유 계약 파일을 직접 수정하지 않는다.
- 변경이 필요하면 `CHANGE_REQUESTS.md`에 이유와 제안값을 남긴다.
- 사용자가 승인한 변경만 계약에 반영한다.

---

## 5. 외부 API 계약

### 5.1 고정 경로

| API | 역할 | 영구 상태 변경 |
|---|---|---|
| `POST /audio/sightings/upload` | 녹음 업로드 및 품질 검사 | 없음 |
| `POST /audio/identify` | 조류 후보 분석 | 없음 |
| `POST /audio/identify/confirm` | 사용자가 선택한 종 확정 | 관찰·도감·보상 |
| `POST /audio/similarity/score` | 선택 종과 유사도 채점 | 없음 |
| `GET /species/:speciesId/sounds` | 참조 음원 조회 | 없음 |
| `DELETE /audio/sightings/:id` | 미확정 녹음 폐기 | 임시 데이터만 삭제 |

### 5.2 업로드 계약

요청:

```text
audio
client_recording_id
duration_ms
recorded_at
mode = "ambient"
lat, lng  // 선택
```

응답 핵심 필드:

```text
audio_sighting_id
status
quality.usable
quality.duration_ms
quality.feedback_codes
quality.valid_segments
expires_at
```

### 5.3 동정 계약

요청:

```json
{
  "audio_sighting_id": "as_123"
}
```

응답 핵심 필드:

```text
candidates[].species_id
candidates[].common_name_ko
candidates[].scientific_name
candidates[].confidence
candidates[].confidence_level
candidates[].start_ms
candidates[].end_ms
candidates[].is_dangerous
unknown
model_version
```

규칙:

- 후보는 최대 3개다.
- 후보는 현재 서버 종 데이터에 실제 존재해야 한다.
- 모델 장애를 `unknown`으로 위장하지 않는다.
- 이 API는 관찰·도감·퀘스트·보상을 변경하지 않는다.

### 5.4 확정 계약

요청:

```text
audio_sighting_id
species_id
confirmation_id
```

규칙:

- 해당 동정 결과 후보에 포함된 종만 확정할 수 있다.
- 같은 `confirmation_id`가 반복되면 동일 결과를 반환한다.
- 관찰과 보상은 정확히 한 번 생성한다.
- 이 API만 영구 상태를 변경할 수 있다.

### 5.5 유사도 계약

응답 핵심 필드:

```text
score
grade
score_reliability
matched_segment
feedback_codes
model_version
reference_set_version
```

규칙:

- 점수는 종일 확률이 아니다.
- 원시 코사인 유사도에 100을 곱해서 만들지 않는다.
- 품질, 목표 종 참조 집합, 혼동종 차이, 교정기를 반영한다.
- 유사도 요청은 관찰과 보상을 만들지 않는다.

---

## 6. 상태 계약

### 6.1 앱 상태

```text
idle
requesting_permission
ready
recording
local_checking
uploading
identifying
result
confirming
confirmed
failed
discarded
```

정상 흐름:

```text
idle
→ requesting_permission
→ ready
→ recording
→ local_checking
→ uploading
→ identifying
→ result
→ confirming
→ confirmed
```

규칙:

- 녹음과 업로드를 중복 실행하지 않는다.
- 재시도 시 같은 `client_recording_id`를 유지한다.
- 로딩 단계가 변해도 로딩 컴포넌트를 제거했다 다시 만들지 않는다.
- 앱 재시작 시 `recording` 상태를 복원하지 않는다.
- 로그아웃하면 미확정 로컬 오디오와 대기열을 삭제한다.

### 6.2 서버 상태

```text
uploaded
quality_checked
identified
rejected
confirmed
deleted
expired
```

규칙:

- `confirmed`는 한 번만 도달할 수 있다.
- `rejected`, `deleted`, `expired`는 확정할 수 없다.
- 미확정 세션은 생성 후 최대 24시간 안에 삭제한다.
- 남의 세션과 존재하지 않는 세션은 같은 404로 처리한다.

---

## 7. 데이터 계약

기존 관찰:

```text
Observation.modality = "photo" | "audio"
기존 레코드 기본값 = "photo"
```

미디어:

```text
media_kind = "image" | "audio"
mime_type
duration_ms
sha256
retention_class
```

신규 서버 데이터:

```text
audio_sighting
audio_identification_result
species_sound_reference
```

MVP 제외:

- 유사도 점수 영구 저장
- 연습 모드 데이터
- 기존 퀘스트의 오디오 자동 인정

---

## 8. 고정 테스트 종과 JSON

공통 fixture 위치는 `docs/audio/fixtures/`로 고정한다.

예시 조류:

| 용도 | 이름 | 종 ID |
|---|---|---|
| 높은 신뢰도 | 직박구리 | `taxon-hypsipetes-amaurotis` |
| 복수 후보 | 참새 | `taxon-passer-montanus` |
| 다른 음색 비교 | 까치 | `taxon-pica-serica` |

fixture 규칙:

- 성공 응답에는 `model_version`을 포함한다.
- 유사도 응답에는 `reference_set_version`을 포함한다.
- 품질 실패는 `TOO_SHORT`, `MOSTLY_SILENCE`, `TOO_NOISY`, `CLIPPED`, `SPEECH_DETECTED` 등의 코드를 사용한다.
- 앱과 서버 테스트는 같은 fixture를 기준으로 한다.
- 두 구현 담당자는 fixture를 임의로 수정하지 않는다.

---

## 9. 작업 분기와 전달

공통 설계 완료 후:

1. `docs/audio`를 커밋한다.
2. 커밋 SHA를 `COMMON_DESIGN_COMMIT`으로 기록한다.
3. 원격에 push한다.
4. 사용자는 여기서 `feature/audio-app`을 만든다.
5. 팀원은 같은 커밋에서 `feature/audio-server-gpu`를 만든다.

전달할 정보:

```text
BASE_COMMIT
COMMON_DESIGN_COMMIT
외부 API 계약 버전
fixture 버전
CAMP-3 배포 대상 이름
```

GPU 주소, 계정, 비밀번호, 토큰은 저장소 문서에 기록하지 않고 별도 보안 채널로 전달한다.

---

## 10. 공통설계 완료 조건

- [ ] 승인된 제품 결정 8개가 `DECISIONS.md`에 기록됨
- [ ] 외부 API 요청·응답과 오류 코드가 확정됨
- [ ] 앱과 서버 상태 흐름이 확정됨
- [ ] 데이터 추가 항목과 기존 데이터 기본값이 확정됨
- [ ] fixture 9개가 유효한 JSON임
- [ ] fixture의 종 ID가 실제 seed 데이터에 존재함
- [ ] `confirm` 이외 API의 부수효과 금지가 명시됨
- [ ] 앱과 서버 담당 파일 경계가 명시됨
- [ ] 통합 테스트 기준이 `ACCEPTANCE.md`에 기록됨
- [ ] `COMMON_DESIGN_COMMIT`이 원격에 push됨

이 조건을 모두 만족한 뒤에만 앱과 서버·GPU 구현을 시작한다.
