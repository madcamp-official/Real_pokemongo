# 실전 개발 시 반드시 명심할 것 — 『씨앗(SEED)』 프로덕션 가이드

> **이 문서의 대상:** 서버·도메인이 확보되고 유료 API를 실제로 구매·연동하며 본격 개발을 시작할 때의 **나 자신과, 그때 함께 작업할 Claude Code**.
> **이 문서의 성격:** 일반적인 웹 개발 상식이 아니라, **이 서비스에 고유한 사고(事故) 지점**을 사전에 못 박아두는 안전 체크리스트. 여기 적힌 것은 "지키면 좋은 것"이 아니라 "어기면 사람이 다치거나, 법을 어기거나, 파산할 수 있는 것"이다.
> **작성일:** 2026-07-12 / **선행 문서:** [기능명세서](어린이_생태교육_서비스_기능명세서.md), [코어 코드](seed-service/)

---

## 0. 이 서비스가 유난히 위험한 이유 (먼저 뼈에 새길 것)

대부분의 CRUD 서비스와 달리, 이 제품은 **네 가지 고위험 요소가 한 몸**에 있다. 개발 중 어떤 결정을 할 때든 이 네 렌즈를 통과시켜야 한다.

| 위험 축 | 최악의 시나리오 | 대가 |
|---|---|---|
| **① 아동 개인정보** | 만 14세 미만 아동의 위치·사진·식별정보 유출·오남용 | 개인정보보호법 위반(과징금·형사), 서비스 폐업, 언론 사고 |
| **② 안전 크리티컬 동정** | 독버섯·옻나무·벌을 "안전한 것"으로 오동정 | 아동 신체 상해, 배상책임, 신뢰 붕괴 |
| **③ 유료 종량 API** | 재시도 폭탄·악의적 대량 업로드로 동정 API 호출 폭증 | 하루 만에 수백만 원 청구(비용 폭탄) |
| **④ B2B2C 신뢰** | 학부모·기관이 "안전하지 않다"고 판단 | 구매자 이탈 = 수익 0 |

> **핵심 비대칭:** 이 서비스에서 오류는 두 방향이 대칭이 아니다.
> - 안전한 생물을 "위험할 수 있다"고 경고 → **허용**(과보호, 불편할 뿐).
> - 위험한 생물을 "안전하다"고 표시 → **절대 불가**(아이가 다친다).
> 모든 안전 판단은 **보수적으로(거짓 양성 허용, 거짓 음성 금지)** 설계한다.

---

## 1. 보안 (Security)

### 1.1 [치명] 유료 동정 API 키는 절대 클라이언트에 두지 않는다
- 앱(모바일/웹)에 API 키를 심으면 **디컴파일·프록시 스니핑으로 즉시 유출**되고, 유출된 키로 남이 당신 계정에 종량 과금을 태운다.
- **반드시 서버가 프록시**한다: 앱 → 우리 서버 → (서버가 보관한 키로) 외부 동정 API. 앱은 우리 서버 토큰만 안다.
- 코드 기준점: [`config/index.ts`](seed-service/src/config/index.ts)가 키를 `process.env`에서만 읽는 구조를 유지. `PlantIdProvider`는 **서버에서만** 실행.

### 1.2 [치명] 시크릿 관리
- API 키·DB 비밀번호·JWT 서명키·스토리지 자격증명을 **git에 절대 커밋하지 않는다**. `.env`는 `.gitignore`에 (그리고 `.env.example`만 커밋).
- 커밋 히스토리에 한 번이라도 들어가면 삭제해도 남는다 → 즉시 키 **폐기·재발급(rotate)**.
- 프로덕션은 시크릿 매니저(AWS Secrets Manager / GCP Secret Manager / Vault)로. 환경변수 평문 파일은 최소화.
- **Claude Code 협업 규칙:** 실제 키 값을 프롬프트·코드·주석·로그에 붙여넣지 않는다. 항상 `process.env` 참조로만 다룬다.

### 1.3 [치명] 접근통제 — IDOR(다른 집 아이 데이터 접근) 방지
- 이 서비스에서 가장 흔하고 치명적인 취약점: `GET /children/{childId}/observations`에서 **childId만 바꾸면 남의 아이 데이터가 나오는** 경우.
- **모든 자녀 데이터 접근은 "이 childId가 요청한 보호자 계정 소유인가"를 서버에서 검증**한다. 클라이언트가 보낸 childId를 신뢰하지 않는다.
- 규칙: 리소스 접근 시 `resource.guardianId === authenticatedGuardian.id` 확인을 **모든 엔드포인트에서** 강제. 미들웨어/가드로 중앙화.
- 보호자 모드 진입 게이트(PIN/생체)는 UI 편의일 뿐, **진짜 인가는 항상 서버에서**.
- **코드 기준점(구현 완료):** `core/auth/Authorization.ts`의 `Authorizer.assertOwnsChild`/`assertSelf`. 자녀 데이터 진입점(`ObservationFlow.observe`, `AccountService`, `GuardianDashboard`)이 `AuthContext`를 **타입 수준에서 요구**해 "체크를 빠뜨리는 것"이 컴파일되지 않게 했다. 인가는 한도 체크·동정 호출보다 먼저 실행(비용 공격 차단). 미존재/소유 불일치는 동일 에러(존재 여부 누설 방지) → API 계층은 404로 매핑. 골든 테스트: `core/auth/Authorization.test.ts`. **HTTP 계층을 붙일 때 `AuthContext`는 반드시 토큰 검증 결과로만 생성하고, 클라이언트가 보낸 guardianId를 담지 말 것.**

### 1.4 [치명] 미디어(아동 사진) 보안 — EXIF GPS 제거
- **놓치기 쉬운 최악의 함정:** 우리는 `Observation` 스키마에서 좌표를 뺐지만, **스마트폰 사진 파일(EXIF)에는 촬영 위치 GPS가 박혀 있다.** 이 사진을 그대로 저장/전송하면 "좌표 저장 안 함" 원칙이 무력화된다.
- **업로드 파이프라인에서 반드시 EXIF(특히 GPS)를 제거(strip)**한 뒤에만 저장·외부 전송한다. 이건 선택이 아니라 프라이버시 원칙의 실제 구현이다.
- **코드 기준점(구현 완료):** `core/media/MediaSanitizer.ts`의 `sanitizeImage()`가 JPEG(APP1/EXIF·COM)·PNG(eXIf·tEXt 등) 메타데이터를 바이너리 수준에서 제거한다. 게이트웨이는 브랜디드 `SanitizedImage`만 받으므로 **정화 안 된 원시 사진이 외부로 나가면 컴파일 에러**. 미지 포맷은 거부(보수적). 골든 테스트: `core/media/MediaSanitizer.test.ts` + `ObservationFlow.test.ts`(종단). **주의:** 이 정화기는 현재 도메인 로직 경로(동정 전송)만 보장한다. 실제 업로드 파이프라인(presigned URL로 스토리지에 저장하는 경로, 썸네일/리사이즈 서버 등) **모든 미디어 경로가 저장 전에 이 정화기를 반드시 거치도록** 배선할 것. 리사이즈 라이브러리가 EXIF를 되살리지 않는지도 확인.
- 그 밖의 미디어 보안:
  - 업로드는 **presigned URL** 방식(서버 경유 스토리지 직행), 파일 크기·타입(MIME 실검사, 확장자 신뢰 금지) 제한.
  - 저장된 사진은 **비공개 버킷 + 짧은 만료 서명 URL**로만 접근. 절대 공개 URL·순번 예측 가능한 경로 금지.
  - 이미지 폭탄(디컴프레션 봄), 악성 파일 방어.

### 1.5 [치명] 외부 동정 API로의 아동 사진 전송 = 제3자 제공/국외이전
- 아이 사진을 Plant.id 등 **해외 벤더에 보내는 순간, 개인정보 제3자 제공·국외 이전**에 해당할 수 있다.
- **필수 확인:**
  - 개인정보처리방침·동의서에 "동정을 위해 사진이 외부 처리자에게 전송됨"을 명시하고 동의받았는가.
  - 벤더가 **전송된 이미지를 자사 모델 학습에 쓰는가** → 계약/설정에서 **학습 이용 opt-out** 확보. (아동 이미지가 벤더 학습셋에 들어가면 사고.)
  - 가능하면 사진에서 배경·얼굴이 최소화되도록 유도(대상 생물 중심 촬영 가이드가 프라이버시에도 기여).
- 장기적으로 **온디바이스/자체 모델**로 옮기면 이 리스크 자체가 사라진다(명세서 로드맵과 정합).

### 1.6 [높음] 비용 폭탄 방지 = 보안 문제로 취급
- 종량 API 앞단에 **rate limiting + 일일/월간 상한 + 서킷 브레이커**를 반드시 둔다. (§3.3, §5.4와 연결)
- 인증 없는(또는 무료) 사용자의 동정 호출은 특히 엄격히 제한. 봇의 대량 업로드가 곧 비용이다.

### 1.7 [높음] 로그·관측에 민감정보를 남기지 않는다
- 로그·에러 리포트(Sentry 등)·분석 이벤트에 **좌표, 아동 실명/닉네임, 사진 URL, 원본 이미지, 토큰**을 남기지 않는다.
- 특히 예외 스택트레이스에 요청 본문(사진·위치 포함)이 통째로 찍히는 사고가 흔하다 → **로깅 직렬화에서 민감 필드 마스킹** 미들웨어 필수.

### 1.8 [높음] 입력 검증 & 표준 방어
- 모든 외부 입력(경로/쿼리/본문) 서버측 스키마 검증(zod 등). 클라이언트 검증은 UX용일 뿐.
- SQL 인젝션(파라미터 바인딩·ORM), NoSQL 인젝션, 경로 순회 방어.
- 인증: 토큰 만료·회전, 안전한 저장(refresh token은 httpOnly 등). **비밀번호·결제정보는 우리가 직접 다루지 않는다**(명세서 안전규칙: 인증·결제는 표준 제공자에 위임).
- CORS 화이트리스트, 보안 헤더(CSP·HSTS 등), 관리자 콘솔 별도 인증·IP 제한.
- 의존성 취약점 스캔(`npm audit`, Dependabot)과 정기 업데이트.

### 1.9 [중간] 관리자/내부 도구
- 콘텐츠 감수·오동정 신고 처리용 어드민은 **강력한 인증(MFA) + 감사 로그 + 최소권한**. 아동 데이터 접근은 목적·기록이 남게.

---

## 2. 규제·법률·정책 (개발 전에 반드시 확인)

> 이건 코드 이전의 문제다. 여기서 실수하면 기술이 완벽해도 서비스가 불법이 된다.

### 2.1 [치명] 만 14세 미만 아동 개인정보 — 법정대리인 동의
- **개인정보보호법상 만 14세 미만 아동의 개인정보 처리에는 법정대리인의 동의**가 필요. 동의 없이 수집 불가.
- 코드 기준점: [`AccountService.createChild`](seed-service/src/child/account/AccountService.ts)가 `legalGuardianConsent` 없으면 예외. **이 가드를 절대 우회·완화하지 말 것.**
- **동의 방법의 실효성**: 단순 체크박스로 충분한지, 법정대리인 진위 확인(성인 인증 연계)이 필요한지 **법률 검토**. 동의 항목을 목적별로 분리(수집·이용 / 외부 전송 / 마케팅).
- 데이터 최소 수집: 실명·정밀 생년월일·연락처를 아동 프로필에 받지 않는 원칙 유지.

### 2.2 [치명] 위치정보 — 이중 규제
- 아동 위치는 특히 민감. **위치정보의 보호 및 이용 등에 관한 법률**상 개인위치정보 처리 요건(동의·보관·파기)을 별도로 검토.
- 우리 설계는 "기본 미저장 + 시·군·구 일반화"로 리스크를 낮췄다. **이 정책을 코드에서 약화시키는 변경(예: 정밀좌표 저장 기능 추가)은 법률 검토 없이는 금지.**
- 코드 기준점: [`regionGeneralizer.ts`](seed-service/src/core/observation/regionGeneralizer.ts), `Observation` 타입에 좌표 필드 부재. 이 불변식을 지킨다.

### 2.3 [높음] 해외 배포 시 추가 규제
- 미국: **COPPA**(13세 미만), 애플 Kids/구글 Families 정책. 유럽: **GDPR** 및 아동 조항.
- 국내만 할지, 해외도 할지 초기에 정하고 그에 맞춰 동의·데이터 설계.

### 2.4 [높음] 앱스토어 아동 카테고리 특수 심사
- **Apple Kids Category / Google Play "Families(Designed for Families)"**는 일반 앱보다 심사가 엄격.
  - 아동 대상 앱은 **행동기반 광고 금지**, 외부 링크·결제 유도 제한, 개인정보 처리 제한.
  - **결제·구매 유도는 반드시 보호자 게이트 뒤**(아이 화면에서 결제 유도 시 반려/삭제).
  - 제3자 SDK(분석·광고)도 아동 정책 준수 여부 확인.
- 심사 반려는 출시 일정을 통째로 밀 수 있으니 **정책을 개발 초기부터** 반영.

### 2.5 [높음] 콘텐츠 책임 & 면책
- 오동정으로 인한 사고 대비: **이용약관에 동정 결과의 한계·안전 주의(직접 만지거나 먹지 말 것)를 명시**하고, 앱 내에도 상시 노출.
- 단, 면책 문구가 §1의 안전 설계 책임을 면제해주지 않는다. 문구는 최후의 방어선일 뿐.

### 2.6 [중간] 필수 문서
- 개인정보처리방침, 이용약관, (해당 시) 위치정보 이용약관, 청소년 보호 정책. **출시 전 완비**.
- 데이터 파기 정책(보관 기간, 회원 탈퇴 시 파기)을 문서화하고 코드로 이행(§5.6).

---

## 3. 반드시 해야 하는 예외처리 (Exception Handling)

> 원칙: **외부 세계는 항상 실패한다**(네트워크, 벤더 API, 결제, 스토리지). 이 서비스는 "밖에서, 약전파에서, 아이가" 쓰므로 실패가 기본값이라고 가정한다.

### 3.1 [치명] 외부 동정 API 실패를 "안전하게" 처리
- 타임아웃·5xx·rate limit(429)·**quota 소진**·잘못된 응답 스키마 — 모두 구분해 처리.
- **절대 금지:** API가 애매한 응답을 줬을 때 임의로 "그럴듯한 종"으로 단정. 실패 시 **좌절 없는 폴백**("다음에 또 찾아보자")로 마무리.
- 코드 기준점: [`IdentificationGateway.identify`](seed-service/src/core/identification/IdentificationGateway.ts)의 try/catch → `unknownOutcome`. 이 경로를 유지·강화.
- **타임아웃을 반드시 설정**(무한 대기 금지). 재시도는 **지수 백오프 + 상한**(무한 재시도는 비용 폭탄, §3.3).

### 3.2 [치명] 안전 필터의 "미지의 종" 처리
- `SafetyFilter`는 `Taxon.riskTags`가 있어야 경고한다. **DB에 없는/처음 보는 위험 생물은 경고되지 않는 사각지대**가 존재.
- 대응: **동정 신뢰도가 낮거나 종을 특정 못 한 경우, 기본적으로 "함부로 만지지 말고 관찰만"이라는 보수적 안내**를 붙인다(거짓 음성 방지). 특히 곤충·버섯·미확인 식물.
- 위험 생물 DB 커버리지를 지속 확장하고, 신규 종 추가 시 `riskTags` 검토를 **필수 리뷰 항목**으로.

### 3.3 [치명] 비용/호출 한도 초과 처리
- 무료 사용자 일일 한도([`ObservationService.isWithinDailyLimit`](seed-service/src/core/observation/ObservationService.ts))뿐 아니라, **시스템 전체 일일 예산 상한**을 두고 초과 시 우아하게 저하(graceful degradation): 저해상도/캐시 우선, 신규 호출 지연 안내.
- 벤더 quota 소진 시: 사용자에게 "잠시 뒤 다시" 안내, 관리자에게 즉시 알림(§5.5).

### 3.4 [높음] 부분 실패 & 멱등성(트랜잭션 경계)
- 골든 패스([`ObservationFlow.observe`](seed-service/src/child/ObservationFlow.ts))는 관찰저장→도감→퀘스트→보상의 다단계다. **중간에 실패하면?**
  - 이상적: 하나의 트랜잭션(같은 DB) 또는 보상 트랜잭션(saga)으로 일관성 확보.
  - 현실적 최소선: **관찰 저장을 먼저 확정**하고, 도감/퀘스트/보상은 실패해도 **재계산 가능**하게(멱등). 재시도 시 배지·XP가 **중복 지급되지 않도록** 멱등키/유니크 제약.
- **네트워크 중복 제출**: 아이가 버튼을 여러 번 누르거나 오프라인 큐가 재전송할 때 **같은 관찰이 2번 저장되지 않도록** 클라이언트 idempotency key + 서버 중복 방지.

### 3.5 [높음] 오프라인 & 업로드 재개
- 야외 약전파: 촬영은 되지만 업로드/동정은 실패 가능. 로컬 큐에 저장 후 재연결 시 재시도(명세서 F13). **큐 항목 손실·무한 재시도·중복 방지**.

### 3.6 [높음] 결제 예외
- 결제 실패·타임아웃·**중복 청구**·환불·구독 상태 불일치(스토어 영수증 검증). 결제는 스토어(IAP)에 위임하되 **영수증 서버 검증**과 웹훅 멱등 처리 필수.
- 구독 만료/갱신 경계에서 기능 접근 권한이 정확히 전환되는지.

### 3.7 [중간] 입력·상태의 엣지 케이스
- 위치 없음(권한 거부/실내), 사진 0장·과다, 손상 이미지, 매우 긴 닉네임/이모지/유니코드, 시간대·자정 경계(일일 한도 계산), 자녀 프로필 4개 초과([`ChildLimitError`]).
- 모든 서비스 메서드는 "존재하지 않는 ID"를 안전하게 처리(현재 `throw` → API 계층에서 404/403으로 매핑).

---

## 4. 디버깅 시 반드시 테스트해야 하는 것 (Testing)

> 이 서비스에는 **회귀하면 사람이 다치거나 법을 어기는 경로**가 있다. 그 경로들은 반드시 자동화 테스트로 잠가둔다. "수동으로 확인했다"로는 부족하다.

### 4.1 [치명] 안전 필터 회귀 테스트 (골든 테스트)
- 위험 생물 각각(벌=쏘임, 옻나무=접촉염, 독버섯=독성, 보호종)에 대해:
  - `SafetyFilter.evaluate`가 **반드시 경고를 반환**하고 `showFirst=true`.
  - 게이트웨이 결과에서 **안전 안내가 종 정보보다 먼저** 노출(`childMessage`가 안전 문구).
  - 버섯은 **식용 판정을 절대 하지 않음**(`EDIBILITY_JUDGEMENT_SUPPORTED === false`).
- 중확신(후보 여러 개)에 위험 후보가 섞이면 **보수적 경고**가 나오는지.
- **이 테스트가 깨지면 배포 차단(CI 게이트).** 안전 필터 관련 코드 수정 시 반드시 이 스위트 통과 확인.

### 4.2 [치명] 프라이버시 불변식 테스트
- 정밀 좌표를 넣어도:
  - 위치 저장 OFF(기본) → `Observation.region === null`.
  - 저장 ON → **시·군·구 코드만**, 원본 lat/lng는 어디에도 없음.
- **API 응답·DB 레코드·로그 어디에도 정밀좌표가 나타나지 않는지** 검사하는 테스트.
- 업로드된 이미지에 **EXIF GPS가 제거**됐는지 검사(§1.4).
- 로그 직렬화가 민감 필드를 마스킹하는지.

### 4.3 [치명] 접근통제(IDOR) 테스트
- 보호자 A가 보호자 B의 childId로 관찰/도감/대시보드를 **조회·수정 시도 → 반드시 403/404**.
- 이 테스트를 모든 자녀 데이터 엔드포인트에 대해.

### 4.4 [높음] 동정 확신도 분기 테스트
- high → 즉시 해금 / medium → 후보 고르기(도감 미반영) / 저확신 → 상위 분류 폴백 / unknown → 재시도 안내(도감·퀘스트 미반영).
- 코드 기준점: [`confidencePolicy.ts`](seed-service/src/core/identification/confidencePolicy.ts) 임계값 경계값 테스트. 임계값 튜닝 후 회귀 확인.
- **미확정 관찰이 도감·퀘스트·보상에 절대 반영되지 않는지**(오동정이 진행으로 굳는 것 방지).

### 4.5 [높음] 보상 멱등성·정확성 테스트
- 같은 관찰 중복 제출 → 배지·XP **중복 지급 안 됨**.
- 퀘스트 완료 조건(서로 다른 N종)·레벨 경계·배지 규칙 각각.

### 4.6 [높음] 비용 상한·rate limit 테스트
- 일일 한도 도달 시 `blocked: daily_limit` 반환하고 **외부 API를 호출하지 않는지**(호출 후 차단이면 비용 이미 발생).
- 재시도 로직이 상한 안에서 멈추는지(무한 재시도 없음).

### 4.7 [높음] 실제 조건 테스트 (합성 데이터로는 못 잡는 것)
- **실기기 야외 촬영**: 역광·흔들림·원거리·저조도에서 동정 품질과 폴백 동작.
- **약전파/오프라인** 전환 중 업로드 큐.
- **글 못 읽는 연령**: 음성 나레이션·아이콘만으로 핵심 루프 완주 가능한지(접근성).
- **한국 종 커버리지 실측**: 벤더 API가 국내 흔한 종을 실제로 맞히는지(명세서 §15). 벤치마크 세트를 만들어 정기 측정.

### 4.8 [중간] 결제 샌드박스 테스트
- 스토어 샌드박스로 구매·갱신·취소·환불·복원, 영수증 검증, 구독 상태 전환.

### 4.9 [중간] 부하·동시성
- 다자녀 동시 관찰, 대량 업로드, 대시보드 집계 성능(현재 대시보드는 관찰마다 taxon 조회 → **N+1 주의**, 실제 DB에선 배치 조회로).

---

## 5. 배포·운영 (Deployment & Operations)

### 5.1 [치명] 환경 분리 & 시크릿
- dev / staging / prod **완전 분리**(DB·버킷·API 키 각각). 프로덕션 키로 개발하지 않는다.
- 코드 기준점: [`assertProductionConfig`](seed-service/src/config/index.ts)가 프로덕션 부팅 시 필수 공란(DB·스토리지·인증키·동정키)을 검사해 **Mock으로 조용히 도는 사고를 차단**. 이 가드를 유지.
- **프로덕션에서 MockProvider가 절대 활성화되지 않도록**(진짜 사고: 아이에게 가짜 동정 결과).

### 5.2 [높음] DB 마이그레이션 & 스키마 규율
- 마이그레이션 도구로 버전 관리, **되돌리기(rollback) 스크립트** 포함, 배포 전 staging에서 검증.
- **프라이버시 불변식을 스키마로 강제**: 좌표 컬럼을 만들지 않는다. 민감 컬럼은 암호화. 스키마 변경 PR은 프라이버시 영향 검토 필수.

### 5.3 [높음] 점진 배포 & 롤백
- 카나리/단계적 롤아웃, 빠른 롤백 경로. 앱은 스토어 심사·강제 업데이트 지연이 있으니 **서버 API 하위호환** 유지(구버전 앱이 계속 동작).
- 피처 플래그로 위험한 기능을 즉시 끌 수 있게.

### 5.4 [치명] 비용 모니터링 & 알림
- 동정 API **일/시간 단위 호출량·비용 대시보드**와 **임계 초과 즉시 알림**(예: 시간당 X회 초과, 일 예산 Y% 도달).
- **자동 서킷 브레이커**: 이상 급증 시 자동으로 신규 호출 차단·저하 모드 전환. 새벽에 비용 폭탄이 터지면 아무도 못 막는다 → 자동화 필수.

### 5.5 [높음] 관측성 (에러·성능·품질 드리프트)
- 에러율·지연·크래시(APM/Sentry) — 단 **민감정보 마스킹**(§1.7).
- **동정 품질 모니터링**: 저확신 폴백 비율, 오동정 신고율 추이. 벤더 모델 업데이트로 품질이 조용히 나빠지는 **드리프트**를 잡는다.
- 안전 안내 노출·이탈 지표(사고 0 유지 확인, KPI와 연결).

### 5.6 [치명] 백업·복구 & 데이터 주체 권리 이행
- 정기 백업 + **복구 훈련**(백업이 실제로 복구되는지 테스트). 백업에도 암호화·접근통제.
- **삭제권·이동권 실제 이행**: 회원 탈퇴/보호자 요청 시 아동 데이터를 **백업·로그·외부 벤더 포함** 실제로 파기·내보내기. "삭제 버튼은 있는데 백업엔 남아있다"는 사고 방지.
- 보관 기간 정책을 배치 작업으로 강제(오래된 원본 사진 자동 파기 등).

### 5.7 [중간] 인프라 위생
- HTTPS 강제·인증서 자동 갱신, WAF/DDoS 방어, 최소권한 IAM, 스토리지 버킷 **공개 접근 차단** 확인(사고 1위 원인).
- 헬스체크·자동 재시작, 저운영 지향(명세서: 복무 중 운영 고려) → 관리형 서비스·자동화 우선.

### 5.8 [중간] 출시 전 최종 게이트(체크리스트)
- [ ] 개인정보처리방침·약관·아동 정책 문서 완비 및 앱 내 노출
- [ ] 앱스토어 아동 카테고리 정책 준수(광고·결제·외부링크)
- [ ] 안전 필터·프라이버시·IDOR 테스트 CI 통과
- [ ] 비용 상한·서킷브레이커·알림 동작 확인
- [ ] MockProvider 프로덕션 비활성 확인
- [ ] 시크릿이 코드/히스토리에 없음 확인
- [ ] 백업·삭제권 이행 경로 검증

---

## 6. Claude Code와 작업할 때의 협업 규칙 (나와 AI 모두에게)

> 이 서비스는 안전·프라이버시 크리티컬이라, AI에게 맡길 때 특히 조심할 지점이 있다.

1. **[치명] 시크릿을 대화·코드에 넣지 않는다.** 실제 API 키·DB 비밀번호를 프롬프트에 붙여넣지 말 것. 항상 `process.env`/시크릿 매니저 참조로만.
2. **[치명] 안전·프라이버시 코드는 "정리·간소화" 명목으로 약화시키지 않는다.** `SafetyFilter`, `regionGeneralizer`, `assertProductionConfig`, 동의 가드, IDOR 체크를 리팩터링할 때는 **관련 골든 테스트 통과를 반드시 확인**. AI가 "불필요해 보이는 방어 코드"를 지우자고 하면 거부한다.
3. **[높음] 안전 크리티컬 경로 수정 시 테스트부터.** 동정 폴백·안전 필터·보상 멱등성·접근통제를 건드리면 §4의 해당 테스트를 먼저 돌리고, 없으면 먼저 작성한다.
4. **[높음] 스키마·모델 변경 시 프라이버시 불변식 재확인.** 좌표 컬럼 추가, 새 개인정보 필드, 외부 전송 추가는 **법률 검토 신호**로 취급하고 임의 진행 금지.
5. **[높음] 외부 연동 코드는 실패 경로부터 짠다.** 새 API 어댑터를 구현할 때 타임아웃·429·에러 스키마·재시도 상한을 **먼저** 정의. 해피 패스만 짜고 끝내지 않는다.
6. **[중간] "그럴듯하게 채우기" 금지.** 벤더 응답 매핑, 임계값, 위험 태그처럼 **틀리면 위험한 값**은 추측으로 채우지 말고 `TODO(제공 필요)`로 남기고 사람이 확정한다. (현재 코드의 미구현 어댑터가 조용한 오답 대신 명시적 에러를 던지는 것도 같은 이유.)
7. **[중간] 큰 변경은 작게 쪼개고, 각 단계에서 검증.** 특히 골든 패스([`ObservationFlow`])처럼 여러 엔진이 얽힌 곳.

---

## 7. 우선순위 요약 (한 장으로)

**절대 어기면 안 되는 5가지 (외우기):**
1. 아동 사진은 **EXIF GPS 제거** 후에만 저장·전송한다.
2. 유료 API 키는 **서버에만**, 호출엔 **상한·서킷브레이커**.
3. 위험 생물은 **보수적으로 경고**(거짓 음성 금지), 미지의 종도 "만지지 말 것".
4. 자녀 데이터 접근은 **모든 경로에서 소유권 검증**(IDOR).
5. 안전·프라이버시 **골든 테스트를 CI 게이트**로, 통과 못 하면 배포 안 함.

**개발 순서 권장:** 인증·접근통제 → 미디어 파이프라인(EXIF strip) → 동정 프록시(상한 포함) → 안전 필터 + 테스트 → 관찰/도감/퀘스트/보상 → 대시보드 → 결제 → 관측·비용 알림 → 출시 게이트.

---

*이 문서는 살아있는 체크리스트다. 새로운 함정을 만날 때마다 여기에 추가하고, 사고가 날 뻔한 지점은 반드시 골든 테스트로 잠근다.*
