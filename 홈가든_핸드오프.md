# 홈가든 핸드오프

## 1. 현재 상태

홈가든은 **가로 화면 전용의 2D 아이소메트릭 꾸미기 화면**이다.
실시간 3D, Three.js, GLB 로딩, WebGL은 홈가든 실행 경로에서 사용하지 않는다.

- 홈가든 진입 시 가로 모드(`LANDSCAPE`)로 전환된다.
- 다른 탭으로 나가면 세로 모드(`PORTRAIT_UP`)로 복귀한다.
- 배경은 장식이 없는 넓은 초기 정원 이미지이며 뷰포트보다 큰 월드에 배치된다.
- 한 손가락 pan과 두 손가락 pinch-zoom으로 정원을 탐색한다.
- 왼쪽 구역은 Lv.3, 오른쪽 구역은 Lv.5에 열린다.
- 사용자가 발견한 종 중 2D 스프라이트가 준비된 종만 보관함과 정원에 표시된다.
- 보관함의 친구를 드래그해 정원 좌표에 배치하고, 배치 상태는 기존 정원 API로 저장한다.
- 장식은 배경과 분리된 엔티티이며 도감 수·레벨 조건으로 해금하고 드래그 배치한다.
- 동물/곤충은 간단한 좌우 이동·상하 바운스, 식물은 미세한 좌우 흔들림으로 표현한다.

## 2. 실행 방법

앱 패키지는 프로젝트 루트가 아니라 `app` 폴더에 있다.

```powershell
cd "C:\Users\gjtjw\Desktop\nature Go\app"
npx expo start -c
```

화면 회전 모듈과 의존성 변경이 포함되어 있으므로 Expo Go 앱을 완전히 종료한 뒤 다시 연결한다.
개발 빌드를 사용한다면 네이티브 모듈 반영을 위해 새 빌드가 필요하다.

## 3. 주요 파일

| 파일 | 역할 |
|---|---|
| `app/src/screens/GardenScreen.tsx` | 홈가든 화면 조립, 가로 전환, HUD, 드래그·배치, 보관함 연결 |
| `app/src/components/garden/GardenScene2D.tsx` | 큰 2D 월드, pan/pinch-zoom, 구역 잠금, 종·장식 레이어 렌더링 |
| `app/src/components/garden/CreatureTray.tsx` | 하단의 친구/장식 탭과 드래그 시작점 |
| `app/src/components/garden/DecorationArt.tsx` | 배경과 분리된 장식 SVG 렌더링 |
| `app/src/components/garden/gardenDecorations.ts` | 장식 카탈로그와 도감·레벨 해금 조건 |
| `app/src/components/garden/CreatureStatusSheet.tsx` | 배치된 친구를 누를 때 여는 상태/이름/상호작용 시트 |
| `app/src/components/species/CreatureArt.tsx` | 종별 2D SVG 스프라이트 원본 |
| `app/src/theme/garden.ts` | 논리적 6×6 좌표와 2D 배경상의 백분율 좌표 매핑 |
| `app/assets/garden/garden-starter-world-v1.png` | 비어 있는 확장형 초기 정원 배경 |
| `app/src/navigation/MainTabs.tsx` | 앱 시작 시 세로 모드 고정 |
| `app/app.json` | 양 방향 허용 및 `expo-screen-orientation` 플러그인 설정 |

## 4. 화면 구조

```text
GardenScreen
├─ GardenScene2D
│  ├─ garden-starter-world-v1.png
│  ├─ pan/pinch-zoom 월드
│  ├─ Lv.3/Lv.5 잠금 구역
│  ├─ 배치 가능한 타일 표시(드래그 중)
│  ├─ DecorationArt 장식 레이어
│  └─ CreatureArt 동식물 레이어
├─ 상단 압축 HUD: 뒤로, 레벨, 도감 수, 설정
├─ 2.4초 후 사라지는 정원 인트로
├─ 하단 CreatureTray: 친구/장식 탭
└─ CreatureStatusSheet
```

배경에는 완성된 연못·다리·벤치·아치·나무가 없다. 동물·식물과 장식은 모두 별도 레이어다.
동식물은 기존 `row`, `col`을 유지하고 장식은 월드 백분율 `x`, `y`로 로컬 저장한다.

## 5. 데이터 흐름

```text
사진 촬영·동정 확정
  → 도감(DexEntry)에서 discovered=true
  → creature 생성
  → GardenScreen이 보유 creature 목록 구성
  → CreatureArt가 있는 species_id만 보관함에 노출
  → 드래그하여 row/col에 배치
  → gardenStore가 로컬 저장 후 /garden/layout으로 동기화
```

현재 배치 API 계약은 다음과 같다.

- 조회: `GET /garden/layout`
- 저장: `PUT /garden/layout`
- 타일별 서식지 호환성: `GET /garden/tile-compatibility`

기존 서버의 6×6 `row`, `col` 값은 계속 사용한다. 2D 전환으로 서버 스키마를 바꿀 필요는 없다.

## 6. 현재 제공하는 2D 종 스프라이트

`CreatureArt.tsx`에 있는 종은 다음과 같다.

- 무당벌레
- 배추흰나비/나비
- 꿀벌/벌
- 민들레
- 개나리
- 닭의장풀
- 옻나무
- 광대버섯

새 종을 추가할 때는 `CreatureArt.tsx`에 SVG 아트를 추가하고, `hasCreatureArt()`가 해당
`species_id`를 인식하도록 키를 넣는다. 그러면 별도 홈가든 코드 수정 없이 보관함과 정원에
자동으로 포함된다.

## 7. 다음 작업 권장 순서

1. 서버 DB의 종별 3D 모델 레지스트리(`species_model_asset`)는 현재 앱에서 사용하지 않는다.
   이를 `species_sprite_asset` 형태로 바꾸고 `species_id → sprite_key/sprite_sheet_url`을
   제공하도록 마이그레이션한다.
2. 앱의 `hasCreatureArt()` 기반 필터를 위 API 기반 필터로 교체한다.
3. 종별 PNG/WebP 스프라이트 시트(정지·걷기·날기 프레임)를 추가한다. 현재 SVG는 MVP용이다.
4. 배경의 빈 잔디 영역에 맞춰 `gardenPointPercent()` 좌표를 실기기에서 미세 조정한다.
5. 장식 배치 엔티티를 서버 API와 DB에 추가한다. 현재 장식은 기기 로컬에만 영속 저장된다.
6. 구역별 콘텐츠와 해금 보상을 서버 저작 데이터로 옮긴다.

## 8. 주의 사항

- `app/assets/models/ladybug-mobile.glb`는 이전 실험 파일로 남아 있지만 현재 Metro 설정과 앱 코드에서는 참조하지 않는다.
- `GardenCreature3D.tsx`, `species3D.ts`와 Three.js 관련 패키지는 제거됐다.
- `IsoGrid.tsx`, `IsoTile.tsx`는 이전 타일 렌더러이며 현재 `GardenScreen`은 사용하지 않는다. 다른 화면 참조가 없는지 확인한 뒤 정리할 수 있다.
- 정원 배경은 약 2.3MB PNG다. 출시 전 WebP와 저해상도 파생본을 검토한다.
- 장식은 길게 눌러 로컬 정원에서 제거한다.

## 9. 검증 결과

- `npx tsc --noEmit` 통과
- `npx expo-doctor` 20/20 통과
- Android Expo export 통과
- 출력 번들에는 `garden-starter-world-v1.png`가 포함되고 GLB는 포함되지 않음
