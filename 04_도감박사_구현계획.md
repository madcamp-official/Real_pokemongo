# Nature Go 도감 박사(F22) 구현 계획

> 기준 문서: `도감 박사(Dex Professor) 기능 설계 명세서 v0.1`
> 기준 코드: `design` 브랜치
> 목표: 외부 생성형 LLM 없이, 검수된 도감 문장만 검색해 답하는 캐릭터형 질의응답 기능

## 1. 결론

도감 박사는 다음 구조로 구현한다.

```text
앱
  → POST /professor/ask
  → seed-service(TypeScript)
       1. 식용 질문 차단
       2. 질문 벡터 요청
       3. 정적 지식 벡터와 코사인 검색
       4. 발견 여부·안전 정책 적용
       5. 검수 문장을 박사 템플릿으로 포장
  → embedding-worker(Python, CPU)
       질문 한 문장만 384차원 벡터로 변환
```

- CAMP-3에 새로운 GPU 모델을 올리지 않는다.
- Python 임베딩 워커는 `seed-service`와 같은 서버의 CPU 프로세스로 실행한다.
- 검색·안전·도감 공개 범위 판단은 기존 TypeScript 서버가 담당한다.
- Python 워커는 문장을 벡터로 바꾸는 기능만 제공한다.
- 질문은 단발성으로 처리하고 대화 문맥은 MVP에서 지원하지 않는다.

## 2. 현재 코드 기준 확인 결과

현재 `seed-service`에는 82종이 모두 등록돼 있다.

| 데이터 | 현재 수량 |
|---|---:|
| 종 `SEED_TAXA` | 82 |
| 종 콘텐츠 `SEED_CONTENT` | 82 |
| 재미있는 사실 | 82문장 |
| 관찰 포인트 | 164문장 |
| 크기 정보 | 82개 |
| 활동 시간 | 82개 |
| 위험 태그가 있는 종 | 5종 |

기존 데이터만 활용하면 서식지·크기·활동 시간·재미있는 사실·관찰 포인트·안전 문장은 만들 수 있다. 그러나 명세의 예시 질문인 “사마귀는 뭘 먹어?”, “무슨 소리를 내?”에 안정적으로 답하려면 `먹이`와 `소리` 데이터를 별도로 보강해야 한다.

예상 초기 지식 문장은 약 500~650개다. 800문장은 먹이·소리·계절·유사종 데이터를 충분히 보강했을 때의 목표치로 본다.

## 3. 확정할 공통 계약

### 3.1 지식 레코드

```ts
type ProfessorFieldType =
  | "habitat"
  | "diet"
  | "activity"
  | "sound"
  | "size"
  | "season"
  | "safety"
  | "funfact"
  | "observation"
  | "similar";

interface KnowledgeChunk {
  chunk_id: string;
  sentence: string;
  species_id: string;
  species_name: string;
  field_type: ProfessorFieldType;
  is_safety: boolean;
  content_version: string;
}

interface KnowledgeVector extends KnowledgeChunk {
  vector: number[];
}
```

`chunk_id`는 아래 값으로 결정론적으로 생성한다.

```text
{species_id}:{field_type}:{순번}
```

### 3.2 모델·인덱스 메타데이터

`knowledge-vectors.json` 최상단에 다음 정보를 포함한다.

```json
{
  "schema_version": "professor-knowledge-v1",
  "model_id": "sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2",
  "model_revision": "고정 커밋 해시",
  "dimension": 384,
  "normalized": true,
  "content_hash": "sha256...",
  "records": []
}
```

서버 시작 시 워커의 모델 정보와 인덱스 메타데이터가 다르면 기능을 비활성화하고 명확한 오류를 남긴다. 서로 다른 모델로 만든 벡터를 조용히 비교해서는 안 된다.

### 3.3 질문 API

```http
POST /professor/ask
Authorization: Bearer ...
Content-Type: application/json
```

```json
{
  "question": "밤에 우는 벌레는 뭐야?",
  "context_species_id": null
}
```

`context_species_id`는 종 카드의 “박사에게 묻기”에서 사용한다. 값이 있으면 해당 종 문장을 우선 검색하되, 안전 정책은 동일하게 적용한다.

응답:

```ts
type ProfessorConfidence = "high" | "medium" | "low" | "unknown";

interface ProfessorAskResponse {
  confidence: ProfessorConfidence;
  answer: string;
  matched_species: {
    species_id: string;
    name: string;
    discovered: boolean;
  } | null;
  safety_warning: string | null;
  related: Array<{
    species_id: string;
    name: string;
    reason: string;
  }>;
  similarity_score: number | null;
  restricted: boolean;
  response_source: "indexed_sentence" | "fixed_safety" | "unknown";
}
```

### 3.4 공개 범위

하이브리드 정책을 적용한다.

| 상황 | 동작 |
|---|---|
| 발견한 종의 일반 정보 | 답변·종명·종 카드 링크 공개 |
| 미발견 종의 일반 정보 | 종명과 사실을 숨기고 탐색 힌트만 제공 |
| 위험 종 안전 정보 | 발견 여부와 무관하게 종명·안전 안내 공개 |
| 식용·섭취 질문 | 검색하지 않고 고정 안전 안내 |
| 임계값 미만 | unknown |

미발견 일반 종은 API 응답에서도 `matched_species: null`, `restricted: true`로 처리해 앱이 실수로 이름을 노출하지 못하게 한다.

## 4. 단계별 구현

## Phase 0. 데이터 계약과 검수 기준

### 서버 작업

1. `SpeciesContent`에 도감 박사용 검수 지식을 추가한다.

```ts
knowledgeFacts?: Array<{
  fieldType: "diet" | "sound" | "season" | "observation";
  sentence: string;
}>;
```

2. 기존 필드에서 결정론적으로 문장을 생성한다.

- `Taxon.habitatTags` → 서식지 문장
- `Taxon.activeTime` → 활동 시간 문장
- `Taxon.sizeDescription` → 크기 문장
- `Taxon.seasonTags` → 계절 문장
- `SpeciesContent.funFact` → 재미있는 사실
- `SpeciesContent.observePoints` → 관찰 포인트
- `SafetyFilter` 결과 → 안전 문장
- `confusionPairs` → 유사종 문장

3. 먹이·소리는 출처가 검수된 종만 수동 추가한다.
4. 한 문장에 하나의 사실만 담는다.
5. 식용 가능성을 암시하는 문장은 지식 데이터에 넣지 않는다.

### 산출물

- `seed-service/src/core/professor/professorTypes.ts`
- `seed-service/src/core/professor/KnowledgeIndexer.ts`
- `seed-service/src/core/professor/KnowledgeIndexer.test.ts`
- `seed-service/data/professor-knowledge.json`

### 완료 조건

- 82종 모두 최소 4개 이상의 검색 문장을 가진다.
- 빈 문장과 중복 `chunk_id`가 없다.
- 위험 태그 5종은 안전 문장을 반드시 가진다.
- 먹이·소리 데이터가 없는 종은 해당 사실을 생성하지 않는다.

## Phase 1. CPU 임베딩 워커와 인덱스 생성

### Python 워커

경로:

```text
seed-service/embedding-worker/
  pyproject.toml
  professor_worker/
    model.py
    api.py
    build_index.py
  tests/
```

역할:

- 모델 1회 로드
- 문장 배치 임베딩
- L2 정규화
- 질문 1문장 임베딩 API
- 모델 ID·리비전·차원 반환

내부 API:

```http
GET /health
POST /embed
```

```json
{
  "text": "밤에 우는 벌레는 뭐야?"
}
```

```json
{
  "model_id": "...",
  "dimension": 384,
  "normalized": true,
  "vector": []
}
```

### 빌드 명령

```text
종 데이터 → professor-knowledge.json
professor-knowledge.json → knowledge-vectors.json
```

인덱스는 런타임에 다시 만들지 않는다. 콘텐츠나 모델 버전이 바뀔 때만 명시적으로 재생성한다.

### 성능 목표

| 항목 | 목표 |
|---|---:|
| 워커 최초 모델 로드 | 10초 이내 |
| 질문 임베딩 p95 | 150ms 이하 |
| 코사인 검색 p95 | 5ms 이하 |
| `/professor/ask` 전체 p95 | 400ms 이하 |

명세의 20~50ms는 목표가 아니라 실측 전 가설로 취급한다.

### 완료 조건

- 같은 문장은 반복 호출해도 사실상 같은 벡터가 나온다.
- 벡터 길이가 항상 384다.
- 벡터의 L2 norm이 1에 가깝다.
- 인덱스 모델 버전과 런타임 모델 버전이 일치한다.
- CPU 환경에서 성능 측정 결과를 기록한다.

## Phase 2. 검색·안전·답변 서버

### 서버 모듈

```text
seed-service/src/core/professor/
  professorTypes.ts
  KnowledgeIndexer.ts
  EmbeddingClient.ts
  SemanticSearch.ts
  SafetyOverride.ts
  VisibilityPolicy.ts
  AnswerComposer.ts
  ProfessorService.ts
```

역할:

- `EmbeddingClient`: Python 워커 호출, timeout·응답 검증
- `SemanticSearch`: 정규화 벡터 내적, top-k 검색
- `SafetyOverride`: 식용 질문과 위험 종 우선 규칙
- `VisibilityPolicy`: 발견/미발견 공개 범위 결정
- `AnswerComposer`: 검수 문장을 고정 템플릿으로 포장
- `ProfessorService`: 전체 순서 조립

### 처리 순서

```text
1. 인증 사용자 확인
2. 질문 길이·형식 검증
3. 식용·섭취 의도 검사
4. 질문 임베딩
5. top-k 검색
6. 임계값 판정
7. 발견 여부 조회
8. 위험·버섯 오버라이드
9. 공개 범위 필터
10. 고정 템플릿 조립
11. 읽기 전용 응답
```

### 입력 제한

- 질문: 공백 제거 후 2~200자
- 빈 질문: 400
- 요청당 1문장
- 사용자별 rate limit 적용
- HTML·제어 문자는 정규화

### 식용 질문 우회 사전

초기 키워드:

```text
먹어, 먹을 수, 식용, 섭취, 요리, 삶아, 구워, 맛, 독버섯
```

키워드만으로 끝내지 않고 아래 문장들을 회귀 테스트로 고정한다.

- “이거 먹어도 돼?”
- “독 없으면 먹어도 되지?”
- “삶으면 괜찮아?”
- “사람이 먹는 버섯이야?”

### 신뢰도

초기값:

```text
high    >= 0.70
medium  >= 0.50
low     >= 0.35
unknown <  0.35
```

임계값은 설정 파일로 분리한다. 코드 곳곳에 숫자를 직접 넣지 않는다.

### API 라우트

- `POST /professor/ask`
- `GET /professor/suggestions`
- `GET /professor/greeting`

라우트는 인증이 필요하다. 사용자 발견 상태를 조회해야 하기 때문이다.

### composition 연결

- `App`에 `professor: ProfessorService` 추가
- `composition.ts`에서 의존성 조립
- `server.ts`에서 `registerProfessorRoutes` 등록
- `.env.example`에 워커 URL·timeout 추가

### 장애 처리

| 장애 | 응답 |
|---|---|
| 워커 미기동 | 503 + 재시도 안내 |
| 워커 timeout | 503 |
| 모델/인덱스 불일치 | 서버 시작 시 기능 비활성화 |
| 인덱스 파일 손상 | 서버 시작 실패 또는 기능 비활성화 |
| 검색 결과 없음 | 정상 200 + unknown |

BioCLIP 장애와 도감 박사 장애는 서로 영향을 주지 않는다.

## Phase 3. 앱 계약과 화면

### 타입·API

- `app/src/types/api.ts`
- `app/src/api/professor.ts`
- mock adapter 응답

추가 함수:

```ts
askProfessor(question, contextSpeciesId?)
fetchProfessorSuggestions()
fetchProfessorGreeting()
```

### 내비게이션

`RootStackParamList`:

```ts
Professor: {
  contextSpeciesId?: string;
};
```

진입점:

1. 도감 화면 우측 하단 플로팅 버튼
2. 종 카드 내부 “박사에게 묻기”

### 화면 구조

```text
ProfessorScreen
  ├─ 상단 뒤로가기·도감 박사 제목
  ├─ ProfessorAvatar
  ├─ GreetingBubble
  ├─ QuestionChips
  ├─ 단일 질문 입력창
  ├─ 전송 버튼
  ├─ AnswerBubble
  └─ 관련 종 카드 버튼
```

MVP는 대화 목록이 아니라 `질문 1개 → 답변 1개` 구조로 시작한다. 이전 답변은 다음 질문의 문맥으로 사용하지 않는다.

### UI 상태

- `idle`: 인사말과 추천 질문
- `typing`: 질문 입력
- `thinking`: 응답 대기
- `answered`: 답변·관련 카드
- `unknown`: 모름 안내
- `warning`: 안전 안내를 가장 위에 표시
- `offline`: 재시도

### 앱 안전 규칙

- `safety_warning`은 답변보다 먼저 렌더링한다.
- `restricted=true`이면 종명·종 이미지·종 카드 링크를 렌더링하지 않는다.
- `matched_species.discovered=false`라도 안전 답변이면 안전 정보만 허용한다.
- 서버 문장을 앱에서 재작성하거나 요약하지 않는다.

## Phase 4. 추천 질문·인사말

### 추천 질문

진행도와 발견 종을 기준으로 서버가 검증된 질문만 반환한다.

- 발견 종이 적음: 관찰 장소·시간 중심
- 발견 종이 많음: 유사종·특징 비교
- 위험 종 발견: 안전 질문 포함
- 특정 종 카드 진입: 해당 종 질문

추천 질문은 실제 인덱스에 답이 존재하는 것만 노출한다.

### 인사말

도감 완성도 구간:

| 완성도 | 인사 |
|---|---|
| 0~24% | 탐험 시작 안내 |
| 25~49% | 발견 격려 |
| 50~74% | 절반 달성 축하 |
| 75~99% | 완성 임박 |
| 100% | 도감 완성 축하 |

인사말은 고정 템플릿이며 생성하지 않는다.

## Phase 5. 캐릭터 아트와 연출

### 원칙

- 특정 게임의 박사 외형·이름·복장을 모방하지 않는다.
- Nature Go의 수채화·자연 관찰 톤에 맞춘 독자 캐릭터를 사용한다.
- MVP는 정적 일러스트 1장으로도 완료 가능하다.

### 표정

- 기본
- 생각
- 확신
- 갸웃
- 경고

애니메이션은 화면 연출일 뿐 답변 내용을 바꾸지 않는다. 실제 응답이 도착하면 즉시 표시하며, 인위적인 긴 지연은 넣지 않는다.

## 5. 테스트 계획

### 5.1 지식 인덱스

- 82종 모두 chunk 존재
- chunk ID 중복 없음
- 빈 문장 없음
- 벡터 차원·정규화 검증
- 콘텐츠 변경 시 hash 변경
- 같은 콘텐츠는 같은 chunk ID 유지

### 5.2 의미 검색 골든셋

최소 60개 질문을 준비한다.

| 유형 | 최소 수 |
|---|---:|
| 종 이름 직접 질문 | 10 |
| 이름 없는 특징 질문 | 15 |
| 서식지·시간·계절 | 10 |
| 먹이·소리 | 8 |
| 위험·식용 | 10 |
| 도감 밖 질문 | 7 |

각 질문에 다음을 기록한다.

- 기대 종 또는 unknown
- 기대 field type
- 기대 안전 오버라이드
- 공개 가능 여부
- 최소 허용 confidence

### 5.3 서버 불변 조건

1. `/professor/ask` 전후 도감·XP·퀘스트·개체가 동일하다.
2. 식용 질문은 임베딩 워커를 호출하지 않는다.
3. 위험 정보는 미발견 종이어도 공개된다.
4. 미발견 일반 종 이름은 응답에 포함되지 않는다.
5. unknown 응답에 임의 종명이 포함되지 않는다.
6. 답변의 사실 문장은 실제 chunk의 원문과 동일하다.
7. 워커 장애가 동정·지도·도감 API에 영향을 주지 않는다.

### 5.4 앱

- 중복 전송 방지
- 키보드에 입력창이 가리지 않음
- 긴 답변 줄 잘림 없음
- 안전 배너 최상단
- 미발견 종 링크 미노출
- 종 카드 딥링크 정상
- 네트워크 실패 후 재시도
- 화면 이탈 후 늦은 응답이 상태를 덮지 않음

## 6. 배포 계획

### 개발 환경

```text
Terminal 1: embedding-worker
Terminal 2: seed-service
Terminal 3: Expo
```

### 운영 환경

CPU 워커와 `seed-service`를 같은 호스트에서 별도 프로세스로 실행한다.

```text
embedding-worker: 127.0.0.1 전용 포트
seed-service: 외부 앱 요청 수신
```

워커 포트는 외부에 공개하지 않는다. Fastify 서버만 워커에 접근한다.

### 시작 순서

1. 워커 시작 및 `/health` 확인
2. `seed-service` 시작
3. 인덱스·모델 버전 검사
4. 앱 요청 허용

## 7. 개인정보와 로그

어린이 사용자가 장소·이름 등 개인정보를 질문에 입력할 수 있으므로 MVP에서는 원문 질문을 기본 저장하지 않는다.

기본 수집 가능 지표:

- 익명 요청 ID
- confidence tier
- 최상위 score
- 선택된 field type
- latency
- unknown 여부
- 오류 코드

임계값 튜닝을 위해 원문 질문이 필요하다면 별도 동의·보관 기간·삭제 정책을 먼저 정의한다.

## 8. 권장 작업 순서와 병렬화

### 선행 공통 작업

아래는 먼저 함께 확정해야 한다.

1. API 타입
2. 지식 chunk 스키마
3. 모델 ID·revision·차원
4. 미발견 공개 정책
5. 안전 고정 문구
6. 골든 질문 20개 초안

### 병렬 작업

공통 계약 이후 다음을 병렬 진행할 수 있다.

| 담당 | 작업 |
|---|---|
| 서버/임베딩 | Python 워커, 인덱스 생성, 성능 측정 |
| TypeScript 서버 | 검색·안전·공개 정책, 라우트, 테스트 |
| 앱 | 타입·mock·대화 UI·내비게이션 |
| 디자인/콘텐츠 | 캐릭터, 추천 질문, 먹이·소리 문장 검수 |

단, 실제 통합은 `POST /professor/ask` 계약과 mock 응답이 고정된 뒤 시작한다.

## 9. 예상 일정

| 단계 | 예상 |
|---|---:|
| Phase 0 공통 계약·데이터 점검 | 0.5일 |
| Phase 1 워커·인덱스 | 0.5~1일 |
| Phase 2 서버 검색·안전·API | 1일 |
| Phase 3 앱 MVP | 0.5~1일 |
| 골든셋·통합 테스트·튜닝 | 1일 |
| 캐릭터 아트·표정 연출 | 0.5~1일 |

기능 MVP는 약 3~4일, 캐릭터 연출까지 포함하면 약 4~5일을 예상한다. 먹이·소리 데이터 검수 범위에 따라 일정이 늘어날 수 있다.

## 10. MVP 완료 정의

다음을 모두 만족하면 도감 박사 MVP를 완료로 본다.

- 도감 화면과 종 카드에서 박사 화면에 진입할 수 있다.
- 한국어 자연어 질문을 받아 검수 문장만으로 답한다.
- 도감 밖 질문은 unknown으로 정직하게 거절한다.
- 식용 질문은 검색 없이 고정 안전 안내를 반환한다.
- 위험 종 안내가 항상 일반 답변보다 먼저 나온다.
- 미발견 일반 종의 이름과 상세 정보가 노출되지 않는다.
- 관련 발견 종 카드를 열 수 있다.
- 질문이 도감·지도·퀘스트·보상·개체를 변경하지 않는다.
- GPU 서버 없이 CPU에서 동작한다.
- 워커 장애가 기존 동정 기능에 영향을 주지 않는다.
- 골든 질문셋과 앱·서버 테스트가 통과한다.

## 11. 구현 시작 전 결정할 항목

1. 박사 캐릭터 이름과 디자인
2. 먹이·소리 데이터의 MVP 지원 범위
3. 임베딩 모델 후보를 1개로 확정할지, 2개를 골든셋으로 비교할지
4. 원문 질문 로그를 저장하지 않는 정책 승인
5. 미발견 안전 종은 완전히 숨길지, 서식지 힌트만 줄지

권장 기본값:

- 모델은 `paraphrase-multilingual-MiniLM-L12-v2`로 먼저 구현한다.
- 미발견 안전 종은 “직접 찾아보게나” 수준의 일반 힌트만 제공한다.
- 질문 원문은 저장하지 않는다.
- 먹이·소리는 데이터가 검수된 종부터 부분 지원하고, 데이터가 없으면 모른다고 답한다.
- 캐릭터 연출은 정적 1장으로 시작한다.
