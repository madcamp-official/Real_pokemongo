# 리빙 도감 (가칭) 프론트엔드 기능명세서

### 현실 생물 채집·동정·육성 서비스 — "현실판 포켓몬고"

---

## 0. 문서 정보

| 항목 | 내용 |
| --- | --- |
| 문서 목적 | 「리빙도감 FE/BE 기능명세서 v0.3」에서 **프론트엔드(클라이언트 앱)** 담당 범위만 분리·재구성 |
| 기반 문서 | 리빙도감_FE_BE_기능명세서.md (v0.3) |
| 버전 | v1.0 (FE 단독판) |
| 우선순위 표기 | P0 = MVP 필수 · P1 = 정식 출시 목표 · P2 = 이후 확장 |
| 범위 제외 | F15(오프라인 모드)는 스코프 제외, 번호 결번 유지 |
| 변경 이력 | v1.1: F19(터치 기반 사전 위험 경고) 신규 추가 / v1.2: 보호자·자녀 2단계 계정 모델을 단일 사용자 계정으로 통합, F13(보호자 대시보드) 제거 |

---

## 1. 프론트엔드 역할 원칙

- 화면/입력/카메라·센서 제어
- 로컬 상태 관리
- 3D 렌더링(three.js)
- 서버 API 호출 및 응답 반영
- AI 추론(사진 보정, 종 동정 등)은 자체 처리하지 않으며, 전량 백엔드(AI 추론 서버)에 위임 — 프론트는 결과 수신 및 화면 반영만 담당

---

## 2. 기능별 프론트엔드 상세 명세

### F1. 온보딩 & 회원가입 · P0

> v1.2: 보호자/자녀 2단계 계정 구조를 제거하고 단일 사용자 계정 모델로 단순화.

- 계정 생성 UI(이메일/비밀번호 + 닉네임/아바타를 한 화면에서 입력)
- 순차 동의 화면(개인정보/위치/사진) 및 동의 상태 로컬 표시
- 최초 실행 3단계 튜토리얼 플로우 및 실제 카메라 연동 데모
- 게스트 모드 로컬 세션 관리(비로그인 상태에서 촬영 1~2회 임시 저장), 한도 도달 시 계정 생성 유도

**호출 API**: `POST /auth/signup`, `POST /auth/consent`, `POST /session/guest/convert`

---

### F2. 생물 촬영 · P0

- 카메라 프리뷰 및 단일/버스트 촬영 UI
- 가이드 오버레이(프레이밍·근접 경고)
- 촬영 직후 로딩 애니메이션
- 갤러리 업로드 연동(메타데이터 유무 판별 후 지도 기록 반영 여부 클라이언트에서 1차 필터링)
- 카메라/위치 권한 요청 플로우(위치는 별도 분리 요청)
- 촬영 원본 이미지의 로컬 임시 저장 및 서버 업로드 큐 관리

**호출 API**: `POST /sightings/upload` (multipart, 위치 메타데이터 optional) → 응답으로 받은 `sighting_id`로 결과 폴링 또는 소켓 수신

---

### F3. 저품질 사진 보정 · P1 (서버 AI 처리 결과 반영)

- 별도 보정 처리 없음 — 원본/버스트 프레임을 그대로 서버에 전송
- 서버 응답의 신뢰도(`confidence`)·재촬영 필요 여부(`retake_suggested`)에 따라 "다시 찍어볼까요?" 가이드 UI 노출

**연동**: `POST /vision/enhance` 응답 소비 (내부 파이프라인, F2 업로드에 연쇄 호출되므로 프론트에서 직접 호출하지 않음)

---

### F4. AI 동정 · P0

- 동정 결과 수신 후 UI 렌더링
- 신뢰도 낮을 시 "어떤 모습에 가까운가요?" 선택 UI
- 동정 실패 시 재시도 유도 카피 노출
- 위험 생물 판별 시 종 카드(F6)를 우선 노출하는 화면 우선순위 처리
- "새로운 친구 발견!" 연출 트리거

**호출 API**: `POST /identify` → `{candidates:[{species_id, confidence}], is_dangerous, needs_user_confirmation}` 수신, 사용자 후보 선택 시 `POST /identify/confirm`

---

### F5. 도감 · P0

- 그리드/리스트 뷰 렌더링
- 미발견 종 실루엣 처리
- 필터·정렬 UI(분류/서식지)
- 종 카드 상세 화면 라우팅
- 도감 완성도(%) 표시
- 개체 단위 구분 표시(같은 종이라도 서로 다른 Creature로 리스트업)

**호출 API**: `GET /dex?filter=...&sort=...`, `GET /dex/completion`, `GET /creatures/{id}`

---

### F6. 종 카드 콘텐츠 · P0

- 종 카드 UI(이름/서식지/크기/활동시간대/재미있는 사실/유사종 팁)
- 위험 생물 안전 수칙 섹션 최상단 고정 렌더링
- 홈 가든(F16)·퀘스트(F10)에서 동일 콘텐츠 재사용 시 캐시 활용

**호출 API**: `GET /species/{id}/card`

---

### F7. 3D 모델 생성·렌더링 · P1

- three.js 기반 실시간 렌더링(홈 가든·도감 상세 공용 컴포넌트화)
- 종별 베이스 리그 + 팔레트 스와핑 적용 로직 (서버가 내려준 컬러 슬롯 값·무늬 프리셋 ID를 셰이더/머티리얼에 매핑)
- 3D 에셋 미존재 종에 대한 2D 일러스트 폴백 + "준비 중" 배지 처리
- 에셋 로딩 최적화(캐싱, LOD)

**호출 API**: `GET /assets/species/{id}/base_model`, `GET /creatures/{id}/variation`

---

### F8. 배지 · 레벨 보상 · P1

- 경험치 바/레벨업 연출
- 배지 목록 UI(테마별 그룹핑)
- 레벨업 시 언락된 데코레이션/아이템/필터를 F16·촬영 화면에 반영

**호출 API**: `GET /profile/xp`, `GET /badges`, `POST /badges/claim`

---

### F9. 친밀도(Bond) 시스템 · P1

- Bond 게이지 UI
- 홈 가든 탭 상호작용 이벤트 전송
- Bond 구간별 반응 모션 풀/장식 해금 상태 반영
- "오랜만이에요!" 재회 연출 트리거

**호출 API**: `POST /creatures/{id}/interact`, `GET /creatures/{id}/bond`

---

### F10. 퀘스트 시스템 · P1

- 퀘스트 목록/진행률 UI
- 퀘스트 힌트에 종 카드(F6) 콘텐츠 연동 표시
- 완료 연출 및 보상 수령 UI
- 누적 도감 수 기준으로 서버가 내려준 난이도별 퀘스트 노출

**호출 API**: `GET /quests?active=true`, `POST /quests/{id}/progress`(서버가 자동 판정할 수도 있음), `POST /quests/{id}/claim`

---

### F11. 지도 & 탐험 기록 · P1

- 지도 UI 렌더링
- 발견 핀 표시
- "나만의 탐험 구역"/"아직 안 가본 곳" 하이라이트 UI
- 사용자가 설정한 정밀도 옵션에 따른 로컬 표시 처리

**호출 API**: `GET /map/pins`, `GET /map/explored-regions`

---

### F12. 알림 & 재참여 유도 · P2

- 푸시 알림 수신·표시(OS 알림 채널 연동)
- 알림 클릭 시 딥링크 라우팅(해당 개체/퀘스트 화면으로 이동)
- 알림 총량·시간대 설정 UI(F18 설정과 연동)

**연동**: `POST /notifications/preferences`(설정 전송), 서버 → 클라이언트 푸시(딥링크 payload 포함) 수신

---

### ~~F13. 보호자 대시보드 & 안전장치~~ · 제거됨 (v1.2)

> 단일 사용자 계정 모델로 전환하면서 보호자 전용 화면(로그인 분리, 사용 시간 제한, 보호자 알림)을 스코프에서 제외했다. 알림/개인정보 수집 범위 설정은 F18(설정)로 흡수, 닉네임 금칙어 필터는 F1 계정 생성 입력 검증으로 흡수. 번호 결번 유지.

---

### F14. 소셜 기능 · P2

- 친구 목록/추가 요청 UI(상대방 수락 대기 상태 표시)
- 도감 완성도 비교 화면
- 친구 홈 가든 읽기 전용 뷰어
- 이모지/스티커 반응 UI(자유 텍스트 채팅 없음)

**호출 API**: `POST /friends/request`, `POST /friends/approve`, `GET /friends/{id}/garden`(read-only), `POST /reactions/send`

---

### F16. 홈 가든 (3D 개체 전시 공간) · P1

- isometric 6×6 그리드 렌더링(배경은 2D 레이어, 개체는 F7 3D 컴포넌트 재사용)
- 개체 드래그 배치 UI 및 타일-개체 호환성 로컬 검증(예: 수생 곤충→물웅덩이 타일만 허용)
- 탭 반응 모션·효과음·햅틱 트리거
- 기기 시간·계절에 따른 개체 포즈/배경 조명·파티클 전환 로직(클라이언트에서 현재 시각 기준 계산)
- 상태 카드 바텀시트(이름/함께한 일수/Bond 게이지/오늘의 상태 문구)
- 작명식 연출 및 이름 입력 UI
- 배치 데이터 로컬 우선 저장

**호출 API**: `GET/PUT /garden/layout`, `GET /garden/tile-compatibility`, `POST /creatures/{id}/name`(작명), `GET /creatures/{id}/status`(시간/계절 연산은 클라이언트 수행, 기준 데이터는 서버 제공)

---

### F17. 계절 · 한정 이벤트 · P2

- 이벤트 배너/한정 종 안내 UI
- 이벤트 기간 한정 퀘스트·데코레이션 노출
- 이벤트 종료 후에도 획득 아이템은 정상 노출(로컬 상태 유지)

**호출 API**: `GET /events/active`

---

### F18. 설정 & 계정 관리 · P0

> v1.2: 옛 F13(보호자 대시보드)의 알림/수집 범위 설정을 이 기능으로 흡수.

- 설정 화면(알림/개인정보/위치정보 수집 범위 토글)
- 계정 정보 표시(닉네임/아바타/이메일), 로그아웃
- 기기 변경 시 데이터 복원 UI
- 계정 삭제 요청 플로우 및 안내 문구

**호출 API**: `GET /account/restore-bundle`, `DELETE /account`

---

### F19. 터치 기반 사전 위험 경고 (신규) · P1

> 포켓몬고처럼 카메라 프리뷰 화면에서 피사체를 직접 터치하면, 셔터를 누르기 전에 대략적인 위험 여부를 미리 알려주는 기능. F2(촬영)의 프리뷰 화면을 확장하고, F4(AI 동정) 파이프라인의 경량 버전을 재사용한다.

- 카메라 프리뷰 화면에서 피사체(동물) 터치 제스처 인식(셔터 버튼과는 별도 동작)
- 터치 좌표 기준으로 프리뷰 프레임을 크롭하여 서버로 전송
- 응답 대기 중 터치 지점에 로딩 인디케이터(스피너/펄스) 표시
- 응답 수신 후 화면 위 오버레이 라벨로 "대략적인 동물 이름 + 위험 여부" 표시(포켓몬고식 말풍선/라벨 UI)
- 위험 판정 시 촬영 전 경고 문구 노출, 그래도 촬영을 계속할지는 사용자가 선택
- 이 프리뷰 판별 결과는 **잠정치**이며, 실제 셔터 촬영 후 F4의 정밀 동정 결과로 최종 대체됨을 UI 문구로 명확히 안내(과신 방지)
- 매 프레임 상시 스캔은 하지 않음 — 터치 시점에만 1회 호출(배터리·발열 부담 최소화)

**호출 API**: `POST /vision/preview-scan` (신규, 터치 좌표 + 크롭 이미지 전송) → `{species_guess, is_dangerous, confidence}` 수신

---

## 3. 프론트엔드 → 백엔드 API 호출 목록

| 엔드포인트 | 메서드 | 관련 기능 |
| --- | --- | --- |
| `/auth/signup` | POST | F1 |
| `/auth/consent` | POST | F1 |
| `/session/guest/convert` | POST | F1 |
| `/sightings/upload` | POST | F2 |
| `/identify` | POST | F4 |
| `/identify/confirm` | POST | F4 |
| `/dex` | GET | F5 |
| `/dex/completion` | GET | F5 |
| `/creatures/{id}` | GET | F5 |
| `/species/{id}/card` (age_group 파라미터 v1.2에서 제거) | GET | F6 |
| `/assets/species/{id}/base_model` | GET | F7 |
| `/creatures/{id}/variation` | GET | F7 |
| `/profile/xp`, `/badges`, `/badges/claim` | GET/POST | F8 |
| `/creatures/{id}/interact`, `/bond` | POST/GET | F9 |
| `/quests`, `/quests/{id}/progress`, `/quests/{id}/claim` | GET/POST | F10 |
| `/map/pins`, `/map/explored-regions` | GET | F11 |
| `/notifications/preferences` | POST | F12 |
| `/friends/*`, `/reactions/send` | POST/GET | F14 |
| `/garden/layout`, `/garden/tile-compatibility`, `/creatures/{id}/name`, `/creatures/{id}/status` | GET/PUT/POST | F16 |
| `/events/active` | GET | F17 |
| `/account/restore-bundle`, `/account` | GET/DELETE | F18 |
| `/vision/preview-scan` | POST | F19 |

---

## 4. 프론트엔드 소유 데이터

| 데이터 | 저장 위치 | 비고 |
| --- | --- | --- |
| 로그인/세션 토큰 | 로컬 secure storage | 서버 발급, 클라이언트 보관 |
| 촬영 원본 이미지 | 로컬(임시) | 업로드 후 로컬 캐시 정리 정책 필요 |
| 홈 가든 배치(GardenLayout) | 로컬 우선 → 서버 동기화 | 다중 기기 사용 시 충돌은 서버 기준 병합을 따름 |

---

## 5. 릴리즈 로드맵 (프론트엔드)

- **MVP(v1.0)**: F1·F2 기본 UI, F5·F6 조회 화면, F16 축소 버전(2D 일러스트 기반)
- **정식 출시(v1.x)**: F7 3D 렌더링, F9 Bond UI, F10 퀘스트 UI, F11 지도, F16 전체 기능
- **확장(v2.0)**: F12 알림 수신 UI, F14 소셜 화면, F17 이벤트 배너

---

## 6. 프론트엔드 관점 리스크

1. **네트워크 불안정 구간 대응**: 오프라인 모드 미지원. 지하·터널 등 일시적 신호 저하 구간에서 업로드 실패 시 재시도 로직 필요.
2. **동기화 충돌**: 홈 가든 배치처럼 로컬 우선 저장 데이터는 다중 기기 사용 시 충돌 해결 정책(서버 기준 병합)을 백엔드와 동일하게 구현해야 함.
3. **F19 잠정치 오인 위험**: 터치 시 나오는 결과는 정밀 동정이 아닌 잠정 추정치이므로, 아이가 "안전하다"고 과신하지 않도록 문구·UI 톤 설계에 주의 필요.
4. **보호자 감독 부재**: v1.2에서 보호자 대시보드를 제거해 사용 시간 제한·보호자 알림이 없어졌음 — 아동 보호가 필요한 서비스로 재확장할 경우 별도 기능으로 재검토 필요.

---

*본 문서는 「리빙도감_FE_BE_기능명세서.md (v0.3)」에서 프론트엔드 담당 범위만 재구성한 문서이며, 상세 API 요청/응답 스키마는 별도 문서로 후속 작성 예정.*
