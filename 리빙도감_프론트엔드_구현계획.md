# 리빙 도감 — 프론트엔드 구현 계획 (React Native / Expo)

### 단계별 실행 로드맵

---

## 0. 문서 정보

| 항목 | 내용 |
| --- | --- |
| 문서 목적 | 「리빙도감_프론트엔드_기능명세서.md (v1.2)」를 실제 구현 순서로 재배열한 실행 계획 |
| 대상 스택 | React Native + Expo (TypeScript) |
| 3D | expo-gl + @react-three/fiber (three.js) |
| 전제 | 백엔드 API 스키마 미확정 → mock API 레이어로 병행 개발 |
| 원칙 | P0(MVP) 화면 우선, 백엔드 완성을 기다리지 않고 UI 선행 |

---

## 1. 기술 스택 결정

| 영역 | 선택 | 비고 |
| --- | --- | --- |
| 프레임워크 | Expo (managed workflow) | 카메라·위치·푸시 네이티브 모듈 접근 용이 |
| 언어 | TypeScript | API 응답 타입 안정성 |
| 네비게이션 | React Navigation (또는 expo-router) | 탭 + 스택 구조 |
| 상태관리 | Zustand | 로컬 우선 저장(F16) 패턴에 가벼움 |
| 영속화 | MMKV 또는 AsyncStorage | 토큰·게스트 세션·GardenLayout |
| 서버 상태 | TanStack Query (React Query) | 캐싱·폴링·재시도 내장 |
| API 클라이언트 | axios + 인터셉터 | 토큰 주입, 401 처리 |
| 3D | @react-three/fiber + expo-gl + expo-three | F7·F16 |
| Mock | MSW 또는 로컬 mock 모듈 | 백엔드 대기 없이 개발 |

---

## 2. 단계별 구현 계획

### Phase 0 — 프로젝트 초기화 (0.5주) ✅ 완료

- [x] `npx create-expo-app` (blank-typescript 템플릿)으로 `app/` 생성
- [x] 폴더 구조 설계 (`src/screens`, `src/components`, `src/api`, `src/store`, `src/types`, `src/mocks`, `src/config`, `src/navigation`)
- [x] tsconfig path alias(`@/*`) 설정 + 타입 체크 통과
- [x] React Navigation 설치 및 루트 탭 네비게이터(촬영/도감/홈가든/설정) 뼈대
- [x] axios 인스턴스 + `src/config/env.ts`로 base URL·타임아웃 분리, 토큰 인터셉터
- [x] TanStack Query Provider 설정(`queryClient` 재시도/스테일 정책)
- [x] zustand 인증/게스트 세션 스토어(AsyncStorage persist)
- [x] mock API 어댑터 초기 셋업(URL 매핑 테이블 + 더미 응답) — `USE_MOCK` 플래그
- [x] Android 번들 export로 alias·빌드 검증 통과

**산출물**: 앱이 빌드되고 빈 탭 4개가 전환되는 상태 ✅

> 실행: `cd app && npm run android` (또는 `npm run web` / `npm run ios`)

---

### Phase 1 — 촬영 코어 (F2) · P0 (1.5주) ✅ 완료

> 이 앱의 존재 이유. 가장 먼저, 가장 탄탄하게.

- [x] `expo-camera` 권한 요청 플로우 (카메라 전용 요청, 위치는 촬영 시 분리 요청) — `useCameraPermissions`, `CameraView`
- [x] 카메라 프리뷰 화면 + 셔터 버튼 UI — `CameraScreen`, `CaptureButton`
- [x] 단일 촬영 → 버스트(연속 3프레임) 촬영 모드 토글 — `useCapture`
- [x] 가이드 오버레이 (중앙 프레이밍 가이드 + 힌트 카피) — `GuideOverlay`
- [x] 촬영 직후 로딩 애니메이션 — `CapturingLoader`
- [x] 촬영 원본 로컬 영구 저장 (SDK 57 File API, document 영역) — `photoStorage`
- [x] 서버 업로드 큐 관리 (재시도 3회·선형 backoff·영속화·재수화 복구 — 리스크 1) — `uploadQueueStore`, `UploadStatusStrip`
- [x] `POST /sightings/upload` (multipart) 연동 → `sighting_id` 수신 — `sightings.ts` (mock 어댑터로 검증)
- [x] 갤러리 업로드 연동 (expo-image-picker) + EXIF 위치 유무 1차 필터링 — `gallery.ts`

**산출물**: 사진을 찍어 서버(mock)에 올리고 `sighting_id`를 받는 전체 흐름 ✅

> 검증: `tsc --noEmit` 통과, Android 번들 export 성공(949 모듈).
> 미사용 설치분: `expo-image-manipulator`(F19 크롭용으로 Phase 6에서 사용 예정).
> 실기기 카메라 동작은 dev build/실기기에서 확인 필요(번들·타입은 검증 완료).

---

### Phase 2 — 온보딩 & 인증 (F1) · P0 (1주) ✅ 완료 (v1.2에서 단일 계정 모델로 리팩터링)

> v1.2: 보호자/자녀 2단계 계정 구조를 제거하고 단일 사용자 계정으로 통합. 초기 구현(GuardianSignup/ChildProfile 2화면)을 `SignupScreen` 1화면으로 병합.

- [x] 게스트 모드 로컬 세션 (비로그인 촬영 2회까지 임시 저장, `GUEST_SIGHTING_LIMIT`) — `authStore`, 한도 도달 시 카메라 화면에서 전환 유도
- [x] 계정 생성 UI (이메일/비밀번호 + 닉네임/아바타 한 화면) → `POST /auth/signup` — `SignupScreen`
- [x] 순차 동의 화면 (개인정보→위치→사진 3단계) + 동의 상태 로컬 표시 → `POST /auth/consent` — `ConsentScreen`
- [x] 3단계 튜토리얼 플로우 + 실제 카메라 프리뷰 데모(3단계에서 권한 요청 후 라이브 프리뷰) — `TutorialScreen`
- [x] 게스트 → 정식 전환 마이그레이션 → `POST /session/guest/convert` — Consent/Signup을 `mode: 'convert'`로 재사용, 설정 탭 배너·카메라 한도 초과 알림에서 진입
- [x] 토큰 secure storage 보관(`expo-secure-store`) + axios 인터셉터 주입 — `secureToken.ts`, authStore는 토큰을 영속화하지 않고 앱 시작 시 `hydrateToken()`으로 복원
- [x] 로그아웃 액션(`authStore.logout`) — 세션 정리 후 `Choice` 화면으로 복귀, `onboardingComplete`는 유지(재방문 시 튜토리얼 생략)

**산출물**: 신규 사용자가 튜토리얼→선택(게스트/가입)→(동의→계정 생성)→메인까지 도달, 게스트는 한도 도달 시 동일 플로우로 전환, 로그아웃 후 재로그인 가능 ✅

> 네비게이션 재구성: 기존 탭 네비게이터를 `MainTabs`로 분리하고, 루트를 native-stack으로 바꿔 온보딩 화면들과 `Main`을 한 스택에 배치. 초기 라우트는 `onboardingComplete`·세션(`accessToken`/`isGuest`) 조합으로 Tutorial/Choice/Main 3분기.
> 검증: `tsc --noEmit` 통과, Android 번들 export 성공(981 모듈, v1.2 리팩터 후 재검증).
> 실기기 확인 필요: 온보딩 전체 흐름의 실제 조작감(스토리지 재시작 후 하이드레이션 동작 포함)은 dev build/실기기 테스트 권장.

---

### Phase 3 — 동정 결과 & 도감 (F4, F5, F6) · P0 (2주)

- [x] `sighting_id` 기반 동정 결과 대기 처리 — 업로드 큐 항목을 구독해 sighting_id 확보 후 동정 호출 (`IdentifyResultScreen`)
- [x] F4 동정 결과 화면: 후보 리스트, "어떤 모습에 가까운가요?" 선택 UI — `needs_user_confirmation` 분기
- [x] `POST /identify/confirm` 사용자 선택 확정 → 도감 캐시 무효화 → 종 카드로 이동
- [x] "새로운 친구 발견!" 연출 (✨ + 컬러 썸네일 + 확신도)
- [x] 위험 생물 판별 시 종 카드 우선 노출 — 동정 결과에서 위험이면 "안전 정보 보기"로 안전 수칙 최상단 카드로 유도
- [x] F5 도감: 3열 그리드, 미발견 종 실루엣("???"), 분류 필터(전체/곤충/양서류/식물), 완성도(%) 헤더+프로그레스 — 시안 반영
- [x] F6 종 카드 상세: 히어로 + 안전/위험 배지 + 2×2 정보 타일(서식지/크기/활동시간/희귀도) + 재미있는 사실 + CTA(정원 초대) + 유사종 칩, 위험 시 안전 수칙 최상단 고정 — 시안 반영
- [x] 종 카드 콘텐츠 캐시 (react-query, `['species', id]`)

**산출물**: 촬영 → 동정 → (새 친구 발견/후보 선택) → 도감 등록 → 상세 열람까지 P0 핵심 루프 완성 ✅

> 디자인: Phase 3 시안(코랄+크림)을 반영해 공용 테마(`theme/colors`, `theme/species`) 신설. 탭 바 강조색도 코랄로 통일. 종 일러스트는 이모지+파스텔 원형으로 근사(실제 일러스트/3D는 F7·디자인 핸드오프에서 교체).
> 네비게이션: `SpeciesCard`(카드), `IdentifyResult`(풀스크린 모달)를 루트 스택에 추가. 카메라 촬영 → 업로드 큐 등록 → IdentifyResult 자동 진입.
> 검증: `tsc --noEmit` 통과, Android 번들 export 성공(980 모듈).
> 후속 검토: 시안의 5탭(홈/도감/[카메라]/지도/나 + 중앙 FAB) 구조는 홈·지도·프로필 화면(후속 Phase)이 생기면 네비게이션 재편으로 반영 예정. 온보딩/카메라 화면의 기존 그린 톤도 코랄 테마로 점진 수렴 필요.

---

### Phase 4 — 설정 & 계정 관리 (F18) · P0 (1주) ✅ 완료

> v1.2: 옛 F13(보호자 대시보드)이 통째로 제거되면서, Phase 4 스코프를 F18(설정) 중심으로 재정의. 알림/수집 범위 설정을 F18이 흡수.

- [x] 설정 상태 스토어(`settingsStore`) — 알림/위치수집/사진수집 3개 토글, 온보딩 동의값으로 초기화(`initFromConsent`)
- [x] 설정 화면 UI — 계정 카드(닉네임/아바타/이메일) 또는 게스트 배너, 알림·개인정보 섹션(Switch 3개), 계정 섹션(데이터 복원/로그아웃/계정 삭제)
- [x] 카메라 화면의 위치 첨부 기본값을 `settingsStore.locationCollectionEnabled`와 연동
- [x] 데이터 복원 → `GET /account/restore-bundle` — `api/account.ts`
- [x] 계정 삭제 플로우(확인 다이얼로그 → `DELETE /account` → 로그아웃 → Choice 이동) — `api/account.ts`
- [x] 로그아웃 → `authStore.logout()` + Choice 화면 리셋
- [x] 게스트 전용: "게스트 체험 종료" 액션(로컬 게스트 데이터 초기화)

**산출물**: MVP(v1.0) 범위 완료 ✅

> 검증: `tsc --noEmit` 통과, Android 번들 export 성공(981 모듈).
> 스코프 밖(의도적 제외): 사용 시간 제한, 보호자 알림, 닉네임 금칙어 서버 재검증 — 옛 F13 항목으로 단일 계정 모델에서는 대상이 없어 제거. 아동 보호 기능이 필요해지면 별도 기능으로 재도입 검토.

---

### 🏁 MVP(v1.0) 릴리즈 지점

F1·F2·F4·F5·F6·F18 + F16 축소버전(아래 Phase 5의 2D 부분)

---

### Phase 5 — 홈 가든 축소판 (F16 2D) · P0/P1 (1.5주) ✅ 완료

- [x] isometric 6×6 그리드 렌더링 (다이아몬드 타일, 2D 레이어) — `IsoGrid`, `IsoTile`, `theme/garden`(투영/역투영 수학)
- [x] 개체를 2D 이모지 마커로 배치 (3D는 Phase 7에서 교체) — `IsoGrid` 마커
- [x] 개체 드래그 배치 UI + 타일-개체 호환성 로컬 검증 — `CreatureTray`(PanResponder 드래그) + `screenToTile` 드롭 히트테스트 + `GET /garden/tile-compatibility` 기반 호환 타일 하이라이트/비호환 경고
- [x] 배치 데이터 로컬 우선 저장 → `GET/PUT /garden/layout` 동기화 — `gardenStore`(persist, 배치 변경 시 자동 PUT)
- [x] 상태 카드 바텀시트 (이름/함께한 일수/Bond 게이지/오늘의 상태) — `CreatureStatusSheet`, `BondGauge`, `GET /creatures/{id}/status`
- [x] 작명식 연출 + 이름 입력 UI → `POST /creatures/{id}/name` — 미작명 개체는 시트에서 작명, 확정 시 ✨ 축하 + 로컬 오버라이드 저장
- [x] 시간대 인사(기기 시각 기반) — 배경/포즈 전환의 축소 버전

**산출물**: 3D 없이도 동작하는 홈 가든 (2D 폴백) ✅

> 상호작용: 트레이 칩을 드래그 → 격자에 드롭(호환 타일만), 배치된 개체 탭 → 상태 시트, 시트에서 "정원에서 치우기"로 트레이 복귀. 한 타일당 한 개체.
> 검증: `tsc --noEmit` 통과, Android 번들 export 성공(988 모듈). 드롭 좌표는 `measureInWindow` + iso 역투영으로 계산 — 픽셀 정밀 튜닝은 실기기 확인 권장.
> Phase 7 연계: `IsoGrid`의 이모지 마커를 three.js 개체 컴포넌트로 교체하면 3D 전환 완료. Bond 실제 증가 로직·구간 해금은 Phase 8(F9)에서 연결(현재는 표시 전용).

---

### Phase 6 — 사전 위험 경고 (F19) · P1 (1주)

- [ ] 프리뷰 화면 터치 제스처 인식 (셔터와 별도)
- [ ] 터치 좌표 기준 프레임 크롭 + 저해상도 전송
- [ ] 터치 지점 로딩 인디케이터
- [ ] `POST /vision/preview-scan` → `{species_guess, is_dangerous, confidence}`
- [ ] 포켓몬고식 오버레이 라벨 (동물 이름 + 위험 여부)
- [ ] 위험 시 촬영 전 경고 + "잠정치" 안내 문구 (리스크 4 대응)

**산출물**: 찍기 전에 터치로 위험 미리보기

---

### Phase 7 — 3D 렌더링 (F7 + F16 3D 전환) · P1 (2.5주)

- [ ] expo-gl + @react-three/fiber 브릿지 셋업
- [ ] three.js 렌더링 공용 컴포넌트 (홈가든·도감 상세 공유)
- [ ] 베이스 리그 + 팔레트 스와핑 (컬러 슬롯·무늬 프리셋 ID → 머티리얼 매핑)
- [ ] `GET /assets/species/{id}/base_model`, `GET /creatures/{id}/variation`
- [ ] 3D 미존재 종 → 2D 폴백 + "준비 중" 배지
- [ ] 에셋 캐싱 / LOD 최적화
- [ ] Phase 5의 2D 개체를 3D 컴포넌트로 교체
- [ ] 시간·계절에 따른 포즈/조명/파티클 전환 (클라이언트 시각 연산)

**산출물**: 홈 가든·도감 상세에 실제 3D 개체 표시

---

### Phase 8 — 육성 시스템 (F8, F9, F10) · P1 (2주)

- [ ] F9 Bond 게이지 UI + 탭 상호작용 이벤트 → `POST /creatures/{id}/interact`
- [ ] Bond 구간별 모션/장식 해금 반영, "오랜만이에요!" 재회 연출
- [ ] F8 경험치 바/레벨업 연출, 배지 목록 (테마 그룹핑), `POST /badges/claim`
- [ ] 레벨업 언락 아이템을 홈가든·촬영 화면에 반영
- [ ] F10 퀘스트 목록/진행률, 힌트에 종 카드 연동, 완료·보상 수령 UI

**산출물**: 반복 참여 동기 부여 루프

---

### Phase 9 — 지도 (F11) · P1 (1주)

- [ ] 지도 UI (react-native-maps) + 발견 핀 표시
- [ ] "나만의 탐험 구역" / "아직 안 가본 곳" 하이라이트
- [ ] 사용자 정밀도 옵션(F18 설정)에 따른 로컬 표시 처리
- [ ] `GET /map/pins`, `GET /map/explored-regions`

**산출물**: 탐험 기록 시각화

---

### 🏁 정식 출시(v1.x) 릴리즈 지점

Phase 5~9 (F7·F9·F10·F11·F16 전체 + F19)

---

### Phase 10 — 확장 (F12, F14, F17) · P2 (2주+)

- [ ] F12 푸시 알림 수신·표시 + 딥링크 라우팅, 알림 설정 UI
- [ ] F14 소셜: 친구 요청/수락 대기, 도감 완성도 비교, 친구 홈가든 read-only, 스티커 반응
- [ ] F17 이벤트 배너 / 한정 종·데코레이션 노출

**산출물**: v2.0 확장 기능

---

## 3. 공통 인프라 (전 Phase 병행)

- **API 타입 정의**: 명세서 응답 스키마를 `src/types`에 TypeScript 타입으로 선점 (백엔드 확정 시 조정)
- **Mock 서버**: 각 Phase 진입 시 해당 엔드포인트 mock 우선 작성 → 실제 API 교체
- **에러/재시도**: 업로드 큐, 폴링 타임아웃, 오프라인 감지 배너 (리스크 1)
- **동기화 정책**: GardenLayout 등 로컬 우선 데이터의 서버 기준 병합 규칙을 백엔드와 합의 (리스크 2)
- **디자인 시스템**: 아동 대상 → 큰 터치 타깃, 명료한 색/아이콘, 접근성

---

## 4. 백엔드와 사전 합의가 필요한 항목 (블로커)

| 항목 | 내용 | 관련 |
| --- | --- | --- |
| 동정 결과 수신 방식 | 폴링 vs WebSocket/소켓 | F2·F4 |
| GardenLayout 충돌 병합 규칙 | 서버 기준 병합의 구체 정책 | F16 (리스크 2) |
| 응답 스키마 확정 | 각 API 요청/응답 JSON 구조 | 전체 |
| 3D 에셋 포맷 | glTF/glb 여부, 컬러 슬롯·무늬 프리셋 데이터 형태 | F7 |

---

## 5. 예상 일정 요약

| 마일스톤 | 포함 Phase | 누적 기간(대략) |
| --- | --- | --- |
| MVP(v1.0) | Phase 0~5 | ~7.5주 |
| 정식 출시(v1.x) | Phase 6~9 | +7.5주 |
| 확장(v2.0) | Phase 10 | +2주~ |

> 기간은 1인 개발 기준 러프 추정. 3D(Phase 7)는 에셋 준비 상태에 따라 변동 폭이 큼.

---

*본 계획은 프론트엔드 기능명세서 v1.2를 기준으로 하며, 백엔드 API 스키마 확정 시 Phase별 세부 항목이 조정될 수 있음.*
