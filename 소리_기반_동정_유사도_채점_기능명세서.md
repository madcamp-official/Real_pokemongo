# 소리 기반 동정 & 소리 유사도 채점 기능명세서

> 문서 목적: Nature Go에 소리 기반 생물 동정과 소리 유사도 채점 기능을 추가하기 위한 제품·UX·API·데이터·모델·안전 요구사항을 정의한다.
>
> 대상 독자: 기획자, 디자이너, 앱/서버/ML 개발자, QA 담당자, 후속 작업을 수행할 AI
>
> 작성 기준 브랜치: `living-dex_ver2.6`
>
> 문서 상태: 구현 전 제안 명세
>
> 최종 수정일: 2026-07-28

---

## 1. 한눈에 보는 기능

이 기능은 다음 두 가지를 제공한다.

1. **소리 기반 동정**
   - 사용자가 주변의 새·곤충·양서류 등의 소리를 짧게 녹음한다.
   - 서버가 녹음 품질을 검사하고 생물 종 후보를 반환한다.
   - 사용자가 후보를 직접 확인한 뒤에만 관찰 기록·도감·보상에 반영한다.

2. **소리 유사도 채점**
   - 사용자가 녹음한 소리가 선택한 종의 검증된 참조 소리와 얼마나 유사한지 0~100점으로 보여준다.
   - 점수는 “그 종일 확률”이 아니라 **참조 소리와의 음향적 유사도**이다.
   - 소음, 짧은 녹음, 여러 종의 겹침 등 점수에 영향을 준 요소를 함께 설명한다.

두 기능은 같은 녹음 파일을 사용할 수 있지만 결과의 의미와 저장 정책은 분리한다.

---

## 2. 목표와 비목표

### 2.1 목표

- 사진을 찍기 어려운 새와 야행성 생물을 소리로 발견할 수 있게 한다.
- 어린 사용자도 3단계 이내에서 녹음하고 결과를 이해할 수 있게 한다.
- 오동정이 도감·퀘스트·보상에 자동 반영되는 일을 막는다.
- 점수만 보여주는 것이 아니라 개선 가능한 녹음 피드백을 제공한다.
- 기존 사진 동정, 지도, 도감, 퀘스트, 보상 흐름에 부수적인 문제를 일으키지 않는다.
- 모델과 참조 음원 버전을 기록하여 같은 입력의 결과를 재현할 수 있게 한다.
- 사람 목소리와 위치 정보가 포함될 수 있는 오디오의 개인정보 위험을 최소화한다.

### 2.2 비목표

- 백그라운드에서 상시 주변 소리를 수집하지 않는다.
- 실시간 도청·라이브 스트리밍·항상 켜진 마이크 기능을 제공하지 않는다.
- MVP에서 모든 동물 종을 지원하지 않는다.
- 소리 유사도 점수를 종 동정 확률이나 전문가 판정으로 표현하지 않는다.
- 사람의 동물 소리 흉내를 실제 야생 생물 관찰로 등록하지 않는다.
- 앱이 야생 동물의 반응을 유도하기 위해 큰 소리로 호출음을 반복 재생하게 하지 않는다.
- 낮은 신뢰도 결과를 사용자의 확인 없이 자동 확정하지 않는다.

---

## 3. 기존 앱과의 관계

### 3.1 현재 구조에서 유지해야 할 원칙

- 사진 동정은 기존대로 동작해야 한다.
- 업로드 또는 동정 요청만으로 관찰 기록, 도감, 경험치, 퀘스트 진행을 변경하지 않는다.
- 사용자가 최종 후보를 선택하고 확인한 `confirm` 단계만 부수 효과를 발생시킨다.
- 같은 확인 요청이 재전송되어도 관찰과 보상은 한 번만 생성되어야 한다.
- 서버가 반환한 후보에 없는 종을 클라이언트가 임의로 확정할 수 없어야 한다.
- 지도에는 확정된 실제 주변 소리 관찰만 표시한다.
- 유사도 연습 결과는 지도·도감·퀘스트·경험치에 반영하지 않는다.

### 3.2 현재 데이터 구조에서 필요한 변화

현재 `Observation`과 `IdentifyInput`은 사진 중심이다. 오디오를 이미지 입력에 억지로 포함하지 않고 다음처럼 분리한다.

- `Observation`에는 `modality: "photo" | "audio"`를 추가한다.
- 기존 레코드는 마이그레이션 시 `photo`를 기본값으로 갖는다.
- 사진용 `IdentifyInput`은 변경하지 않는다.
- 별도의 `AudioIdentifyInput`, `AudioIdentificationProvider`를 도입한다.
- 미디어에는 종류, MIME, 길이, 해시를 기록할 수 있어야 한다.
- 현재 식물·균류처럼 소리 동정이 의미 없는 분류군은 오디오 대상에서 명시적으로 제외한다.

---

## 4. 용어

| 용어 | 정의 |
|---|---|
| 녹음 세션 | 사용자가 녹음 시작부터 결과 폐기 또는 확정까지 수행하는 한 번의 흐름 |
| 오디오 관찰 후보 | 아직 확정되지 않은 임시 녹음과 모델 결과 |
| 소리 동정 | 입력 오디오에서 생물 종 후보를 추론하는 기능 |
| 동정 신뢰도 | 모델이 해당 후보를 선택한 정도. 종일 실제 확률과 항상 같지는 않음 |
| 참조 음원 | 출처·라이선스·종 라벨이 검증된 비교 기준 오디오 |
| 임베딩 | 오디오의 특징을 숫자 벡터로 표현한 값 |
| 원시 유사도 | 사용자 녹음과 참조 음원 임베딩 간의 계산값 |
| 유사도 점수 | 원시 유사도, 혼동종 차이, 품질을 교정해 0~100으로 표시한 값 |
| 혼동종 | 소리가 비슷하여 잘못 분류되기 쉬운 다른 종 |
| 품질 게이트 | 길이·무음·클리핑·음성·신호 대 잡음비 등을 검사하는 단계 |
| 주변 소리 모드 | 실제 자연에서 녹음한 생물 소리를 동정·채점하는 모드 |
| 연습 모드 | 사용자가 소리를 흉내 내거나 학습 목적으로 비교하는 모드 |

---

## 5. 지원 범위와 단계별 출시

### 5.1 MVP 범위

- **우선 대상: 조류**
- 앱의 종 데이터에 존재하고 검증된 참조 음원과 모델 지원이 확보된 종만 지원한다.
- 녹음 길이: 최소 3초, 권장 6~10초, 최대 15초
- 한 번의 녹음에서 상위 후보와 후보별 주요 감지 구간을 반환한다.
- 주변 소리 동정 및 주변 소리 유사도 채점을 제공한다.
- 서버 분석 방식으로 시작한다.
- 녹음 원본은 기본적으로 임시 저장하고 일정 시간 후 삭제한다.
- 참조 음원은 앱에서 짧게 미리 들을 수 있다.

### 5.2 2차 범위

- 소리로 구분 가능한 곤충·양서류로 지원 범위를 확대한다.
- 지역·계절·시간 정보를 보조 신호로 사용할 수 있다.
- 하나의 녹음에 여러 종이 포함된 경우 구간별 다중 감지를 제공한다.
- 기기 내 1차 품질 검사 또는 경량 추론을 도입한다.
- 사용자의 명시적 선택에 따라 유사도 점수 기록을 저장한다.

### 5.3 장기 검토

- 연속 녹음에서 자동 하이라이트 추출
- 네트워크가 없는 환경을 위한 기기 내 동정
- 전문가 또는 커뮤니티 검증
- 지역별 맞춤 생물음향 모델
- 개인 학습용 연습 모드

---

## 6. 사용자 진입점

### 6.1 권장 진입 방식

기존 카메라 촬영 화면에 녹음 기능을 섞지 않고, 메인 방사형 메뉴 또는 탐험 화면에 **“소리 찾기”**를 별도 항목으로 추가한다.

권장 이유:

- 카메라의 짧게 누르기·길게 누르기 동작과 충돌하지 않는다.
- 카메라 권한과 마이크 권한을 분리해 설명할 수 있다.
- 사진 촬영 로딩 상태와 녹음 분석 상태가 서로 영향을 주지 않는다.
- 접근성 도구에서 “사진 촬영”과 “소리 녹음”을 명확히 구분할 수 있다.

### 6.2 메뉴 문구

- 기능명: `소리 찾기`
- 보조 설명: `주변 생물의 소리를 녹음해 보세요`
- 아이콘: 마이크 또는 음파
- 사진 동정과 구분되는 색상과 접근성 라벨을 사용한다.

---

## 7. 핵심 사용자 흐름

### 7.1 최초 사용

1. 사용자가 `소리 찾기`를 누른다.
2. 기능 설명과 개인정보 안내를 보여준다.
3. 오디오 녹음 동의를 받는다.
4. OS 마이크 권한을 요청한다.
5. 권한이 승인되면 녹음 화면으로 이동한다.
6. 거부되면 앱 설정으로 이동하는 방법을 보여주되 다른 기능은 계속 사용할 수 있다.

기존 사진 촬영 동의는 오디오 녹음 동의를 대신하지 않는다.

### 7.2 소리 동정

1. 사용자가 녹음 버튼을 누른다.
2. 화면에 녹음 중 상태, 경과 시간, 간단한 음량 반응을 표시한다.
3. 3초 전에는 정지할 수 있지만 분석은 불가능하다는 안내를 표시한다.
4. 사용자가 정지하거나 15초가 되면 자동으로 녹음을 끝낸다.
5. 기기에서 최소 길이, 무음, 심한 클리핑을 우선 검사한다.
6. 통과하면 서버에 임시 업로드한다.
7. 서버가 형식 검증, 변환, 음성 감지, 품질 분석, 모델 추론을 수행한다.
8. 상위 종 후보와 신뢰도 수준, 감지 구간, 안전 정보를 반환한다.
9. 사용자가 후보를 선택한다.
10. 사용자가 `이 종으로 기록하기`를 누른 경우에만 관찰을 확정한다.
11. 확정 후 도감·지도·보상 흐름을 한 번 실행한다.

### 7.3 소리 유사도 채점

유사도 채점 진입 경로는 두 가지다.

- 동정 결과에서 후보를 선택하고 `소리 비교하기`를 누른다.
- 도감의 지원 종 상세 화면에서 `내가 들은 소리 비교`를 누른다.

흐름:

1. 비교할 종을 명확히 표시한다.
2. 기존 녹음을 재사용하거나 새 녹음을 한다.
3. 품질 게이트를 통과한 구간만 비교한다.
4. 여러 참조 음원과 비교하고 혼동종 점수를 함께 계산한다.
5. 0~100점, 등급, 품질 설명, 개선 팁을 보여준다.
6. `다시 녹음`, `참조 소리 듣기`, `동정 결과로 돌아가기`를 제공한다.
7. 유사도 점수 자체로는 관찰이나 보상을 생성하지 않는다.

### 7.4 폐기

- 사용자가 결과 화면을 닫으면 `녹음 삭제` 또는 `임시 보관 후 자동 삭제` 정책을 명확히 적용한다.
- 확정하지 않은 녹음은 기본 24시간 이내 삭제한다.
- 연습 모드 녹음은 분석 완료 직후 삭제하는 것을 기본값으로 한다.

---

## 8. 화면 및 상태 명세

### 8.1 소리 찾기 안내 화면

표시 항목:

- “주변 생물의 소리를 6~10초 녹음해 주세요.”
- “사람의 대화가 들어가지 않게 해 주세요.”
- “동물을 놀라게 하거나 소리를 따라가지 마세요.”
- 마이크 권한 요청 버튼
- 개인정보 처리 안내 링크

### 8.2 녹음 화면

필수 요소:

- 닫기 버튼
- 녹음 시작/정지 버튼
- `00:00 / 00:15` 시간
- 녹음 상태 텍스트
- 음량 반응 또는 파형
- “휴대폰을 소리 나는 방향으로 두고 조용히 기다려 주세요.”
- 설정 버튼은 녹음 버튼과 겹치지 않게 배치

상태:

| 상태 | 표시 |
|---|---|
| 준비 | `주변 소리를 들어볼까요?` |
| 녹음 중 | `소리를 담는 중…` |
| 최소 길이 미달 | `3초 이상 녹음해 주세요` |
| 자동 종료 임박 | `곧 녹음이 끝나요` |
| 로컬 품질 검사 | `소리를 확인하는 중…` |
| 업로드 | `안전하게 보내는 중…` |
| 분석 | `어떤 친구인지 찾는 중…` |
| 네트워크 실패 | `연결을 확인하고 다시 시도해 주세요` |

로딩 표시기는 화면 상태 전환 때 컴포넌트를 제거·재생성하지 않는다. 하나의 고정된 로딩 컨테이너 안에서 문구와 진행 상태만 변경하여 깜빡임을 방지한다.

### 8.3 품질 안내 화면

분석 불가 사유를 하나 이상의 코드로 제공한다.

| 코드 | 사용자 문구 | 행동 |
|---|---|---|
| `TOO_SHORT` | `소리가 너무 짧아요` | 다시 녹음 |
| `MOSTLY_SILENCE` | `생물 소리가 잘 들리지 않아요` | 가까운 안전한 위치에서 재시도 |
| `TOO_NOISY` | `주변 소음이 너무 커요` | 바람·차량 소음 피하기 |
| `CLIPPED` | `소리가 너무 커서 찌그러졌어요` | 기기를 조금 멀리 두기 |
| `SPEECH_DETECTED` | `사람 목소리가 포함되어 분석하지 않았어요` | 대화가 없는 상태에서 재시도 |
| `MULTIPLE_OVERLAP` | `여러 소리가 겹쳐 정확히 찾기 어려워요` | 한 소리가 잘 들릴 때 재시도 |
| `UNSUPPORTED_SOUND` | `아직 지원하지 않는 소리예요` | 녹음 삭제 또는 피드백 |
| `NO_TARGET_ACTIVITY` | `비교할 생물 소리 구간을 찾지 못했어요` | 다시 녹음 |

### 8.4 동정 결과 화면

필수 표시:

- 가장 가능성 높은 후보 최대 3개
- 종의 한국어 이름과 학명
- 후보별 신뢰도 단계: `높음`, `보통`, `낮음`
- 소리가 감지된 대표 구간
- 위험 생물 여부와 안전 행동
- `이 종으로 기록하기`
- `다른 후보 보기`
- `소리 비교하기`
- `다시 녹음`
- `잘 모르겠어요`

금지 표현:

- 교정되지 않은 모델 출력을 `정확도 98%`처럼 확률로 단정하지 않는다.
- 낮은 신뢰도인데 종을 확정적으로 표현하지 않는다.
- 위험 종을 잡거나 가까이 접근하도록 유도하지 않는다.

### 8.5 유사도 결과 화면

필수 표시:

- `OOO 소리와 78점 비슷해요`
- 등급
- “이 점수는 종일 확률이 아니라 참조 소리와의 유사도입니다.”
- 가장 잘 맞은 소리 구간
- 점수에 영향을 준 품질 피드백
- 비슷한 혼동종이 있으면 주의 문구
- `참조 소리 듣기`
- `다시 녹음`

제안 등급:

| 점수 | 표시 | 의미 |
|---:|---|---|
| 0~39 | `낮은 유사도` | 참조 소리와 특징이 많이 다르거나 품질이 낮음 |
| 40~64 | `조금 비슷해요` | 일부 특징이 비슷하지만 혼동 가능성이 큼 |
| 65~84 | `많이 비슷해요` | 여러 음향 특징이 유사함 |
| 85~100 | `매우 비슷해요` | 검증된 참조 집합과 강하게 유사함 |

이 구간은 출시 전 검증 데이터로 재교정한다. 모델의 원시 코사인 유사도를 위 구간에 바로 대입하지 않는다.

---

## 9. 소리 녹음 요구사항

### 9.1 녹음 설정

- 앱 플랫폼: Android/iOS
- 녹음 길이: 3~15초
- 권장 길이: 6~10초
- 채널: 단일 채널로 정규화
- 서버 표준 형식: 모델이 요구하는 샘플레이트의 mono PCM
- 기본 변환 목표: 48 kHz mono PCM WAV
- 앱 업로드 허용 형식: 플랫폼이 생성하는 M4A/MP4 계열 및 WAV
- 파일 크기 제한: 10 MB
- 서버는 확장자나 클라이언트 MIME만 신뢰하지 않고 파일 시그니처를 검사한다.
- 분석 전 DC offset 제거, 채널 변환, 리샘플링, 과도한 정규화 방지를 수행한다.

앱은 Expo SDK 57의 공식 오디오 모듈인 [`expo-audio`](https://docs.expo.dev/versions/v57.0.0/sdk/audio/) 사용을 전제로 한다. 실제 설치 시 SDK 호환 버전을 Expo 설치 명령으로 결정한다.

### 9.2 녹음 제어

- 앱이 포그라운드에 있을 때만 녹음한다.
- 녹음 중임을 시각적으로 계속 표시한다.
- 전화, 오디오 장치 분리, 앱 백그라운드 전환 시 안전하게 중지한다.
- 백그라운드 녹음 권한은 요청하지 않는다.
- 동시에 두 개의 녹음 세션을 시작할 수 없다.
- 분석 중 새 녹음을 시작하면 이전 요청을 취소하거나 별도 세션으로 명확히 분리한다.

### 9.3 네트워크

- MVP에서는 서버 분석을 위해 네트워크가 필요하다.
- 연결 실패 시 사용자가 명시적으로 재시도할 수 있다.
- 무한 자동 재시도는 하지 않는다.
- 동일한 `client_recording_id` 재전송은 새 녹음으로 중복 저장하지 않는다.
- 업로드 대기 중 로그아웃하면 임시 파일과 큐를 삭제한다.

---

## 10. 오디오 품질 검사

### 10.1 검사 순서

1. 파일 형식 및 크기 검증
2. 디코딩 가능 여부
3. 길이 검사
4. 무음 비율
5. 클리핑 비율
6. 신호 대 잡음비 추정
7. 사람 음성 감지
8. 목표 생물음 활동 구간 탐지
9. 겹치는 음원 또는 과도한 배경음 평가
10. 유효 구간 추출

### 10.2 결과 구조

```ts
type AudioQualityResult = {
  usable: boolean;
  durationMs: number;
  activeDurationMs: number;
  snrDb?: number;
  clippingRatio: number;
  silenceRatio: number;
  speechRatio?: number;
  feedbackCodes: AudioQualityFeedbackCode[];
  validSegments: Array<{
    startMs: number;
    endMs: number;
    qualityScore: number; // 0..1
  }>;
};
```

### 10.3 사람 음성 처리

- 사람 음성이 우세하거나 연속 대화가 감지되면 분석하지 않는다.
- 해당 파일은 장기 저장하지 않는다.
- 음성 전사 기능은 사용하지 않는다.
- 음성 내용을 로그에 기록하지 않는다.
- 음성 검출 결과는 비율과 사유 코드만 남기고 음성 특징 벡터는 보관하지 않는다.
- 추후 연구·학습 목적 사용은 별도 명시적 동의를 받아야 하며 기본값은 꺼짐이다.

---

## 11. 소리 동정 모델 명세

### 11.1 모델 제공자 인터페이스

사진용 동정 인터페이스와 분리한다.

```ts
type AudioIdentifyInput = {
  canonicalAudioRef: string;
  validSegments: Array<{ startMs: number; endMs: number }>;
  targetGroups: Array<"bird" | "insect" | "amphibian" | "mammal" | "other">;
  region?: {
    latitudeBucket?: number;
    longitudeBucket?: number;
    countryCode?: string;
  };
  observedAt?: string;
};

type AudioIdentificationCandidate = {
  taxonId: string;
  scientificName: string;
  commonNameKo: string;
  confidence: number; // 교정된 0..1 값
  rawScore: number;
  startMs: number;
  endMs: number;
  group: string;
};

interface AudioIdentificationProvider {
  identify(input: AudioIdentifyInput): Promise<{
    candidates: AudioIdentificationCandidate[];
    modelVersion: string;
    provider: string;
  }>;
}
```

### 11.2 추천 모델 전략

MVP 후보:

- [BirdNET](https://github.com/birdnet-team/birdnet): 조류 종 분류 점수와 임베딩을 제공하므로 빠른 조류 MVP에 적합하다.
- [Perch](https://github.com/google-research/perch): 사전 학습 임베딩과 맞춤 분류기 확장에 적합하여 국내 조류·곤충·양서류 데이터로 확대할 때 유리하다.

권장안:

1. 조류 MVP는 BirdNET 기반 어댑터를 우선 검증한다.
2. 앱이 실제 지원하는 국내 종과 검증 데이터로 후보 점수를 별도 교정한다.
3. Perch 임베딩 기반 맞춤 분류기를 병행 실험한다.
4. 장기적으로 직접 분류 점수와 맞춤 분류기를 앙상블할 수 있게 제공자 인터페이스를 유지한다.

[CLAP](https://github.com/LAION-AI/CLAP)은 범용 오디오·텍스트 임베딩 실험에는 사용할 수 있지만, 검증 없이 종 판정이나 종별 유사도 점수의 기준 모델로 사용하지 않는다.

모델과 데이터셋의 상업적 사용, 재배포, 파생 모델 조건은 구현 착수 전에 별도로 법률·라이선스 검토한다.

### 11.3 추론 처리

- 유효 오디오를 모델 권장 길이의 겹치는 창으로 나눈다.
- 각 창에서 후보와 임베딩을 계산한다.
- 일시적인 단일 피크만으로 높은 신뢰도를 주지 않는다.
- 여러 창에서 반복 검출된 후보에 안정성 가중치를 준다.
- 품질이 낮은 창은 집계 가중치를 낮춘다.
- 동일 종의 연속 구간을 합쳐 대표 감지 구간을 반환한다.
- 상위 후보가 서로 매우 비슷하면 “확인 필요”로 표시한다.
- 지원 종 목록에 없는 후보는 앱의 확정 후보로 반환하지 않는다.

### 11.4 위치·시간 보조 정보

- 위치, 계절, 시간은 후보 우선순위를 조정하는 보조 신호다.
- 음향 증거를 무시하고 위치 정보만으로 후보를 확정해서는 안 된다.
- 위치 권한을 거부해도 소리 동정은 사용할 수 있어야 한다.
- 정밀 위치 대신 지역 버킷 또는 국가 코드 사용을 우선한다.
- 모델 결과에 위치 사전확률을 적용했는지 응답 메타데이터에 기록한다.

### 11.5 신뢰도 정책

초기 표시 정책:

| 교정 신뢰도 | UI | 동작 |
|---:|---|---|
| `>= highThreshold` | 높음 | 후보를 강조하되 사용자 확인 필요 |
| `>= mediumThreshold` | 보통 | 유사 후보와 함께 표시 |
| `< mediumThreshold` | 낮음 | 기록 전 추가 확인을 요구 |
| 모든 후보가 unknown 조건 | 알 수 없음 | 재녹음 또는 미확정 종료 |

`highThreshold`, `mediumThreshold`, unknown 기준은 사진 동정 임계값을 재사용하지 않고 오디오 검증 데이터로 분류군별 교정한다.

---

## 12. 소리 유사도 채점 명세

### 12.1 점수의 의미

유사도 점수는 다음 질문에 답한다.

> “이 녹음의 유효한 생물 소리 구간이 선택한 종의 검증된 참조 음원 집합과 음향적으로 얼마나 비슷한가?”

답하지 않는 질문:

- “이 소리가 해당 종일 확률은 몇 퍼센트인가?”
- “이 관찰이 전문가 검증을 통과했는가?”
- “사용자가 동물 소리를 얼마나 잘 흉내 냈는가?”

### 12.2 참조 음원 집합

종별 참조 집합에는 한 개가 아닌 여러 음원이 필요하다.

필수 메타데이터:

- 종 ID와 학명
- 호출 유형: 울음, 경계음, 노래 등
- 성별·생애 단계: 알려진 경우
- 지역과 계절
- 녹음 품질 등급
- 원본 출처 URL
- 저작자와 표시 문구
- 라이선스와 상업적 사용 가능 여부
- 편집 이력
- 참조 집합 버전
- 임베딩 모델 버전

출시 전 제안 기준:

- 지원 종마다 최소 20개 이상의 검증 구간
- 가능하면 서로 다른 5개 이상의 개체 또는 녹음 세션
- 같은 원본을 잘라 만든 구간이 학습·검증 양쪽에 섞이지 않도록 그룹 분할
- 혼동종 참조 집합도 함께 구성

데이터 확보가 부족한 종은 동정 후보로 제공할 수 있더라도 유사도 채점은 `지원하지 않음`으로 표시한다.

### 12.3 원시 계산

1. 사용자 녹음의 유효 구간별 임베딩을 만든다.
2. 선택한 종의 참조 임베딩들과 코사인 유사도를 계산한다.
3. 가장 가까운 상위 K개 참조의 평균 또는 견고한 집계값을 양성 유사도로 사용한다.
4. 주요 혼동종의 최고 유사도를 구한다.
5. 양성 유사도와 혼동종 유사도의 차이를 `confuserMargin`으로 계산한다.
6. 품질 검사 결과에서 `qualityFactor`를 계산한다.
7. 종별 또는 분류군별 검증 데이터로 보정 함수를 적용한다.

개념식:

```text
positiveSimilarity = robustMean(topK(cosine(user, targetReferences)))
confuserSimilarity = max(cosine(user, confuserReferences))
confuserMargin = positiveSimilarity - confuserSimilarity
calibratedMatch = calibrator(
  positiveSimilarity,
  confuserMargin,
  speciesId,
  modelVersion
)
finalScore = round(100 * calibratedMatch * qualityFactor)
```

`calibrator`의 초기 후보는 로지스틱 회귀 또는 등위 회귀이며, 검증 성능에 따라 선택한다. 모든 종에 같은 선형 환산식을 쓰지 않는다.

### 12.4 품질 반영

품질이 낮을 때 점수만 낮추면 사용자가 “다른 종”으로 오해할 수 있다. 다음 규칙을 적용한다.

- 분석 가능 임계값 미만이면 점수를 아예 만들지 않는다.
- 분석은 가능하지만 품질이 낮으면 점수 옆에 낮은 신뢰성 경고를 표시한다.
- 무음, 클리핑, 음성 비율이 높으면 `qualityFactor`를 제한한다.
- 여러 종이 겹치면 선택한 종의 대표 구간만 비교하거나 재녹음을 권장한다.
- 원시 유사도, 교정값, 품질 계수를 응답에 분리해 디버깅 가능하게 한다.
- 사용자 UI에는 내부 원시 점수를 기본 노출하지 않는다.

### 12.5 연습 모드

연습 모드를 추후 제공할 경우:

- 주변 소리 모드와 시각적으로 명확히 분리한다.
- 사람 음성을 동물 관찰로 등록하지 않는다.
- 도감, 지도, 퀘스트, 경험치를 변경하지 않는다.
- 원본 녹음은 분석 후 즉시 삭제한다.
- 목소리의 생체 식별, 화자 식별, 전사를 하지 않는다.
- 어린 사용자가 고음이나 큰 소리를 무리하게 내도록 유도하지 않는다.
- MVP 범위에서는 연습 모드를 제외한다.

---

## 13. API 명세

### 13.1 공통 원칙

- 모든 사용자 API는 인증이 필요하다.
- 업로드에는 `client_recording_id`를 사용해 중복을 방지한다.
- 임시 녹음은 사용자 소유권을 확인한다.
- 응답에 `model_version`, 필요 시 `reference_set_version`을 포함한다.
- 분석 API는 관찰·보상에 부수 효과를 만들지 않는다.
- 확정 API만 관찰·도감·퀘스트·보상을 변경한다.
- 오류 응답은 사용자 메시지용 코드와 디버깅용 추적 ID를 분리한다.

### 13.2 오디오 업로드

`POST /audio/sightings/upload`

요청:

- Content-Type: `multipart/form-data`
- `audio`: 파일
- `client_recording_id`: UUID
- `duration_ms`: 클라이언트 측 측정값
- `recorded_at`: ISO 8601
- `lat`, `lng`: 권한 및 사용자 선택이 있는 경우에만 전송
- `mode`: `ambient`

응답:

```json
{
  "audio_sighting_id": "as_123",
  "status": "ready",
  "quality": {
    "usable": true,
    "duration_ms": 8120,
    "active_duration_ms": 4210,
    "snr_db": 17.4,
    "clipping_ratio": 0.001,
    "silence_ratio": 0.22,
    "speech_ratio": 0.0,
    "feedback_codes": [],
    "valid_segments": [
      { "start_ms": 1100, "end_ms": 5300, "quality_score": 0.87 }
    ]
  },
  "expires_at": "2026-07-29T10:00:00Z"
}
```

오류:

- `400 AUDIO_INVALID_FORMAT`
- `400 AUDIO_TOO_SHORT`
- `413 AUDIO_TOO_LARGE`
- `422 AUDIO_QUALITY_REJECTED`
- `429 AUDIO_RATE_LIMITED`
- `503 AUDIO_PROCESSOR_UNAVAILABLE`

### 13.3 소리 동정

`POST /audio/identify`

요청:

```json
{
  "audio_sighting_id": "as_123"
}
```

응답:

```json
{
  "audio_sighting_id": "as_123",
  "candidates": [
    {
      "species_id": "bird_001",
      "scientific_name": "Example scientific name",
      "common_name_ko": "예시 새",
      "confidence": 0.82,
      "confidence_level": "high",
      "start_ms": 1100,
      "end_ms": 5300,
      "is_dangerous": false
    }
  ],
  "needs_user_confirmation": true,
  "unknown": false,
  "model_version": "bird-audio-model@1.0.0",
  "location_prior_used": false
}
```

규칙:

- 최대 후보 수 기본값은 3개다.
- 후보는 서버의 지원 종 목록에 존재해야 한다.
- 결과가 부족하면 `unknown: true`, 빈 후보 목록을 반환할 수 있다.
- 모델 장애를 “미확인 종” 결과로 위장하지 않는다.

### 13.4 동정 확정

`POST /audio/identify/confirm`

요청:

```json
{
  "audio_sighting_id": "as_123",
  "species_id": "bird_001",
  "confirmation_id": "uuid"
}
```

응답:

```json
{
  "observation_id": "obs_456",
  "modality": "audio",
  "species_id": "bird_001",
  "dex_updated": true,
  "reward": {
    "xp": 10,
    "quest_ids": []
  }
}
```

규칙:

- 해당 `audio_sighting_id`의 후보 중 하나만 확정할 수 있다.
- 동일 `confirmation_id` 또는 같은 임시 관찰의 재확정은 동일 결과를 반환한다.
- 보상은 정확히 한 번 지급한다.
- 위치 저장은 기존 사용자 설정과 오디오 동의를 모두 따른다.
- 원본 녹음 저장에 동의하지 않은 경우, 관찰에는 원본 대신 파생 특징 또는 짧은 비식별 대표 구간만 저장하는 정책을 선택할 수 있다.

### 13.5 유사도 채점

`POST /audio/similarity/score`

요청:

```json
{
  "audio_sighting_id": "as_123",
  "species_id": "bird_001",
  "mode": "ambient"
}
```

응답:

```json
{
  "species_id": "bird_001",
  "score": 78,
  "grade": "very_similar",
  "score_reliability": "high",
  "matched_segment": {
    "start_ms": 1100,
    "end_ms": 5300
  },
  "feedback_codes": ["BACKGROUND_NOISE_MILD"],
  "raw_similarity": 0.73,
  "confuser_margin": 0.18,
  "quality_factor": 0.94,
  "model_version": "bird-audio-embedding@1.0.0",
  "reference_set_version": "kr-bird-reference@2026-07"
}
```

규칙:

- API 자체는 관찰·도감·보상을 변경하지 않는다.
- 동일 입력, 모델 버전, 참조 집합 버전이면 허용 오차 내에서 같은 점수를 반환한다.
- 참조 데이터가 부족하면 `422 SIMILARITY_NOT_SUPPORTED_FOR_SPECIES`를 반환한다.
- 음성 또는 품질 게이트에 실패하면 점수를 반환하지 않는다.
- `raw_similarity`는 내부 또는 디버그 권한에서만 내려주는 옵션으로 전환할 수 있다.

### 13.6 참조 소리

`GET /species/:speciesId/sounds`

응답:

```json
{
  "species_id": "bird_001",
  "supported_for_similarity": true,
  "clips": [
    {
      "id": "ref_001",
      "call_type": "song",
      "duration_ms": 5000,
      "playback_url": "signed-url",
      "attribution": "Recorder name",
      "license": "license-id",
      "source_url": "https://example.org/original"
    }
  ],
  "reference_set_version": "kr-bird-reference@2026-07"
}
```

### 13.7 임시 녹음 삭제

`DELETE /audio/sightings/:audioSightingId`

- 사용자가 명시적으로 폐기할 때 즉시 삭제한다.
- 삭제 후 동정 또는 유사도 요청은 `410 AUDIO_SIGHTING_GONE`을 반환한다.
- 이미 확정된 관찰 미디어의 삭제는 기존 데이터 권리 삭제 절차를 따른다.

---

## 14. 데이터 모델

### 14.1 Observation 확장

```ts
type ObservationModality = "photo" | "audio";

type Observation = {
  // 기존 필드 유지
  modality: ObservationModality;
};
```

마이그레이션:

- 기존 레코드: `modality = "photo"`
- 신규 사진 관찰: 명시적으로 `photo`
- 확정된 주변 소리 관찰: `audio`
- 연습 모드: Observation을 만들지 않음

### 14.2 observation_media 확장

제안 필드:

| 필드 | 타입 | 설명 |
|---|---|---|
| `media_kind` | enum | `image`, `audio` |
| `mime_type` | string | 검증 후 확정한 MIME |
| `duration_ms` | integer nullable | 오디오 길이 |
| `sha256` | string | 중복 및 무결성 확인 |
| `retention_class` | enum | `temporary`, `observation`, `reference` |
| `derived_from_media_id` | nullable | 변환본과 원본 관계 |

### 14.3 audio_sighting

임시 분석 세션 테이블 또는 동등한 저장소:

| 필드 | 설명 |
|---|---|
| `id` | 서버 오디오 세션 ID |
| `user_id` | 소유 사용자 |
| `client_recording_id` | 멱등 키 |
| `status` | uploaded, quality_checked, identified, rejected, confirmed, deleted, expired |
| `original_media_ref` | 암호화된 임시 원본 |
| `canonical_media_ref` | 분석용 변환본 |
| `quality_json` | 품질 결과 |
| `recorded_at` | 녹음 시간 |
| `coarse_region` | 선택적 지역 버킷 |
| `precise_coord` | 명시적 위치 동의가 있을 때만 |
| `expires_at` | 자동 삭제 시각 |
| `created_at`, `updated_at` | 감사 시각 |

제약:

- `(user_id, client_recording_id)` unique
- 사용자 간 접근 금지
- `expires_at` 인덱스
- 확인된 관찰과의 연결은 한 번만 생성

### 14.4 audio_identification_result

| 필드 | 설명 |
|---|---|
| `audio_sighting_id` | 대상 임시 녹음 |
| `candidates_json` | 당시 반환 후보 |
| `model_provider` | 제공자 |
| `model_version` | 모델 버전 |
| `location_prior_used` | 위치 보조 사용 여부 |
| `created_at` | 추론 시각 |

후보 스냅샷을 저장해야 확정 시 클라이언트가 임의 후보를 주입하는 것을 막을 수 있다.

### 14.5 species_sound_reference

| 필드 | 설명 |
|---|---|
| `id` | 참조 음원 ID |
| `taxon_id` | 종 ID |
| `media_ref` | 저장 위치 |
| `call_type` | 소리 유형 |
| `region`, `season` | 선택 메타데이터 |
| `source_url` | 원본 링크 |
| `creator` | 녹음자 |
| `license` | 라이선스 |
| `attribution` | 표시 문구 |
| `quality_status` | pending, approved, rejected |
| `reference_set_version` | 참조 집합 버전 |
| `embedding_ref` | 임베딩 위치 |
| `embedding_model_version` | 임베딩 모델 버전 |

### 14.6 audio_similarity_score

기본 정책은 점수를 세션 응답으로만 반환하고 영구 저장하지 않는 것이다. 사용자가 기록 저장을 선택하는 기능을 추가할 경우:

| 필드 | 설명 |
|---|---|
| `id` | 점수 ID |
| `user_id` | 사용자 |
| `taxon_id` | 비교 종 |
| `observation_id` | 주변 관찰인 경우 선택 연결 |
| `score` | 0~100 |
| `grade` | 표시 등급 |
| `quality_factor` | 품질 계수 |
| `model_version` | 모델 버전 |
| `reference_set_version` | 참조 집합 버전 |
| `created_at` | 생성 시각 |

연습 모드 원본 또는 음성 임베딩은 영구 저장하지 않는다.

### 14.7 consent_record 확장

별도 동의 항목:

- `audio_recording`
- `audio_processing`
- `audio_location_linking`
- `audio_training_use` — 기본 꺼짐, 선택 동의

각 동의는 버전, 시각, 철회 시각을 기록한다.

---

## 15. 도감·지도·퀘스트·보상 연동

### 15.1 도감

- 확정된 주변 소리 관찰은 해당 종 발견 횟수에 반영한다.
- 상세 화면에서 사진 관찰과 소리 관찰을 구분한다.
- 가능하면 `사진으로 발견`, `소리로 발견` 배지를 별도 표시한다.
- 유사도 점수만 받은 경우 발견 처리하지 않는다.

### 15.2 지도

- 위치 동의가 있고 확정된 주변 소리 관찰만 지도에 표시한다.
- 사진과 구분되는 소리 아이콘을 사용한다.
- 공개 지도에는 정밀 좌표를 노출하지 않고 기존 위치 프라이버시 정책을 따른다.
- 희귀종·민감종은 위치를 더 크게 흐리거나 비공개 처리한다.

### 15.3 퀘스트

퀘스트마다 허용 관찰 수단을 명시한다.

```ts
type AllowedObservationModality = "photo" | "audio" | "either";
```

- 기존 퀘스트는 기본값을 `photo`로 두어 동작 변화를 막는다.
- 신규 소리 퀘스트만 `audio` 또는 `either`를 사용한다.
- 연습 모드와 유사도 채점은 퀘스트 카운트에 포함하지 않는다.

### 15.4 보상

- 확정된 주변 소리 관찰에만 지급한다.
- 같은 녹음 재확정, 네트워크 재시도, 앱 재시작에도 한 번만 지급한다.
- 동일 파일을 반복 업로드해 보상을 얻는 행위를 해시와 사용자별 시간 제한으로 방지한다.
- 오디오 도입으로 기존 사진 보상량이 변경되지 않게 한다.

---

## 16. 안전·개인정보·윤리

### 16.1 마이크 안전

- 마이크는 사용자가 버튼을 누른 뒤에만 활성화한다.
- 백그라운드 녹음은 금지한다.
- 녹음 중임을 화면에 항상 표시한다.
- 녹음 종료 시 오디오 세션을 확실히 해제한다.
- 권한 거부·철회 후에도 사진, 지도, 도감은 정상 동작해야 한다.

### 16.2 사람 목소리

- 사람 목소리가 포함될 수 있음을 녹음 전에 알린다.
- 음성 우세 파일은 거부하고 즉시 삭제한다.
- 음성 전사, 화자 식별, 감정 분석을 하지 않는다.
- 원본 오디오를 모델 학습에 자동 사용하지 않는다.
- 학습 사용 동의는 서비스 사용 동의와 분리한다.
- 어린이 계정에는 더 보수적인 저장 정책을 적용한다.

### 16.3 위치 정보

- 마이크 동의와 위치 동의를 묶지 않는다.
- 위치를 제공하지 않아도 동정과 채점은 가능해야 한다.
- 모델에는 정밀 좌표보다 지역 버킷을 우선 전달한다.
- 민감종 위치는 지도 공개 범위를 제한한다.
- 데이터 내보내기와 삭제 대상에 오디오 및 연결 위치를 포함한다.

### 16.4 생태 안전

- 동물을 잡거나 접근하도록 안내하지 않는다.
- 위험 종 결과에는 거리 유지와 보호자에게 알리기를 우선 안내한다.
- 야외에서 참조 호출음을 반복 재생하지 않도록 경고한다.
- 참조 소리 재생은 낮은 기본 볼륨과 헤드폰 권장을 제공한다.
- 번식기·보호구역에서 호출 재생을 유도하는 게임 요소를 만들지 않는다.

### 16.5 저작권

- 모든 참조 음원에 출처, 저작자, 라이선스를 기록한다.
- 앱 내 재생·서버 변환·임베딩 생성·상업적 배포가 허용되는지 확인한다.
- 표시 의무가 있는 음원은 재생 화면에 저작자와 원본 링크를 제공한다.
- 라이선스가 철회되거나 만료된 음원은 참조 집합에서 제외하고 새 버전을 발행한다.

---

## 17. 보안 및 운영 요구사항

- 업로드 파일의 실제 포맷과 디코딩 가능 여부를 검사한다.
- 허용된 오디오 포맷만 서버 변환기로 전달한다.
- 변환 프로세스는 제한된 CPU, 메모리, 실행 시간 안에서 격리한다.
- 파일명은 서버 저장 경로로 직접 사용하지 않는다.
- 저장 객체는 추측 불가능한 ID를 사용한다.
- 재생 URL은 짧은 만료 시간을 가진 서명 URL을 사용한다.
- 사용자별·IP별 업로드 및 분석 속도 제한을 둔다.
- 비정상적으로 동일한 파일의 반복 분석을 차단한다.
- 로그에는 원본 오디오, 정밀 좌표, 서명 URL을 남기지 않는다.
- 분석 작업에는 추적 ID를 사용하되 개인정보와 분리한다.
- 임시 파일 삭제 작업을 주기적으로 실행하고 삭제 실패를 모니터링한다.
- 모델 장애, 변환기 장애, 저장소 장애를 서로 다른 오류로 계측한다.
- 모델 교체는 버전 고정, 그림자 평가, 점진 배포, 롤백 절차를 갖는다.

---

## 18. 성능 요구사항

제안 출시 목표:

| 항목 | 목표 |
|---|---|
| 녹음 버튼 반응 | 200 ms 이내 시각적 반응 |
| 로컬 녹음 종료 처리 | P95 500 ms 이내 |
| 10초·10 MB 이하 파일 업로드 | 일반 Wi-Fi 기준 P95 3초 이내 |
| 서버 품질 검사 + 조류 동정 | GPU 환경 P95 5초 이내 |
| 유사도 채점 | 캐시된 참조 임베딩 기준 P95 3초 이내 |
| 중복 확인 요청 | 추가 관찰/보상 0건 |
| 앱 분석 화면 | 로딩 표시 깜빡임 0회 |

네트워크 시간을 포함한 최종 목표는 실제 CAMP-3(GPU) 환경과 모바일 네트워크 측정 후 확정한다.

---

## 19. 모델 품질과 출시 기준

### 19.1 평가 데이터

- 국내 실제 사용 환경에서 수집한 독립 검증 세트를 사용한다.
- 공원, 도로변, 실내 창가, 바람, 비, 사람 목소리 등 환경을 포함한다.
- 같은 원본·같은 개체의 구간이 학습과 검증에 동시에 들어가지 않게 한다.
- 지원 종뿐 아니라 혼동종, 비생물 소리, 미지원 종을 포함한다.
- 기기 종류와 Android/iOS 녹음 차이를 포함한다.

### 19.2 동정 출시 기준

제안 기준:

- 높은 신뢰도 구간의 정밀도 95% 이상
- 지원 종 기준 Top-3 재현율 90% 이상
- 미지원·비생물 소리의 잘못된 확정 후보 수용률 5% 이하
- 한국어 성인·어린이 음성 검증 세트에서 음성 우세 녹음 감지 재현율 95% 이상
- 종별 성능과 혼동 행렬을 검토하고 성능 미달 종은 지원 목록에서 제외
- 모델 점수의 신뢰도 보정 오류를 측정하고 릴리스마다 비교

위 수치는 초기 출시 게이트 제안이며 데이터 규모와 제품 위험 검토 후 확정한다.

### 19.3 유사도 출시 기준

- 같은 종과 주요 혼동종을 구분하는 AUC 0.90 이상
- 동일 표준 파일 반복 채점 결과 차이 ±3점 이내
- 동일 종 내 품질 좋은 녹음이 혼동종보다 통계적으로 높은 점수를 가져야 함
- 종별 점수 분포가 특정 참조 녹음자나 장비에 과도하게 편향되지 않음
- 모델 또는 참조 집합 버전 변경 시 회귀 평가 통과
- 점수 등급별 실제 같은 종 비율과 사용자 해석을 별도 검증

### 19.4 공정성과 편향

- 특정 지역, 계절, 성별, 성장 단계의 소리만 참조하지 않는다.
- 녹음 장비가 다른 경우 성능 차이를 측정한다.
- 참조 수가 적은 종에 과도하게 높은 점수를 주지 않는다.
- 위치 사전확률 때문에 드문 종이 항상 배제되지 않는지 검사한다.

---

## 20. 오류 및 예외 처리

| 상황 | 처리 |
|---|---|
| 마이크 권한 거부 | 설정 안내 후 이전 화면 복귀, 다른 기능 정상 유지 |
| 녹음 중 전화/오디오 인터럽트 | 안전하게 중지하고 재시도 안내 |
| 앱 백그라운드 전환 | 녹음 중지, 불완전 파일 폐기 |
| 블루투스 장치 분리 | 녹음 중지 또는 내장 마이크로 전환 여부를 명확히 안내 |
| 3초 미만 녹음 | 서버 업로드 전 차단 |
| 15초 초과 | 자동 종료 |
| 업로드 실패 | 사용자 선택 재시도, 중복 방지 |
| 서버 분석 시간 초과 | 작업 취소 또는 상태 조회, 관찰 생성 금지 |
| 모델 응답 없음 | 서비스 오류 표시, “종 없음”으로 처리하지 않음 |
| 지원하지 않는 종 | 지원 범위 안내, 강제 확정 금지 |
| 여러 종이 겹침 | 구간별 후보 또는 재녹음 안내 |
| 사람 음성 우세 | 분석 중단 및 원본 삭제 |
| 참조 음원 없음 | 동정 결과는 가능하되 유사도 버튼 비활성화 |
| 확정 API 재전송 | 동일 관찰 반환, 중복 보상 없음 |
| 로그아웃/계정 삭제 | 임시 녹음, 점수 기록, 연결 관찰을 정책에 따라 삭제 |

---

## 21. 분석 이벤트와 지표

원본 오디오나 정밀 위치 없이 다음 이벤트를 수집한다.

| 이벤트 | 주요 속성 |
|---|---|
| `audio_feature_opened` | 진입 경로 |
| `audio_permission_result` | granted/denied |
| `audio_recording_started` | 플랫폼 |
| `audio_recording_finished` | 길이 버킷 |
| `audio_quality_rejected` | 사유 코드 |
| `audio_upload_failed` | 오류 코드, 네트워크 유형 |
| `audio_identification_completed` | 후보 수, 신뢰도 단계, 모델 버전 |
| `audio_candidate_confirmed` | 후보 순위, modality |
| `audio_identification_abandoned` | 화면 단계 |
| `audio_similarity_requested` | 종 ID, 진입 경로 |
| `audio_similarity_completed` | 점수 구간, 품질 구간, 버전 |
| `audio_reference_played` | 참조 ID, 재생 길이 |
| `audio_temp_delete_failed` | 저장소, 오류 코드 |

관찰할 제품 지표:

- 녹음 시작 대비 분석 완료율
- 품질 거부율과 주요 사유
- 동정 후보 확인율
- `알 수 없음` 비율
- 후보 1위가 아닌 후보를 선택한 비율
- 유사도 재녹음 비율
- 오디오 관찰의 중복 보상 발생 건수
- 임시 파일 삭제 지연
- 모델 버전별 동정·유사도 분포 변화

---

## 22. 테스트 명세

### 22.1 앱 단위 테스트

- 녹음 상태 머신 전이
- 최소/최대 길이 처리
- 권한 허용·거부·철회
- 녹음 중 화면 이탈 및 앱 백그라운드 전환
- 로딩 컨테이너가 상태 변화 중 유지되는지 확인
- 동일 녹음 재시도 시 `client_recording_id` 유지
- 사진 업로드 큐와 오디오 업로드 큐가 상태를 공유하지 않는지 확인
- 로그아웃 시 임시 오디오 삭제

### 22.2 서버 단위 테스트

- 오디오 MIME 위조 및 손상 파일 거부
- 길이·크기 제한
- 변환기 타임아웃
- 품질 사유 코드 생성
- 사람 음성 우세 파일 거부
- 미지원 종 필터링
- 후보 스냅샷에 없는 종 확정 거부
- 확정 멱등성
- 보상 정확히 한 번 지급
- 임시 파일 TTL 삭제
- 모델/참조 버전 응답 포함

### 22.3 모델 테스트

- 지원 종별 표준 평가
- 주요 혼동종 쌍
- 무음, 차량, 바람, 음악, 말소리, TV 소리
- 여러 종 동시 발생
- 짧은 단일 울음과 연속 노래
- 다양한 스마트폰 및 마이크 거리
- 위치 사전확률 사용 전후 비교
- 참조 집합 교체 전후 점수 회귀

### 22.4 통합 테스트

1. 녹음 → 업로드 → 동정 → 후보 확정 → 관찰 1건 생성
2. 동일 확정 3회 재전송 → 관찰·보상 각각 1건
3. 동정까지만 수행 → 관찰·도감·보상 변경 없음
4. 유사도만 수행 → 관찰·도감·보상 변경 없음
5. 음성 우세 녹음 → 점수 없음, 원본 삭제
6. 네트워크 단절 후 재시도 → 중복 업로드 없음
7. 사진 동정 수행 → 기존 동작과 결과 불변
8. 지도 로딩 → 기존 사진 핀 정상, 오디오 핀은 확정 건만 표시
9. 계정 삭제 → 오디오 및 연결 데이터 삭제

### 22.5 수동 QA

- 실제 Android/iOS 기기
- 유선·블루투스 헤드셋 연결/분리
- 저전력 모드
- 느린 네트워크와 패킷 손실
- 화면 회전과 다양한 해상도
- TalkBack/VoiceOver 접근성
- 글자 크기 확대
- CAMP-3(GPU) 모델 응답 지연 및 장애

---

## 23. 기존 코드 변경 영향 범위

구현 시 검토할 주요 영역이다. 이 문서는 코드 변경을 수행하지 않는다.

### 23.1 앱

- `app/package.json`
  - SDK 호환 `expo-audio` 의존성 추가
- `app/app.json`
  - Android/iOS 마이크 권한 설명과 오디오 설정 추가
  - 백그라운드 녹음은 활성화하지 않음
- 내비게이션 타입과 메인 메뉴
  - `SoundExplore` 또는 동등한 별도 화면 추가
- 신규 화면
  - 소리 찾기 안내
  - 녹음
  - 품질 피드백
  - 소리 동정 결과
  - 유사도 결과
  - 참조 음원 재생
- 신규 모듈
  - `api/audio`
  - `hooks/useAudioRecorder`
  - `stores/audioUploadQueue`
  - 오디오 상태 머신
- 기존 타입
  - `Observation.modality`
  - 미디어 종류와 길이
  - 퀘스트 허용 관찰 방식
- 설정
  - 오디오 동의, 데이터 삭제, 위치 연결 설정

### 23.2 서버

- 오디오 업로드 라우트
- 파일 검증 및 격리된 변환기
- `AudioIdentificationProvider`
- BirdNET/Perch 어댑터
- 품질 검사 서비스
- 유사도 계산·교정 서비스
- 참조 음원 저장소와 라이선스 메타데이터
- 임시 오디오 TTL 정리 작업
- Observation 확정 흐름의 modality 확장
- 도감·지도·퀘스트·보상 규칙
- 데이터 권리 내보내기·삭제 확장
- DB 마이그레이션과 인덱스
- 모델 버전과 참조 집합 버전 설정

### 23.3 배포 환경

- 오디오 변환기 사용 가능 여부
- CAMP-3(GPU)의 모델 런타임과 GPU 메모리
- 모델 파일 저장·캐시 경로
- 임시 오디오 저장 용량과 TTL
- 작업 큐 또는 동시성 제한
- GPU 미사용 시 CPU 폴백과 타임아웃
- 상태 확인 엔드포인트에 오디오 모델 readiness 추가

---

## 24. 구현 순서

### 단계 0 — 결정과 데이터 준비

- MVP 지원 조류 목록 확정
- 모델·데이터·참조 음원 라이선스 검토
- 검증 데이터셋 구성
- 음성·품질 정책 확정
- 원본 오디오 보관 여부와 기간 확정

### 단계 1 — 독립적인 오디오 기반 구성

- 앱 마이크 권한 및 녹음 상태 머신
- 서버 업로드·형식 검증·변환
- 임시 저장과 TTL 삭제
- 품질 검사
- 기존 사진 기능 회귀 테스트

### 단계 2 — 소리 동정

- `AudioIdentificationProvider`
- 조류 모델 연결
- 지원 종 매핑과 신뢰도 교정
- 후보 결과 UI
- 확정 API와 관찰 modality 연결
- 중복 보상 방지 검증

### 단계 3 — 유사도 채점

- 검증된 참조 음원 저장소
- 참조 임베딩 사전 계산
- 혼동종 집합 정의
- 교정기 학습과 버전 관리
- 점수·설명 UI
- 유사도 비부수효과 테스트

### 단계 4 — 제한 출시

- 내부 사용자 → 소규모 테스트 그룹 → 점진 확대
- 품질 거부율, unknown 비율, 잘못된 높은 신뢰도 결과 검토
- 임시 파일 삭제와 개인정보 로그 감사
- 모델 및 참조 집합 회귀 평가

### 단계 5 — 대상 확대

- 곤충·양서류별 별도 검증
- 분류군별 모델·임계값·참조 집합 적용
- 충분한 품질을 통과한 종만 순차 공개

---

## 25. 완료 조건

기능은 다음 조건을 모두 만족해야 완료로 본다.

- 사용자가 마이크 권한을 직접 허용한 뒤에만 녹음된다.
- 백그라운드 또는 상시 녹음이 없다.
- 3~15초 녹음, 품질 검사, 업로드, 동정 결과가 안정적으로 동작한다.
- 사람 목소리 우세 녹음은 분석·장기 저장되지 않는다.
- 지원 종 외 결과를 확정할 수 없다.
- 사용자의 명시적 확인 전에 관찰·도감·지도·퀘스트·보상이 바뀌지 않는다.
- 동일 확인 요청이 반복되어도 관찰과 보상은 한 번만 생성된다.
- 유사도 점수가 확률이 아니라는 설명이 항상 함께 표시된다.
- 유사도 점수는 여러 참조 음원, 혼동종, 품질, 교정기를 반영한다.
- 모델 및 참조 집합 버전이 결과에 기록된다.
- 확정하지 않은 녹음이 보관 정책에 따라 자동 삭제된다.
- 기존 사진 동정, 촬영 로딩, 지도, 도감, 배치 기능의 회귀 테스트가 통과한다.
- Android와 iOS 실제 기기에서 권한·인터럽트·네트워크 실패 테스트가 통과한다.
- 모델 품질, 개인정보, 라이선스 출시 게이트를 모두 통과한다.

---

## 26. 구현 전 확정할 의사결정

| 항목 | 권장 기본안 | 결정 필요 |
|---|---|---|
| MVP 분류군 | 조류만 | 예 |
| MVP 모델 | BirdNET 어댑터 우선, Perch 병행 평가 | 예 |
| 분석 위치 | 서버 | 예 |
| 녹음 최대 길이 | 15초 | 예 |
| 미확정 원본 보관 | 최대 24시간 후 삭제 | 예 |
| 확정 관찰 원본 보관 | 별도 동의 시에만 | 예 |
| 위치 사전확률 | 선택적 지역 버킷만 | 예 |
| 소리 관찰 보상 | 확정된 주변 녹음에만 1회 | 예 |
| 기존 퀘스트 인정 | 기본 사진만 | 예 |
| 연습 모드 | MVP 제외 | 예 |
| 종별 참조 최소 수 | 20구간/5세션 제안 | 데이터에 따라 조정 |
| 점수 등급 구간 | 0~39/40~64/65~84/85~100 제안 | 교정 후 확정 |
| 참조 소리 재생 | 낮은 기본 볼륨 + 헤드폰 권장 | 예 |

---

## 27. 참고 자료

- [Expo Audio SDK 57 공식 문서](https://docs.expo.dev/versions/v57.0.0/sdk/audio/)
- [BirdNET 공식 GitHub 저장소](https://github.com/birdnet-team/birdnet)
- [BirdNET 공식 프로젝트 사이트](https://birdnet.cornell.edu/)
- [Google Research Perch 공식 GitHub 저장소](https://github.com/google-research/perch)
- [Google Research Perch 2.0 연구 소개](https://research.google/pubs/perch-20-the-bittern-lesson-for-bioacoustics/)
- [LAION CLAP 공식 GitHub 저장소](https://github.com/LAION-AI/CLAP)

---

## 28. 후속 AI 작업 지시 요약

다른 AI가 이 명세를 바탕으로 구현할 때 반드시 지킬 사항:

1. 사진용 동정 입력에 오디오를 끼워 넣지 말고 별도 제공자와 API를 만든다.
2. 먼저 DB 및 도메인에 `modality`를 추가하되 기존 레코드와 사진 기능은 `photo`로 유지한다.
3. `upload → quality → identify → user confirm` 단계를 분리한다.
4. `confirm` 외 단계에서는 관찰·도감·퀘스트·보상을 절대 변경하지 않는다.
5. 유사도 점수를 원시 코사인 유사도에 100을 곱해 만들지 않는다.
6. 여러 참조, 혼동종 차이, 품질 계수, 검증 데이터 교정을 사용한다.
7. 사람 음성 우세 녹음은 거부하고 원본을 삭제한다.
8. 원본 녹음의 학습 사용은 별도 선택 동의가 없으면 금지한다.
9. 오디오 지원 종과 유사도 지원 종을 서버 설정으로 명시적으로 관리한다.
10. 모델·참조 집합 버전을 모든 결과와 회귀 테스트에 포함한다.
11. 기존 사진 촬영 로딩, 지도, 도감, 배치, 보상 흐름의 회귀 테스트를 먼저 추가한다.
12. 기능 플래그로 제한 배포하고 품질과 삭제 작업을 확인한 뒤 확대한다.
